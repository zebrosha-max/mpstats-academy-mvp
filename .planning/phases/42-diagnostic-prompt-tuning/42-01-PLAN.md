---
phase: 42-diagnostic-prompt-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ai/src/question-generator.ts
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03
  - PROMPT-04
  - PROMPT-05
  - PROMPT-06
must_haves:
  truths:
    - "System prompt содержит правила маппинга тем к осям (SEO→Маркетинг, бюджет→Финансы и т.д.)"
    - "System prompt запрещает вопросы о курсе, сертификатах, плагинах, биддере"
    - "System prompt требует правдоподобные варианты ответов без абсурда"
    - "System prompt требует указание МП (WB/Ozon) для платформенно-специфичных вопросов"
    - "System prompt запрещает выдуманные термины"
    - "System prompt требует практические вопросы про алгоритм действий"
  artifacts:
    - path: "packages/ai/src/question-generator.ts"
      provides: "Updated buildSystemPrompt function with 6 rule blocks from Mila review"
      contains: "ПРАВИЛА ВЫБОРА РУБРИКИ"
  key_links:
    - from: "packages/ai/src/question-generator.ts"
      to: "LLM API call"
      via: "buildSystemPrompt() return value passed to callLLM()"
      pattern: "buildSystemPrompt"
---

<objective>
Улучшить system prompt генерации диагностических вопросов на основе 12 замечаний Милы к 15 вопросам. Добавить 6 блоков правил: маппинг тем→осей, негативные примеры, качество ответов, контекст МП, терминология, стиль вопросов.

Purpose: Повысить качество AI-генерируемых вопросов диагностики — корректные рубрики, релевантные темы, профессиональная формулировка.
Output: Обновленный `buildSystemPrompt()` в question-generator.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-diagnostic-prompt-tuning/42-CONTEXT.md

@packages/ai/src/question-generator.ts
@screenshots/audit/sheet1_diagnostika/R3_diagnostika_review.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update buildSystemPrompt with 6 rule blocks from Mila review</name>
  <files>packages/ai/src/question-generator.ts</files>
  <read_first>
    - packages/ai/src/question-generator.ts (full file, focus on buildSystemPrompt function lines 260-297)
    - screenshots/audit/sheet1_diagnostika/R3_diagnostika_review.txt (Mila's full review)
  </read_first>
  <action>
Modify ONLY the `buildSystemPrompt()` function (lines 260-297). Replace the current prompt body with an enhanced version that adds 6 rule blocks per user decisions D-01 through D-06. Keep the existing structure (function signature, return type, interpolation of category and count). Keep existing good rules (no authors/speakers, no VPN, etc.).

**Per D-01 — Category Mapping Rules.** Add a section "ПРАВИЛА ВЫБОРА РУБРИКИ" with explicit topic→axis mapping:
```
- SEO, поисковые фразы, ключевые слова, индексация → Маркетинг
- Реклама, ставки, показы, CTR, РК → Маркетинг
- Бюджет, расходы, юнит-экономика, маржа, CPO → Финансы
- Контент карточки, фото, инфографика, описание, воронка → Контент
- Логистика, FBO/FBS, остатки, поставки → Операции
- Аналитика, отчёты, мониторинг, конкуренты → Аналитика
```
Place this BEFORE the requirements section so LLM sees mapping rules early.

**Per D-02 — Negative Examples.** Add to the "СТРОГО ЗАПРЕЩЕНО" section:
```
❌ Сертификаты курса или процесс обучения
❌ Плагины, расширения или инструменты МПСТАТС (биддер, парсер) — вопросы должны проверять знания О МАРКЕТПЛЕЙСАХ, не об инструментах
❌ Цели или структура самого курса
❌ Определения общих IT-терминов (что такое ИИ, нейросеть)
```
Also REMOVE the existing rule about MPSTATS instruments (lines ~283-284) since the new D-02 rule supersedes it more broadly.

**Per D-03 — Answer Quality.** Add a new section "КАЧЕСТВО ВАРИАНТОВ ОТВЕТА":
```
- Все 4 варианта должны быть правдоподобными для специалиста среднего уровня
- Неправильные варианты — частые заблуждения или близкие по смыслу понятия
- Избегай абсурдных вариантов ("Мозг человека из нейросетей")
- Правильный ответ НЕ должен быть очевидным без знания предмета
```

**Per D-04 — Marketplace Context.** Add rule:
```
Если вопрос специфичен для площадки — обязательно укажи: Wildberries или Ozon.
Не задавай вопросы "в общем" когда ответ зависит от площадки.
```

**Per D-05 — Terminology.** Add rule:
```
Используй только реальные бизнес-термины маркетплейсов.
Не придумывай новых понятий. Если не уверен в термине — замени на описательную формулировку.
```

**Per D-06 — Question Style.** Add rule:
```
Предпочитай вопросы про алгоритм действий и практические решения.
Избегай вопросов на определения и теоретические знания.
Формулируй вопросы профессиональным языком маркетплейсов.
```

Keep the JSON format instruction at the end (sourceIndices, difficulty, etc.) unchanged.

Order sections in the prompt:
1. Role intro (existing)
2. ПРАВИЛА ВЫБОРА РУБРИКИ (D-01, new)
3. ПРИНЦИПЫ ХОРОШИХ ВОПРОСОВ (existing, enhanced)
4. ТРЕБОВАНИЯ (existing)
5. КАЧЕСТВО ВАРИАНТОВ ОТВЕТА (D-03, new)
6. КОНТЕКСТ ПЛОЩАДКИ (D-04, new)
7. ТЕРМИНОЛОГИЯ И СТИЛЬ (D-05 + D-06, combined)
8. СТРОГО ЗАПРЕЩЕНО (existing, enhanced with D-02)
9. ХОРОШИЕ ПРИМЕРЫ (existing)
10. JSON format (existing)
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsx -e "const { buildSystemPrompt } = require('./packages/ai/src/question-generator'); console.log('ERROR: buildSystemPrompt is not exported')" 2>/dev/null; grep -c "ПРАВИЛА ВЫБОРА РУБРИКИ" packages/ai/src/question-generator.ts && grep -c "КАЧЕСТВО ВАРИАНТОВ ОТВЕТА" packages/ai/src/question-generator.ts && grep -c "Wildberries или Ozon" packages/ai/src/question-generator.ts && grep -c "Не придумывай новых понятий" packages/ai/src/question-generator.ts && grep -c "алгоритм действий" packages/ai/src/question-generator.ts && grep -c "Сертификаты курса" packages/ai/src/question-generator.ts && echo "All 6 rule blocks present"</automated>
  </verify>
  <acceptance_criteria>
    - grep "ПРАВИЛА ВЫБОРА РУБРИКИ" packages/ai/src/question-generator.ts returns 1 match
    - grep "КАЧЕСТВО ВАРИАНТОВ ОТВЕТА" packages/ai/src/question-generator.ts returns 1 match
    - grep "Wildberries или Ozon" packages/ai/src/question-generator.ts returns 1+ matches
    - grep "Не придумывай новых понятий" packages/ai/src/question-generator.ts returns 1 match
    - grep "алгоритм действий" packages/ai/src/question-generator.ts returns 1 match
    - grep "Сертификаты курса" packages/ai/src/question-generator.ts returns 1 match
    - pnpm build completes without errors
  </acceptance_criteria>
  <done>buildSystemPrompt contains all 6 rule blocks (D-01 through D-06) from Mila's review. Existing rules preserved. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and test generation dry-run</name>
  <files>packages/ai/src/question-generator.ts</files>
  <read_first>
    - packages/ai/src/question-generator.ts (verify Task 1 changes)
  </read_first>
  <action>
1. Run `pnpm build` to verify no TypeScript errors from the prompt change.
2. Run `pnpm typecheck` to verify type safety.
3. Log the full updated prompt to console for visual review: create a temporary script that imports and calls `buildSystemPrompt('MARKETING', 3)` and prints the result. Run it with `npx tsx`. Then delete the temp script.
4. Verify the prompt length is reasonable (under 3000 chars — structured output needs room for response).

If build fails, fix any syntax issues (unterminated template literals, missing backticks, etc.).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build exits with code 0
    - No TypeScript errors related to question-generator.ts
  </acceptance_criteria>
  <done>Project builds successfully with updated prompt. Prompt is well-formatted and under token budget.</done>
</task>

</tasks>

<verification>
1. `grep -c "ПРАВИЛА ВЫБОРА РУБРИКИ" packages/ai/src/question-generator.ts` returns 1
2. `grep -c "СТРОГО ЗАПРЕЩЕНО" packages/ai/src/question-generator.ts` returns 1
3. `grep "Сертификаты курса\|биддер, парсер\|Не придумывай\|алгоритм действий\|Wildberries или Ozon\|КАЧЕСТВО ВАРИАНТОВ" packages/ai/src/question-generator.ts` returns 6 lines
4. `pnpm build` passes
</verification>

<success_criteria>
- All 6 decision blocks (D-01 through D-06) are present in buildSystemPrompt
- Existing good rules preserved (no authors, no VPN, etc.)
- No code logic changes — only prompt text updated
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/42-diagnostic-prompt-tuning/42-01-SUMMARY.md`
</output>
