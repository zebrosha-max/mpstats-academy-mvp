---
phase: 33-cq-email-automation
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified: []
autonomous: false
requirements: [CQ-07, CQ-08]

must_haves:
  truths:
    - "10 automation rules configured in CQ dashboard"
    - "10 HTML email templates uploaded to CQ"
    - "Each automation rule triggers on correct pa_ event"
    - "Test event fires and email is received"
  artifacts: []
  key_links: []
---

<objective>
Configure CQ dashboard automation rules and upload HTML email templates. Verify end-to-end event delivery.

Purpose: Without CQ automation rules, events fire but no emails are sent. This plan connects the events (from Plans 01-02) to actual email delivery via CQ dashboard configuration.
Output: 10 working automation rules in CQ, HTML templates uploaded, end-to-end verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-cq-email-automation/33-CONTEXT.md
@.planning/phases/33-cq-email-automation/33-01-SUMMARY.md
@.planning/phases/33-cq-email-automation/33-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Upload HTML templates to CQ</name>
  <action>
    Upload 10 HTML email templates from `docs/mails/E-mails/` to Carrot Quest dashboard.

    Templates to upload (folder → CQ template name):
    1. `01-payment-success/` → "pa_payment_success"
    2. `02-payment-failed/` → "pa_payment_failed"
    3. `03-subscription-cancelled/` → "pa_subscription_cancelled"
    4. `04-subscription-expiring/` → "pa_subscription_expiring"
    5. `05-welcome/` → "pa_registration_completed"
    6. `06-confirm-email/` → "pa_doi"
    7. `07-password-reset/` → "pa_password_reset"
    8. `08-1-inactive-7d/` → "pa_inactive_7"
    9. `08-2-inactive-14d/` → "pa_inactive_14"
    10. `08-3-inactive-30d/` → "pa_inactive_30"

    **CQ Template Variables** (map event properties to template placeholders):
    - pa_payment_success: `{{pa_amount}}`, `{{pa_course_name}}`, `{{pa_period_end}}`
    - pa_payment_failed: `{{pa_course_name}}`
    - pa_subscription_cancelled: `{{pa_course_name}}`, `{{pa_access_until}}`
    - pa_subscription_expiring: `{{pa_course_name}}`, `{{pa_period_end}}`
    - pa_registration_completed: `{{pa_name}}`
    - pa_doi: `{{pa_doi}}` (confirmation URL)
    - pa_password_reset: `{{pa_password_link}}` (reset URL)
    - pa_inactive_7/14/30: no dynamic variables

    **CTA Links (per D-03, D-04):**
    - "Начать учиться" → `https://platform.mpstats.academy/dashboard`
    - "Попробовать снова" / "Возобновить подписку" → `https://platform.mpstats.academy/pricing`
  </action>
  <how-to-verify>
    In CQ Dashboard → Messages → Email Templates:
    - 10 templates listed with correct names
    - Each template has correct variable placeholders
    - Preview renders correctly
  </how-to-verify>
  <resume-signal>Type "templates uploaded" when all 10 are in CQ</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create 10 CQ automation rules</name>
  <action>
    Create automation rules in CQ Dashboard → Automation → Rules.

    Each rule follows the pattern: **When event X fires → Send email template X**

    | # | Trigger Event | Email Template | Notes |
    |---|---------------|----------------|-------|
    | 1 | `pa_payment_success` | pa_payment_success | On successful payment |
    | 2 | `pa_payment_failed` | pa_payment_failed | On failed payment |
    | 3 | `pa_subscription_cancelled` | pa_subscription_cancelled | On subscription cancellation |
    | 4 | `pa_subscription_expiring` | pa_subscription_expiring | 3 days before expiry (from cron) |
    | 5 | `pa_registration_completed` | pa_registration_completed | First email confirmation |
    | 6 | `pa_doi` | pa_doi | Email confirmation link |
    | 7 | `pa_password_reset` | pa_password_reset | Password reset link |
    | 8 | `pa_inactive_7` | pa_inactive_7 | 7 days inactive |
    | 9 | `pa_inactive_14` | pa_inactive_14 | 14 days inactive |
    | 10 | `pa_inactive_30` | pa_inactive_30 | 30 days inactive |

    For each rule:
    1. Trigger: "User performed event" → select the pa_ event name
    2. Action: "Send email" → select the matching template
    3. Delay: None (send immediately)
    4. Status: Active
  </action>
  <how-to-verify>
    In CQ Dashboard → Automation → Rules:
    - 10 rules listed, all Active
    - Each rule has correct trigger event and email template
  </how-to-verify>
  <resume-signal>Type "rules created" when all 10 automation rules are active</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete CQ email automation pipeline: 10 events from backend → CQ automation rules → HTML email delivery</what-built>
  <how-to-verify>
    **Quick verification (test user):**

    1. **pa_doi (confirm email):**
       - Register new user at https://platform.mpstats.academy/register
       - Check inbox — should receive branded confirmation email (not Supabase default)

    2. **pa_registration_completed (welcome):**
       - Click confirmation link from step 1
       - Check inbox — should receive welcome email

    3. **pa_payment_success / pa_payment_failed:**
       - Go to /pricing, attempt payment (test card)
       - Check inbox for payment confirmation or failure email

    4. **pa_password_reset:**
       - Go to /forgot-password, request reset
       - Check inbox for branded password reset email

    5. **Cron endpoints (manual trigger):**
       ```bash
       # From VPS or local with port forwarding:
       curl -X POST -H "Authorization: Bearer $CRON_SECRET" \
         https://platform.mpstats.academy/api/cron/check-subscriptions

       curl -X POST -H "Authorization: Bearer $CRON_SECRET" \
         https://platform.mpstats.academy/api/cron/inactive-users
       ```
       Both should return `{"ok":true,...}`

    6. **GitHub Action:**
       - Go to GitHub → Actions → "Daily Email Cron"
       - Click "Run workflow" manually
       - Should complete successfully

    **Expected:** Each event fires to CQ, CQ automation rule triggers, email is delivered.
  </how-to-verify>
  <resume-signal>Type "approved" if emails are received correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. 10 CQ automation rules exist and are Active
2. 10 HTML templates uploaded with correct variable placeholders
3. At least pa_doi and pa_registration_completed verified end-to-end (email received)
4. Cron endpoints respond with 200 OK when called with correct secret
5. GitHub Action runs successfully
</verification>

<success_criteria>
- All 10 CQ automation rules are Active in dashboard
- All 10 HTML templates uploaded with correct pa_ variable placeholders
- At least 3 events verified end-to-end (registration flow: pa_doi + pa_registration_completed + pa_password_reset)
- Cron endpoints callable manually and via GitHub Action
- No Supabase default emails leaking through (all intercepted by CQ)
</success_criteria>

<output>
After completion, create `.planning/phases/33-cq-email-automation/33-03-SUMMARY.md`
</output>
