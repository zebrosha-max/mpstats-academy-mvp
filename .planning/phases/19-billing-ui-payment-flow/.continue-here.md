---
phase: 19-billing-ui-payment-flow
task: deployment-and-webhook-config
total_tasks: 4
status: in_progress
last_updated: 2026-03-11T15:26:55.060Z
---

<current_state>
Phase 19 backend + frontend DONE. Deployment to prod DONE.
Paused mid-integration: CloudPayments credentials deployed, seed run, billing_enabled=true.
Waiting for domain migration (Phase 21) before configuring CloudPayments webhook URLs.
</current_state>

<completed_work>

- Phase 19-01 (Backend): DONE — billing tRPC router (6 endpoints), CloudPayments widget wrapper, cancel API helper, PENDING status in Prisma
- Phase 19-02 (Frontend): DONE — /pricing page (2 plans), subscription section in /profile, conditional "Тарифы" in sidebar/mobile-nav
- Dev bypass removed: DEV_BYPASS_AUTH cleaned from middleware.ts, route.ts, layout.tsx — committed `671c306`
- Credentials saved to local `.env` and production `.env.production` on VPS (89.208.106.208)
- Production deployed: git push + docker compose rebuild, health check OK
- Billing seed run: FeatureFlag `billing_enabled=true`, SubscriptionPlan COURSE=2990, PLATFORM=4990, 6 courses updated
</completed_work>

<remaining_work>

- Configure webhook URLs in CloudPayments dashboard (6 URLs, one per event type) — BLOCKED by domain migration
- Test payment flow with test card (4242 4242 4242 4242)
- Verify webhook delivery: pay → ACTIVE, fail → PAST_DUE, cancel → CANCELLED
- Test recurrent payment cycle
</remaining_work>

<decisions_made>

- Decided to migrate domain BEFORE configuring CloudPayments webhooks — avoids reconfiguring URLs twice
- Webhook route uses single endpoint with `?type=` query param instead of separate routes per event type
- HMAC verification uses CLOUDPAYMENTS_API_SECRET as key (SHA256, base64)
- Dev bypass removed from prod — auth works via Supabase as intended
</decisions_made>

<blockers>
- **Domain migration (Phase 21)**: Must complete before setting CloudPayments webhook URLs. Current domain academyal.duckdns.org is temporary.
</blockers>

<context>
CloudPayments credentials are TEST credentials (pk_79b7e8cac0563808c255ad5fd3569).
Test cards: https://developers.cloudpayments.ru/#testirovanie
Success: 4242 4242 4242 4242, Decline: 4000 0000 0000 0002.

Webhook URL format (replace domain after Phase 21):
- https://{domain}/api/webhooks/cloudpayments?type=check
- https://{domain}/api/webhooks/cloudpayments?type=pay
- https://{domain}/api/webhooks/cloudpayments?type=fail
- https://{domain}/api/webhooks/cloudpayments?type=recurrent
- https://{domain}/api/webhooks/cloudpayments?type=cancel
- https://{domain}/api/webhooks/cloudpayments?type=refund

All cloudpayments lib files already committed: hmac.ts, types.ts, subscription-service.ts, widget.ts, cancel-api.ts, webhook route.ts.

Env vars on prod (.env.production):
- NEXT_PUBLIC_CLOUDPAYMENTS_PUBLIC_ID
- CLOUDPAYMENTS_PUBLIC_ID
- CLOUDPAYMENTS_API_SECRET
</context>

<next_action>
After Phase 21 (domain migration) completes:
1. Configure webhook URLs in CloudPayments dashboard with new domain
2. Redeploy if NEXT_PUBLIC_SITE_URL changed (it will)
3. Test full payment flow with test card
4. Verify webhook events arrive and subscription transitions work
</next_action>
