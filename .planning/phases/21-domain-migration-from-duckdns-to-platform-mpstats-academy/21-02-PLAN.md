---
phase: 21-domain-migration-from-duckdns-to-platform-mpstats-academy
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - apps/web/tests/auth/yandex-oauth.test.ts
  - apps/web/tests/auth/oauth-provider.test.ts
  - CLAUDE.md
autonomous: false
requirements: [DOM-04, DOM-05, DOM-06]

must_haves:
  truths:
    - "Yandex OAuth login works on platform.mpstats.academy"
    - "Supabase auth redirects use the new domain"
    - "Test fixtures reference platform.mpstats.academy instead of DuckDNS"
    - "Documentation reflects the new production URL"
  artifacts:
    - path: "apps/web/tests/auth/yandex-oauth.test.ts"
      provides: "Updated test fixtures with new domain"
      contains: "platform.mpstats.academy"
    - path: "apps/web/tests/auth/oauth-provider.test.ts"
      provides: "Updated test fixtures with new domain"
      contains: "platform.mpstats.academy"
    - path: "CLAUDE.md"
      provides: "Updated production URL in documentation"
      contains: "platform.mpstats.academy"
  key_links:
    - from: "Supabase Dashboard"
      to: "Auth callback route"
      via: "Redirect URL allowlist"
      pattern: "platform.mpstats.academy"
    - from: "Yandex OAuth app"
      to: "Auth callback route"
      via: "redirect_uri exact match"
      pattern: "platform.mpstats.academy/api/auth/yandex/callback"
---

<objective>
Complete the domain migration by updating external OAuth services (Supabase Dashboard, Yandex OAuth), updating test fixtures and documentation to reference the new domain, and verifying the full auth flow end-to-end.

Purpose: Ensure all external integrations and codebase references use the new domain.
Output: Working auth flow on new domain, updated tests and docs.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-domain-migration-from-duckdns-to-platform-mpstats-academy/21-CONTEXT.md
@.planning/phases/21-domain-migration-from-duckdns-to-platform-mpstats-academy/21-RESEARCH.md
@.planning/phases/21-domain-migration-from-duckdns-to-platform-mpstats-academy/21-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Update Supabase Dashboard and Yandex OAuth app</name>
  <files>N/A — external service configuration</files>
  <action>
User must update two external services. This cannot be automated — requires dashboard access.

**Supabase Dashboard** (https://supabase.com/dashboard):
1. Open project `saecuecevicwjkpmaoot`
2. Go to Authentication > URL Configuration
3. Update Site URL to `https://platform.mpstats.academy`
4. Update Redirect URLs: replace `https://academyal.duckdns.org/**` with `https://platform.mpstats.academy/**`
5. Save

**Yandex OAuth App** (https://oauth.yandex.ru/):
1. Open the MAAL OAuth app
2. Update Redirect URI to exactly: `https://platform.mpstats.academy/api/auth/yandex/callback`
3. No trailing slash! Must match exactly.
4. Save
  </action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" https://platform.mpstats.academy/api/auth/yandex</automated>
  </verify>
  <done>Supabase Site URL and Redirect URLs point to platform.mpstats.academy. Yandex OAuth redirect URI updated.</done>
  <how-to-verify>
    **Supabase Dashboard** (https://supabase.com/dashboard):
    1. Open project `saecuecevicwjkpmaoot`
    2. Go to Authentication > URL Configuration
    3. Update **Site URL** to `https://platform.mpstats.academy`
    4. Update **Redirect URLs**: replace `https://academyal.duckdns.org/**` with `https://platform.mpstats.academy/**`
    5. Save

    **Yandex OAuth App** (https://oauth.yandex.ru/):
    1. Open the MAAL OAuth app
    2. Update **Redirect URI** to exactly: `https://platform.mpstats.academy/api/auth/yandex/callback`
    3. No trailing slash! Must match exactly.
    4. Save
  </how-to-verify>
  <resume-signal>Type "oauth updated" once both Supabase and Yandex OAuth are updated</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update test fixtures and documentation</name>
  <files>apps/web/tests/auth/yandex-oauth.test.ts, apps/web/tests/auth/oauth-provider.test.ts, CLAUDE.md</files>
  <action>
**Step 1: Update test fixtures**

In `apps/web/tests/auth/yandex-oauth.test.ts`:
- Replace all occurrences of `https://academyal.duckdns.org` with `https://platform.mpstats.academy`
- Lines to update: 73, 79, 96, 192 (vi.stubEnv and Request URL constructors)

In `apps/web/tests/auth/oauth-provider.test.ts`:
- Replace all occurrences of `https://academyal.duckdns.org` with `https://platform.mpstats.academy`
- Lines to update: 34, 51

**Step 2: Update CLAUDE.md**

In the project root `CLAUDE.md`:
- Replace `https://academyal.duckdns.org` with `https://platform.mpstats.academy` in:
  - "Production URL" line
  - "VPS Deploy" section URL field
  - "Domain Migration Checklist" section — mark all items as completed [x]
  - Any other references to the old domain

**Step 3: Run tests to confirm fixtures work**
```bash
pnpm test
```
Tests should pass with updated domain references (these are vi.stubEnv fixtures, not runtime values).
  </action>
  <verify>
    <automated>cd D:/GpT_docs/MPSTATS\ ACADEMY\ ADAPTIVE\ LEARNING/MAAL && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All test files reference platform.mpstats.academy instead of academyal.duckdns.org
    - CLAUDE.md production URL updated
    - Domain Migration Checklist items marked complete
    - Tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification on new domain</name>
  <files>N/A — verification only</files>
  <action>
All migration steps complete. User verifies end-to-end functionality on the new domain in browser.
  </action>
  <verify>
    <automated>curl -s https://platform.mpstats.academy/api/health</automated>
  </verify>
  <done>Full auth flow and application functionality verified on platform.mpstats.academy</done>
  <what-built>Full domain migration: DNS, SSL, Nginx, Docker, OAuth services, test fixtures, documentation.</what-built>
  <how-to-verify>
    1. Open https://platform.mpstats.academy in browser — landing page loads
    2. Check SSL certificate — valid for platform.mpstats.academy (padlock icon)
    3. Click "Войти" — login page loads
    4. Log in via Yandex OAuth — full flow completes, redirects back to dashboard
    5. Open any lesson page — video loads, AI summary/chat works
    6. Open https://academyal.duckdns.org — should NOT load (disabled, no redirect)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm test` passes with updated domain fixtures
- Auth flow works end-to-end on platform.mpstats.academy via browser
- No references to academyal.duckdns.org remain in test files or main CLAUDE.md
</verification>

<success_criteria>
All external services (Supabase, Yandex OAuth) use the new domain. Auth flow verified working end-to-end on platform.mpstats.academy. Test fixtures and documentation updated. Old domain disabled.
</success_criteria>

<output>
After completion, create `.planning/phases/21-domain-migration-from-duckdns-to-platform-mpstats-academy/21-02-SUMMARY.md`
</output>
