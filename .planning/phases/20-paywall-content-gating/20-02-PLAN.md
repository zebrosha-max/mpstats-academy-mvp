---
phase: 20-paywall-content-gating
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - apps/web/src/components/learning/LockOverlay.tsx
  - apps/web/src/components/learning/PaywallBanner.tsx
  - apps/web/src/components/learning/LessonCard.tsx
  - apps/web/src/app/(main)/learn/page.tsx
  - apps/web/src/app/(main)/learn/[id]/page.tsx
  - apps/web/src/app/(main)/diagnostic/results/page.tsx
autonomous: false
requirements: [PAY-03]

must_haves:
  truths:
    - "Locked lesson page shows lock UI with CTA to /pricing instead of video"
    - "Locked lesson page still shows title, description, breadcrumb and prev/next navigation"
    - "AI panel (Summary + Chat) is hidden on locked lessons"
    - "LessonCard shows lock icon instead of play icon for locked lessons"
    - "Free lessons show soft upsell banner under video"
    - "Course listing shows mini-banner with count of locked lessons and CTA to /pricing"
    - "Track preview without PLATFORM subscription shows first 3 lessons, rest blurred with CTA"
    - "When billing_enabled=false, no lock icons, no banners, no blur"
  artifacts:
    - path: "apps/web/src/components/learning/LockOverlay.tsx"
      provides: "Full lock UI block for lesson page (lock icon + title + CTA)"
    - path: "apps/web/src/components/learning/PaywallBanner.tsx"
      provides: "Soft upsell banner for free lessons + mini-banner for course listing"
    - path: "apps/web/src/components/learning/LessonCard.tsx"
      provides: "Lock icon variant when lesson.locked=true"
    - path: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      provides: "Conditional rendering: LockOverlay vs video+AI based on locked flag"
    - path: "apps/web/src/app/(main)/learn/page.tsx"
      provides: "Lock icons on cards, course banners, track preview gating"
  key_links:
    - from: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      to: "apps/web/src/components/learning/LockOverlay.tsx"
      via: "conditional render when data.lesson.locked"
      pattern: "lesson\\.locked.*LockOverlay"
    - from: "apps/web/src/app/(main)/learn/page.tsx"
      to: "apps/web/src/components/learning/LessonCard.tsx"
      via: "locked prop passed from course.lessons"
      pattern: "locked.*lesson\\.locked"
---

<objective>
Frontend lock UI, upsell banners, and track preview gating.

Purpose: Show lock UI on paid lessons, lock icons in catalog, upsell banners on free lessons, and gated track preview -- making the paywall visible to users while keeping the experience browsable.
Output: LockOverlay, PaywallBanner components + modified lesson page, learn page, and diagnostic results page.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-paywall-content-gating/20-CONTEXT.md
@.planning/phases/20-paywall-content-gating/20-RESEARCH.md
@.planning/phases/20-paywall-content-gating/20-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: access utility adds these to tRPC responses -->

LessonWithProgress now includes:
```typescript
locked?: boolean; // true if user cannot access this lesson
```

getLesson response now includes:
```typescript
{
  lesson: LessonWithProgress, // with locked flag, videoId=null if locked
  course: { id, title, slug },
  nextLesson, prevLesson,
  totalLessonsInCourse, currentLessonNumber,
  hasPlatformSubscription: boolean, // NEW: for track gating
}
```

getRecommendedPath response now includes:
```typescript
{
  generatedAt, lessons: [...with locked], totalLessons, completedLessons,
  hasPlatformSubscription: boolean, // NEW: for track preview gating
}
```

getCourses lessons now include `locked: boolean` per lesson.

From DiagnosticGateBanner.tsx (reference pattern for gate banners):
```typescript
// Card with shadow-mp-card, border-mp-blue-200, bg-gradient-to-br from-mp-blue-50 to-white
// Lock icon SVG in w-16 h-16 rounded-2xl bg-mp-blue-100 container
// h2 text-heading, p text-body, Button size="lg" with Link
```

From LessonCard.tsx (current interface):
```typescript
interface LessonCardProps {
  lesson: LessonWithProgress;
  showCourse?: boolean;
  courseName?: string;
  isRecommended?: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lock components + update LessonCard</name>
  <files>apps/web/src/components/learning/LockOverlay.tsx, apps/web/src/components/learning/PaywallBanner.tsx, apps/web/src/components/learning/LessonCard.tsx</files>
  <action>
**1. Create `LockOverlay.tsx`** -- full lock UI for lesson page (replaces video + AI panel area):
- Based on DiagnosticGateBanner pattern: Card with `shadow-mp-card border-mp-gray-200`
- Large lock icon (w-20 h-20 rounded-2xl bg-mp-gray-100 container, lock SVG from DiagnosticGateBanner)
- Title: "Урок доступен по подписке"
- Subtitle: "Оформите подписку, чтобы получить доступ к этому и другим урокам"
- CTA: `<Link href="/pricing"><Button size="lg">Оформить подписку</Button></Link>`
- Props: `{ lessonTitle: string }` (for potential use, the component primarily shows generic text)
- Padded generously: `py-16 text-center`

**2. Create `PaywallBanner.tsx`** with two exported components:

a) `PaywallBanner` -- soft upsell banner shown under video on FREE lessons:
- Muted: `p-3 bg-mp-blue-50 border border-mp-blue-100 rounded-lg`
- Flex row: text left ("Вам доступно ещё {remainingFreeCount} бесплатных уроков в этом курсе"), Button right
- Button: `variant="outline" size="sm"` linking to /pricing
- Props: `{ remainingFreeCount: number }` -- when 0, show "Все бесплатные уроки просмотрены"

b) `CourseLockBanner` -- mini-banner under course in catalog (/learn):
- Similar style but more compact: `p-3 bg-mp-blue-50/50 border border-mp-blue-100 rounded-lg mt-3`
- Text: "Ещё {lockedCount} уроков доступны по подписке"
- Small CTA: `<Link href="/pricing">Подробнее</Link>` as text link (mp-blue-600)
- Props: `{ lockedCount: number }`
- If lockedCount === 0, return null

**3. Update `LessonCard.tsx`:**
- Add `locked?: boolean` to LessonCardProps
- When `locked === true`:
  - Replace status icon with lock SVG icon (same lock icon from DiagnosticGateBanner, sized w-5 h-5)
  - Use color `text-mp-gray-400` for lock icon
  - Do NOT show progress bar (even if watchedPercent > 0)
  - Do NOT show "Рекомендовано" badge
  - Keep everything else: title, description, category badge, duration, arrow, clickability
- When locked is falsy: render exactly as before (no behavior change)
  </action>
  <verify>
    <automated>cd "D:\GpT_docs\MPSTATS ACADEMY ADAPTIVE LEARNING\MAAL" && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>LockOverlay, PaywallBanner, CourseLockBanner components exist. LessonCard shows lock icon when locked=true. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire lock UI into lesson page, learn page, and diagnostic results</name>
  <files>apps/web/src/app/(main)/learn/[id]/page.tsx, apps/web/src/app/(main)/learn/page.tsx, apps/web/src/app/(main)/diagnostic/results/page.tsx</files>
  <action>
**1. Lesson page (`learn/[id]/page.tsx`):**

After destructuring `data`, check `lesson.locked`:

If `lesson.locked === true`:
- Show breadcrumb (unchanged)
- Show header (title, description, badge, lesson number) -- unchanged
- Instead of the `grid lg:grid-cols-3` section (video + summary + chat), render `<LockOverlay lessonTitle={lesson.title} />`
- Show navigation (prev/next) -- unchanged, per user decision locked lessons are browsable
- Do NOT show: video player, summary section, chat sidebar, "Завершить урок" button, watch progress bar
- Do NOT show DiagnosticGateBanner (paywall takes priority over diagnostic gate)

If `lesson.locked !== true`:
- Current behavior unchanged
- If lesson is a free lesson (not locked) AND billing is enabled (check: any sibling lesson in the course has locked=true, infer from data), show `<PaywallBanner remainingFreeCount={N} />` under the video card. Calculate N as: total lessons in course - 2 (free threshold) - but cap at actual locked count if known. Simpler: use `totalLessonsInCourse - currentLessonNumber` as a rough "remaining" count or just show a generic "Ещё N уроков доступны по подписке" with N = totalLessonsInCourse - 2 (the free count).
  - Actually, for simplicity: only show PaywallBanner if `lesson.order <= 2` AND `totalLessonsInCourse > 2`. Use `remainingFreeCount = Math.max(0, 2 - lesson.order)` (how many more free lessons remain: if on lesson 1, 1 more free; if on lesson 2, 0 more free -- show "Все бесплатные уроки просмотрены").

Import LockOverlay from `@/components/learning/LockOverlay` and PaywallBanner from `@/components/learning/PaywallBanner`.

**2. Learn page (`learn/page.tsx`):**

**Courses view:**
- Pass `locked={lesson.locked}` prop to each `<LessonCard>` in the courses view map
- After the lessons grid (inside CardContent, after the "Показать все" button area), add `<CourseLockBanner lockedCount={lockedCount} />` where `lockedCount = course.lessons.filter(l => l.locked).length`. Import CourseLockBanner from `@/components/learning/PaywallBanner`.

**Track view ("Мой трек"):**
- In Case C (normal track with lessons), check `recommendedPath.hasPlatformSubscription`:
  - If `hasPlatformSubscription === true` OR all lessons have `locked !== true`: render as-is (no change)
  - If `hasPlatformSubscription === false`:
    - Show first 3 lessons normally with `<LessonCard>`
    - Remaining lessons: wrap in `<div className="blur-sm pointer-events-none select-none">` with `<LessonCard locked />` (visually blurred)
    - After blurred lessons, show a CTA card:
      ```
      Card border-mp-blue-200 bg-gradient-to-br from-mp-blue-50 to-white
      py-8 text-center
      h3: "Получите полный персональный трек"
      p: "Ещё {remaining} уроков доступны с полной подпиской"
      Button -> /pricing: "Оформить полный доступ"
      ```
- Pass `locked={lesson.locked}` for lessons that also have individual lock status in courses view (LessonCard in track view uses existing `lesson` data which now includes `locked` from getRecommendedPath)

**3. Diagnostic results page (`diagnostic/results/page.tsx`):**

This page shows recommended lessons after diagnostic completion. Add track preview gating:
- Query `trpc.learning.getRecommendedPath` (it may already be called, or add it)
- If recommendedPath exists and `hasPlatformSubscription === false`:
  - Show first 3 recommended lessons normally
  - Remaining: blur with same pattern as learn page
  - CTA card: "Оформите полный доступ для персонального трека обучения" -> /pricing
- If `hasPlatformSubscription === true`: show all lessons normally
- If no recommendedPath or billing disabled: show as-is (current behavior)
  </action>
  <verify>
    <automated>cd "D:\GpT_docs\MPSTATS ACADEMY ADAPTIVE LEARNING\MAAL" && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Lesson page shows LockOverlay for locked lessons. Learn page shows lock icons on cards and course banners. Track view shows blur + CTA for non-PLATFORM users. Diagnostic results page shows track preview with blur gating. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify paywall UI end-to-end</name>
  <action>
Human verifies the complete paywall + content gating system:
- Lock UI on paid lesson pages (lock icon + CTA instead of video)
- Lock icons on LessonCard in course catalog
- Soft upsell banner on free lessons
- Mini-banner under courses showing locked lesson count
- Track preview gating (3 lessons visible, rest blurred + CTA)
- Feature flag: billing_enabled=false makes everything open
  </action>
  <verify>
1. Run `pnpm dev` and open http://localhost:3000
2. Verify lock UI on paid lesson: Navigate to /learn, click on a lesson with order > 2. Should see lock overlay with "Оформить подписку" CTA, no video, no AI panel. Breadcrumb and navigation (prev/next) should work.
3. Verify free lesson: Click on lesson with order 1 or 2. Should see full video + AI + soft banner "Вам доступно ещё N уроков".
4. Verify catalog: On /learn (courses view), lessons with order > 2 should show lock icon instead of play. Under each course should be mini-banner "Ещё N уроков доступны по подписке".
5. Verify track gating: Switch to "Мой трек" tab. Without PLATFORM subscription, first 3 lessons normal, rest blurred with CTA.
6. Verify feature flag off: In Prisma Studio or admin, set billing_enabled=false. Refresh -- all locks should disappear, all content accessible.
7. Verify security: In browser DevTools Network tab, check tRPC response for a locked lesson -- videoId should be null.
  </verify>
  <done>All 7 verification steps pass. Paywall UI approved by human.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passes
- `pnpm build` succeeds
- Locked lesson page shows LockOverlay instead of video+AI
- Free lesson page shows PaywallBanner
- LessonCard with locked=true shows lock icon
- Course listing has CourseLockBanner
- Track view blurs lessons 4+ without PLATFORM
- billing_enabled=false disables all gating
- No videoId leak in network responses for locked lessons
</verification>

<success_criteria>
- Lock overlay visible on paid lesson pages with CTA to /pricing
- Lock icons on LessonCard for paid lessons in catalog
- Free lessons show soft upsell banner
- Course listings show "N уроков доступны по подписке" banner
- Track preview gated: 3 visible, rest blurred, CTA for PLATFORM
- Feature flag toggle works: billing_enabled=false opens everything
- videoId is null in tRPC response for locked lessons (verified in DevTools)
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/20-paywall-content-gating/20-02-SUMMARY.md`
</output>
