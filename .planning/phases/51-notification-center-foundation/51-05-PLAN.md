---
phase: 51-notification-center-foundation
plan: 05
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - apps/web/src/lib/utils/format-time.ts
  - apps/web/src/components/comments/CommentItem.tsx
  - apps/web/src/components/notifications/NotificationBell.tsx
  - apps/web/src/components/notifications/NotificationItem.tsx
  - apps/web/src/app/(main)/notifications/page.tsx
  - apps/web/src/app/(main)/layout.tsx
autonomous: true
requirements: [REQ-51-07, REQ-51-08]

must_haves:
  truths:
    - "formatRelativeTime извлечён в server-safe utility файл, переиспользуется CommentItem + NotificationItem"
    - "NotificationBell видна в Header между HelpCircleButton и UserNav (порядок: Bell | Help | UserNav)"
    - "Badge скрыт при count=0, показывает '99+' при count > 99"
    - "Polling 60с останавливается при document.hidden, возобновляется при возврате"
    - "Открытие dropdown триггерит markSeen mutation"
    - "Dropdown показывает 10 последних, группировка 'Новые / Раньше', footer с двумя CTA"
    - "Click на item → переход на ctaUrl + markRead"
    - "/notifications page рендерит infinite list с пагинацией 20/page"
    - "Filter all|unread через URL ?filter=unread"
    - "Empty states: пустой all, пустой unread, разные тексты"
  artifacts:
    - path: "apps/web/src/lib/utils/format-time.ts"
      provides: "Shared formatRelativeTime utility (server-safe, no client imports)"
      exports: ["formatRelativeTime"]
    - path: "apps/web/src/components/notifications/NotificationBell.tsx"
      provides: "Bell icon + badge + dropdown с группировкой + footer"
      exports: ["NotificationBell"]
      min_lines: 120
    - path: "apps/web/src/components/notifications/NotificationItem.tsx"
      provides: "Reusable item card (используется в Bell + /notifications)"
      exports: ["NotificationItem"]
    - path: "apps/web/src/app/(main)/notifications/page.tsx"
      provides: "Full history page с фильтром + пагинацией + markAllRead"
      exports: ["default page component"]
    - path: "apps/web/src/app/(main)/layout.tsx (modified)"
      provides: "Header с NotificationBell перед HelpCircleButton"
      contains: "<NotificationBell />"
  key_links:
    - from: "NotificationBell.tsx"
      to: "trpc.notifications.unreadCount + markSeen + list"
      via: "useQuery + useMutation"
    - from: "NotificationItem.tsx"
      to: "ctaUrl"
      via: "Link href + onClick markRead"
    - from: "/notifications/page.tsx"
      to: "trpc.notifications.list infinite + markAllRead"
      via: "useInfiniteQuery"
    - from: "NotificationItem.tsx + CommentItem.tsx"
      to: "apps/web/src/lib/utils/format-time.ts"
      via: "import { formatRelativeTime } from '@/lib/utils/format-time'"
---

<objective>
UI слой первой части: NotificationBell в Header (badge + dropdown + polling pause + markSeen on open), reusable NotificationItem, полная страница `/notifications` с фильтрами и пагинацией. Все три компонента используют tRPC procedures из плана 03.

Purpose: Юзер видит badge при появлении нового уведомления, может открыть dropdown за один клик, перейти на полную историю, отметить всё прочитанным. Это видимая часть Phase 51 — без неё все остальные слои невидимы.

Output: 4 новых файла (1 utility + 3 компонента/страницы) + 2 modified (layout + CommentItem). /notifications page работает end-to-end с пагинацией и фильтрами. Bell отображается на каждой странице main layout'а.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@.planning/phases/51-notification-center-foundation/51-03-SUMMARY.md
@apps/web/src/app/(main)/layout.tsx
@apps/web/src/components/comments/CommentItem.tsx

<interfaces>
<!-- From plan 03 (notifications router) -->
```typescript
trpc.notifications.unreadCount.useQuery() → { count: number }
trpc.notifications.markSeen.useMutation()
trpc.notifications.markRead.useMutation({ notificationId })
trpc.notifications.markAllRead.useMutation()
trpc.notifications.list.useInfiniteQuery({ filter: 'all'|'unread', cursor? }) → { items, nextCursor, totalCount }
```

<!-- Existing UI primitives -->
- @/components/ui/popover (Popover + PopoverTrigger + PopoverContent — z-50 уже задан)
- @/components/ui/button (Button)
- @/lib/utils (cn)

<!-- Existing pattern для polling + Page Visibility (KinescopePlayer.tsx:132-156) -->
```typescript
const handleVisibility = () => setDocHidden(document.hidden);
document.addEventListener('visibilitychange', handleVisibility);
// cleanup в useEffect return
```

<!-- VERIFIED — apps/web/src/components/comments/CommentItem.tsx (lines 19-34) -->
```typescript
const MONTHS_SHORT = ['янв', 'фев', 'мар', 'апр', 'май', 'июн', 'июл', 'авг', 'сен', 'окт', 'ноя', 'дек'];

export function formatRelativeTime(date: Date): string {
  const now = new Date();
  const diffMs = now.getTime() - date.getTime();
  const diffMin = Math.floor(diffMs / 60000);
  const diffH = Math.floor(diffMs / 3600000);
  const diffD = Math.floor(diffMs / 86400000);

  if (diffMin < 1) return 'только что';
  if (diffMin < 60) return `${diffMin} мин назад`;
  if (diffH < 24) return `${diffH} ч назад`;
  if (diffD < 2) return 'вчера';
  return `${date.getDate()} ${MONTHS_SHORT[date.getMonth()]}`;
}
```
**Проблема:** `CommentItem.tsx` — `'use client'` component с avatars/edit/delete logic. Импорт оттуда тянет весь client bundle в NotificationItem (который сам должен быть лёгким). Решение в Task 0 ниже.

<!-- Existing layout.tsx structure (lines 81-88) -->
```jsx
<div className="flex items-center gap-2">
  <HelpCircleButton />
  <UserNav user={...} />
</div>
```
Вставить <NotificationBell /> ПЕРЕД <HelpCircleButton />.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Извлечь formatRelativeTime в shared utility apps/web/src/lib/utils/format-time.ts</name>
  <files>apps/web/src/lib/utils/format-time.ts, apps/web/src/components/comments/CommentItem.tsx</files>

  <read_first>
    - apps/web/src/components/comments/CommentItem.tsx (lines 1-50 — для извлечения formatRelativeTime + MONTHS_SHORT)
    - apps/web/src/lib/utils.ts (если существует — для понимания паттерна где живут utility модули)
  </read_first>

  <action>
    **Шаг 1.** Создать новый файл `apps/web/src/lib/utils/format-time.ts` (server-safe, без client imports):

    ```typescript
    /**
     * Shared relative-time formatter (Russian).
     *
     * Used by CommentItem (Phase 12) and NotificationItem (Phase 51).
     * Server-safe — no React, no client APIs. Pure date arithmetic.
     */

    const MONTHS_SHORT = [
      'янв', 'фев', 'мар', 'апр', 'май', 'июн',
      'июл', 'авг', 'сен', 'окт', 'ноя', 'дек',
    ];

    export function formatRelativeTime(date: Date): string {
      const now = new Date();
      const diffMs = now.getTime() - date.getTime();
      const diffMin = Math.floor(diffMs / 60000);
      const diffH = Math.floor(diffMs / 3600000);
      const diffD = Math.floor(diffMs / 86400000);

      if (diffMin < 1) return 'только что';
      if (diffMin < 60) return `${diffMin} мин назад`;
      if (diffH < 24) return `${diffH} ч назад`;
      if (diffD < 2) return 'вчера';
      return `${date.getDate()} ${MONTHS_SHORT[date.getMonth()]}`;
    }
    ```

    **Шаг 2.** Изменить `apps/web/src/components/comments/CommentItem.tsx`:

    - **Удалить** строки 19-34 (MONTHS_SHORT const + formatRelativeTime function)
    - **Добавить** импорт в начало файла (после `'use client'` директивы и существующих imports):
      ```typescript
      import { formatRelativeTime } from '@/lib/utils/format-time';
      ```
    - Re-export для обратной совместимости (если кто-то импортит из CommentItem):
      ```typescript
      export { formatRelativeTime } from '@/lib/utils/format-time';
      ```

    Не трогать остальную логику CommentItem.tsx (использования `formatRelativeTime` в JSX остаются как есть — функция теперь приходит из импорта).

    КРИТИЧЕСКИ ВАЖНО:
    - Имена функции и константы сохранить ТОЧНО — поведение `formatRelativeTime` не меняется (1:1 копия), только перенесено
    - Re-export нужен на случай если другие файлы (например `Phase 21` или `Phase 23`) импортят `formatRelativeTime` из `CommentItem.tsx`. Проверить через `grep -rn "from.*CommentItem.*formatRelativeTime" apps/web/src/` — все такие импорты продолжат работать через re-export
    - NotificationItem (Task 1 ниже) импортит **прямо из** `@/lib/utils/format-time` (без CommentItem на пути — экономия bundle size)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "export function formatRelativeTime" apps/web/src/lib/utils/format-time.ts && grep -c "formatRelativeTime" apps/web/src/components/comments/CommentItem.tsx</automated>
  </verify>

  <done>
    - Файл `apps/web/src/lib/utils/format-time.ts` существует с экспортом `formatRelativeTime`
    - В `CommentItem.tsx` функция больше не определена (удалена) — `grep -c "function formatRelativeTime" apps/web/src/components/comments/CommentItem.tsx` returns 0
    - В `CommentItem.tsx` есть import + re-export — `grep -c "formatRelativeTime" apps/web/src/components/comments/CommentItem.tsx` returns >= 2 (import + re-export + JSX usages)
    - `pnpm typecheck` exits 0
    - Существующая логика CommentItem (avatars, edit/delete, replies) НЕ затронута
  </done>
</task>

<task type="auto">
  <name>Task 1: Создать reusable NotificationItem компонент</name>
  <files>apps/web/src/components/notifications/NotificationItem.tsx</files>

  <read_first>
    - apps/web/src/lib/utils/format-time.ts (созданный в Task 0)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 598-668 (NotificationItem skeleton)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-01, D-03, D-15 (2-line items, unread accent, copy)
    - packages/shared/src/notifications.ts (для NotificationPayload типа)
  </read_first>

  <action>
    Создать `apps/web/src/components/notifications/NotificationItem.tsx`:

    ```typescript
    'use client';

    import Link from 'next/link';
    import { cn } from '@/lib/utils';
    import { formatRelativeTime } from '@/lib/utils/format-time';
    import type { NotificationPayload } from '@mpstats/shared';

    export interface NotificationItemData {
      id: string;
      type: string;
      payload: any; // Prisma Json — runtime narrow через discriminant
      ctaUrl: string | null;
      readAt: Date | string | null;
      createdAt: Date | string;
    }

    interface NotificationItemProps {
      notification: NotificationItemData;
      onClick?: () => void;
    }

    /** Маленькая иконка-индикатор по типу уведомления (D-01) */
    function TypeIcon({ type }: { type: string }) {
      // Простой emoji-style для Phase 51; иконки/цвета можно сделать в Phase 52 при ADMIN_COMMENT_REPLY accent
      const map: Record<string, string> = {
        COMMENT_REPLY: '💬',
        ADMIN_COMMENT_REPLY: '👨‍🏫',
        CONTENT_UPDATE: '📚',
        PROGRESS_NUDGE: '📍',
        INACTIVITY_RETURN: '👋',
        WEEKLY_DIGEST: '📰',
        BROADCAST: '📣',
      };
      return (
        <div className="flex-shrink-0 w-8 h-8 rounded-full bg-mp-gray-100 flex items-center justify-center text-base">
          {map[type] ?? '🔔'}
        </div>
      );
    }

    function deriveTitleAndPreview(payload: NotificationPayload | any): { title: string; preview: string } {
      // Discriminant runtime narrow
      switch (payload?.type) {
        case 'COMMENT_REPLY':
        case 'ADMIN_COMMENT_REPLY':
          return {
            title: `${payload.replyAuthorName ?? 'Пользователь'} ответил на твой комментарий`,
            preview: payload.preview ?? '',
          };
        case 'CONTENT_UPDATE':
          return {
            title: `Новые уроки в курсе «${payload.courseTitle ?? ''}»`,
            preview: `Добавлено уроков: ${payload.lessonIds?.length ?? 0}`,
          };
        case 'PROGRESS_NUDGE':
          return {
            title: 'Продолжишь?',
            preview: payload.lessonTitle ?? '',
          };
        case 'INACTIVITY_RETURN':
          return {
            title: 'Давно тебя не было',
            preview: `${payload.daysSinceLastActive ?? 0} дней без визита — расскажем что нового`,
          };
        case 'WEEKLY_DIGEST':
          return {
            title: 'Твой недельный дайджест',
            preview: `Новых уроков: ${payload.newLessonsCount ?? 0}, активность: ${payload.activityCount ?? 0}`,
          };
        case 'BROADCAST':
          return {
            title: payload.title ?? 'Анонс',
            preview: payload.body ?? '',
          };
        default:
          return { title: 'Уведомление', preview: '' };
      }
    }

    export function NotificationItem({ notification, onClick }: NotificationItemProps) {
      const isUnread = notification.readAt === null;
      const created = new Date(notification.createdAt);
      const { title, preview } = deriveTitleAndPreview(notification.payload);

      const inner = (
        <div
          className={cn(
            'flex gap-3 p-3 transition-colors hover:bg-mp-gray-50',
            isUnread && 'bg-mp-blue-50', // D-03 unread accent
          )}
        >
          <TypeIcon type={notification.type} />
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium text-mp-gray-900 truncate">{title}</p>
            {preview && (
              <p className="text-xs text-mp-gray-500 line-clamp-2 mt-0.5">{preview}</p>
            )}
            <span className="text-xs text-mp-gray-400 mt-1 block">
              {formatRelativeTime(created)}
            </span>
          </div>
        </div>
      );

      if (notification.ctaUrl) {
        return (
          <Link href={notification.ctaUrl} onClick={onClick} className="block">
            {inner}
          </Link>
        );
      }
      return (
        <button onClick={onClick} className="w-full text-left">
          {inner}
        </button>
      );
    }
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - Импорт `formatRelativeTime` из `@/lib/utils/format-time` (созданного в Task 0), НЕ из `CommentItem` — это экономит client bundle (CommentItem тащит avatars + edit/delete + dialogs)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "deriveTitleAndPreview\|TypeIcon" apps/web/src/components/notifications/NotificationItem.tsx</automated>
  </verify>

  <done>
    - Файл существует
    - Покрыты все 7 NotificationType в `deriveTitleAndPreview` (включая default)
    - Use of `bg-mp-blue-50` для unread (D-03)
    - Conditional render: ctaUrl → Link, no ctaUrl → button
    - Импорт `formatRelativeTime` из `@/lib/utils/format-time` (НЕ из CommentItem)
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать NotificationBell с polling pause + dropdown с группировкой + footer</name>
  <files>apps/web/src/components/notifications/NotificationBell.tsx</files>

  <read_first>
    - apps/web/src/components/ui/popover.tsx (для z-50 и a11y)
    - apps/web/src/components/admin/AdminSidebar.tsx (refetchInterval pattern)
    - apps/web/src/components/learning/FilterPanel.tsx (Popover trigger pattern, lines 166-213)
    - apps/web/src/components/video/KinescopePlayer.tsx (Page Visibility API, lines 132-156)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 496-595 (NotificationBell skeleton)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-02, D-04, D-05, D-06, D-07, DC-02, DC-06, DC-07
  </read_first>

  <action>
    Создать `apps/web/src/components/notifications/NotificationBell.tsx`:

    ```typescript
    'use client';

    import { useEffect, useState } from 'react';
    import Link from 'next/link';
    import { Popover, PopoverContent, PopoverTrigger } from '@/components/ui/popover';
    import { trpc } from '@/lib/trpc/client';
    import { cn } from '@/lib/utils';
    import { NotificationItem } from './NotificationItem';

    /** Phase 51 — NotificationBell:
     * - badge с unread count (hidden если 0, '99+' если > 99)
     * - polling 60с через refetchInterval, pause при document.hidden (DC-02)
     * - открытие dropdown триггерит markSeen (D-07)
     * - dropdown: 10 последних, группировка «Новые / Раньше» (D-02), footer с двумя CTA
     *
     * NOTE: markAllRead помечает ВСЕ unread юзера, не только видимые в dropdown —
     * intentional, соответствует SPEC acceptance criterion req 8.
     */

    const POLL_INTERVAL_MS = 60_000;
    const DROPDOWN_LIMIT = 10;

    function BellIcon() {
      return (
        <svg
          className="w-5 h-5 text-mp-gray-600"
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
          strokeWidth={2}
          aria-hidden="true"
        >
          <path
            strokeLinecap="round"
            strokeLinejoin="round"
            d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"
          />
        </svg>
      );
    }

    export function NotificationBell() {
      const [isOpen, setIsOpen] = useState(false);
      const [docHidden, setDocHidden] = useState(false);

      // Page Visibility API — pause polling when tab hidden (D-05)
      useEffect(() => {
        const handler = () => setDocHidden(document.hidden);
        document.addEventListener('visibilitychange', handler);
        return () => document.removeEventListener('visibilitychange', handler);
      }, []);

      const { data: countData } = trpc.notifications.unreadCount.useQuery(undefined, {
        refetchInterval: docHidden ? false : POLL_INTERVAL_MS,
      });
      const count = countData?.count ?? 0;
      const badgeText = count > 99 ? '99+' : String(count);

      const utils = trpc.useUtils();
      const markSeen = trpc.notifications.markSeen.useMutation({
        onSuccess: () => utils.notifications.unreadCount.invalidate(),
      });
      const markRead = trpc.notifications.markRead.useMutation({
        onSuccess: () => {
          utils.notifications.list.invalidate();
          utils.notifications.unreadCount.invalidate();
        },
      });
      // markAllRead: помечает ВСЕ unread юзера (не только в dropdown) — global behavior per SPEC
      const markAllRead = trpc.notifications.markAllRead.useMutation({
        onSuccess: () => {
          utils.notifications.list.invalidate();
          utils.notifications.unreadCount.invalidate();
        },
      });

      // Lazy-fetch dropdown items — только когда открыт
      const { data: listData } = trpc.notifications.list.useQuery(
        { filter: 'all' },
        { enabled: isOpen },
      );
      const items = (listData?.items ?? []).slice(0, DROPDOWN_LIMIT);

      // D-02: split в «Новые» (unread) и «Раньше» (read, последние 7 дней)
      const sevenDaysAgo = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000);
      const newItems = items.filter((n: any) => n.readAt === null);
      const olderItems = items.filter(
        (n: any) => n.readAt !== null && new Date(n.createdAt) >= sevenDaysAgo,
      );

      const handleOpenChange = (open: boolean) => {
        setIsOpen(open);
        if (open) markSeen.mutate(); // D-07
      };

      return (
        <Popover open={isOpen} onOpenChange={handleOpenChange}>
          <PopoverTrigger asChild>
            <button
              type="button"
              aria-label="Уведомления"
              className="relative p-2 hover:bg-mp-gray-100 rounded-md transition-colors"
            >
              <BellIcon />
              {count > 0 && (
                <span
                  className="absolute -top-0.5 -right-0.5 min-w-[18px] h-[18px] px-1
                             rounded-full bg-mp-red-500 text-white text-[10px] font-bold
                             flex items-center justify-center"
                >
                  {badgeText}
                </span>
              )}
            </button>
          </PopoverTrigger>
          <PopoverContent
            align="end"
            sideOffset={8}
            className="w-[380px] max-h-[480px] p-0 z-50 flex flex-col"
          >
            {/* Header */}
            <div className="px-3 py-2 border-b border-mp-gray-100 text-sm font-semibold">
              Уведомления
            </div>

            {/* Body — scrollable */}
            <div className="flex-1 overflow-y-auto">
              {items.length === 0 ? (
                <div className="p-6 text-center text-xs text-mp-gray-500">
                  Пока тихо. Здесь появятся ответы на твои комментарии и важные обновления.
                </div>
              ) : (
                <>
                  {newItems.length > 0 && (
                    <>
                      <div className="px-3 pt-2 pb-1 text-[10px] uppercase tracking-wide text-mp-gray-400 font-semibold">
                        Новые
                      </div>
                      {newItems.map((n: any) => (
                        <NotificationItem
                          key={n.id}
                          notification={n}
                          onClick={() => {
                            if (n.readAt === null) markRead.mutate({ notificationId: n.id });
                            setIsOpen(false);
                          }}
                        />
                      ))}
                    </>
                  )}
                  {olderItems.length > 0 && (
                    <>
                      <div className="px-3 pt-2 pb-1 text-[10px] uppercase tracking-wide text-mp-gray-400 font-semibold">
                        Раньше
                      </div>
                      {olderItems.map((n: any) => (
                        <NotificationItem
                          key={n.id}
                          notification={n}
                          onClick={() => setIsOpen(false)}
                        />
                      ))}
                    </>
                  )}
                </>
              )}
            </div>

            {/* Footer — actions */}
            <div className="border-t border-mp-gray-100 px-3 py-2 flex items-center justify-between gap-2">
              <Link
                href="/notifications"
                onClick={() => setIsOpen(false)}
                className="text-xs text-mp-blue-600 hover:underline"
              >
                Все уведомления →
              </Link>
              <button
                type="button"
                onClick={() => markAllRead.mutate()}
                disabled={count === 0 || markAllRead.isPending}
                className={cn(
                  'text-xs text-mp-gray-500 hover:text-mp-gray-700',
                  (count === 0 || markAllRead.isPending) && 'opacity-50 cursor-not-allowed',
                )}
              >
                Отметить все прочитанными
              </button>
            </div>
          </PopoverContent>
        </Popover>
      );
    }
    ```

    Особенности:
    - `enabled: isOpen` — list НЕ грузится пока dropdown закрыт (экономия RPC)
    - `markSeen.mutate()` на open — обнуляет счётчик в badge (D-07), но `bg-mp-blue-50` остаётся
    - `markRead` срабатывает только если item уже unread (защита от лишних updates)
    - `markAllRead` disabled при count=0 (D-03 / UX hint); помечает ВСЕ unread юзера, не только видимые
    - Пустое состояние с D-15 копией
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "markSeen\|refetchInterval" apps/web/src/components/notifications/NotificationBell.tsx</automated>
  </verify>

  <done>
    - Файл существует
    - `grep -c "markSeen\|markRead\|markAllRead" apps/web/src/components/notifications/NotificationBell.tsx` returns >= 3
    - `grep -n "docHidden ? false : POLL_INTERVAL_MS" apps/web/src/components/notifications/NotificationBell.tsx` находит pause logic
    - `grep -n "Новые\|Раньше" apps/web/src/components/notifications/NotificationBell.tsx` находит группировку
    - `grep -n "Все уведомления\|Отметить все" apps/web/src/components/notifications/NotificationBell.tsx` находит footer CTAs
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Создать страницу /notifications с infinite list + filter all|unread + markAllRead</name>
  <files>apps/web/src/app/(main)/notifications/page.tsx</files>

  <read_first>
    - apps/web/src/app/(main)/profile/history/page.tsx (паттерн full page list)
    - apps/web/src/components/notifications/NotificationItem.tsx (для рендера)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 673-738
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-15 (empty states), D-09 (markAllRead)
  </read_first>

  <action>
    Создать `apps/web/src/app/(main)/notifications/page.tsx`:

    ```typescript
    'use client';

    import { useState, useEffect } from 'react';
    import { useSearchParams } from 'next/navigation';
    import { trpc } from '@/lib/trpc/client';
    import { Button } from '@/components/ui/button';
    import { cn } from '@/lib/utils';
    import { NotificationItem } from '@/components/notifications/NotificationItem';

    type FilterValue = 'all' | 'unread';

    export default function NotificationsPage() {
      const searchParams = useSearchParams();
      const initialFilter = (searchParams.get('filter') as FilterValue) === 'unread' ? 'unread' : 'all';
      const [filter, setFilter] = useState<FilterValue>(initialFilter);

      // sync URL ?filter back if user switches
      useEffect(() => {
        const url = new URL(window.location.href);
        if (filter === 'unread') url.searchParams.set('filter', 'unread');
        else url.searchParams.delete('filter');
        window.history.replaceState({}, '', url.toString());
      }, [filter]);

      const { data, fetchNextPage, hasNextPage, isFetchingNextPage, isLoading } =
        trpc.notifications.list.useInfiniteQuery(
          { filter },
          { getNextPageParam: (last: any) => last?.nextCursor ?? null },
        );

      const utils = trpc.useUtils();
      const markRead = trpc.notifications.markRead.useMutation({
        onSuccess: () => {
          utils.notifications.list.invalidate();
          utils.notifications.unreadCount.invalidate();
        },
      });
      const markAllRead = trpc.notifications.markAllRead.useMutation({
        onSuccess: () => {
          utils.notifications.list.invalidate();
          utils.notifications.unreadCount.invalidate();
        },
      });

      const items = data?.pages.flatMap((p: any) => p?.items ?? []) ?? [];

      const filterPill = (value: FilterValue, label: string) => (
        <button
          key={value}
          type="button"
          onClick={() => setFilter(value)}
          className={cn(
            'px-3 py-1.5 rounded-full text-sm transition-colors',
            filter === value
              ? 'bg-mp-blue-600 text-white'
              : 'bg-mp-gray-100 text-mp-gray-700 hover:bg-mp-gray-200',
          )}
        >
          {label}
        </button>
      );

      return (
        <div className="max-w-2xl mx-auto px-4 py-8 space-y-6">
          <div className="flex items-center justify-between gap-4 flex-wrap">
            <h1 className="text-2xl font-bold text-mp-gray-900">Уведомления</h1>
            <Button
              variant="outline"
              size="sm"
              onClick={() => markAllRead.mutate()}
              disabled={markAllRead.isPending || items.every((n: any) => n.readAt !== null)}
            >
              Отметить все прочитанными
            </Button>
          </div>

          <div className="flex gap-2">
            {filterPill('all', 'Все')}
            {filterPill('unread', 'Непрочитанные')}
          </div>

          {isLoading ? (
            <div className="space-y-2">
              {[0, 1, 2].map((i) => (
                <div key={i} className="h-16 bg-mp-gray-100 rounded animate-pulse" />
              ))}
            </div>
          ) : items.length === 0 ? (
            <p className="text-mp-gray-500 text-center py-12">
              {filter === 'unread'
                ? 'Все уведомления прочитаны. 🎉'
                : 'У тебя пока нет уведомлений.'}
            </p>
          ) : (
            <div className="space-y-1 border border-mp-gray-100 rounded-lg overflow-hidden divide-y divide-mp-gray-100">
              {items.map((n: any) => (
                <NotificationItem
                  key={n.id}
                  notification={n}
                  onClick={() => {
                    if (n.readAt === null) markRead.mutate({ notificationId: n.id });
                  }}
                />
              ))}
            </div>
          )}

          {hasNextPage && (
            <div className="flex justify-center">
              <Button
                variant="outline"
                onClick={() => fetchNextPage()}
                disabled={isFetchingNextPage}
              >
                {isFetchingNextPage ? 'Загрузка...' : 'Показать ещё'}
              </Button>
            </div>
          )}
        </div>
      );
    }
    ```

    Особенности:
    - `useSearchParams` для filter (URL state)
    - `useInfiniteQuery` для пагинации
    - Skeleton loading при первой загрузке
    - Empty states по D-15 (filter=all → "У тебя пока нет уведомлений.", filter=unread → "Все уведомления прочитаны. 🎉")
    - markAllRead disabled когда нет unread
    - Filter pills используют `mp-blue-600` (active) и `mp-gray-100` (inactive)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "useInfiniteQuery\|markAllRead" apps/web/src/app/\(main\)/notifications/page.tsx</automated>
  </verify>

  <done>
    - Файл существует
    - `grep -n "Все уведомления прочитаны" apps/web/src/app/\(main\)/notifications/page.tsx` находит empty state filter=unread
    - `grep -n "У тебя пока нет уведомлений" apps/web/src/app/\(main\)/notifications/page.tsx` находит empty state filter=all
    - `grep -n "useInfiniteQuery" apps/web/src/app/\(main\)/notifications/page.tsx` находит infinite pagination
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 4: Встроить NotificationBell в layout.tsx (Header) перед HelpCircleButton</name>
  <files>apps/web/src/app/(main)/layout.tsx</files>

  <read_first>
    - apps/web/src/app/(main)/layout.tsx (строки 1-100 — header structure, найти точное место HelpCircleButton)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md "Integration Points"
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1104-1141
  </read_first>

  <action>
    В `apps/web/src/app/(main)/layout.tsx`:

    1. **Добавить импорт** (после существующих imports, около line 12):
       ```typescript
       import { NotificationBell } from '@/components/notifications/NotificationBell';
       ```

    2. **Найти блок с HelpCircleButton** (around lines 81-88):
       ```jsx
       <div className="flex items-center gap-2">
         <HelpCircleButton />
         <UserNav user={...} />
       </div>
       ```

    3. **Вставить `<NotificationBell />` ПЕРЕД `<HelpCircleButton />`**:
       ```jsx
       <div className="flex items-center gap-2">
         <NotificationBell />
         <HelpCircleButton />
         <UserNav user={...} />
       </div>
       ```

    Не менять существующие props UserNav. Не менять класс flex-контейнера. Не трогать остальной layout.

    КРИТИЧЕСКИ ВАЖНО: `layout.tsx` — server component, `NotificationBell` — `'use client'`. Это работает в App Router (server component рендерит client component как child).
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "NotificationBell" apps/web/src/app/\(main\)/layout.tsx</automated>
  </verify>

  <done>
    - `grep -c "NotificationBell" apps/web/src/app/\(main\)/layout.tsx` returns 2 (import + JSX)
    - В JSX: NotificationBell стоит ПЕРЕД HelpCircleButton (`grep -A1 "<NotificationBell" apps/web/src/app/\(main\)/layout.tsx | grep "HelpCircleButton"` находит)
    - `pnpm typecheck` + `pnpm build` (если запустить) exit 0
    - Существующие компоненты (HelpCircleButton, UserNav) НЕ удалены
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → tRPC unreadCount/list | Auth через protectedProcedure |
| markRead from dropdown | Ownership check уже в tRPC router (план 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05-01 | I (Info disclosure) | unreadCount polling 60s каждым залогиненным юзером | accept | SPEC constraint: lightweight COUNT через индекс; 500-1000 active users — терпимая нагрузка; pause при document.hidden дополнительно режет |
| T-51-05-02 | D (DoS) | markAllRead на 500+ rows | accept | Hard cap 500/user (план 07); updateMany — single SQL |
| T-51-05-03 | T (Tampering) | URL ?filter=foo → invalid value | mitigate | Tight check `=== 'unread' ? 'unread' : 'all'` — defaults safely |
| T-51-05-04 | I (Info disclosure) | NotificationItem leak preview из чужих уведомлений | mitigate | tRPC list возвращает только userId=ctx.user.id (router guarantee) |
| T-51-05-05 | E (Elevation) | markSeen mutation race condition | accept | Idempotent UPDATE; race не ведёт к state corruption |
</threat_model>

<verification>
- formatRelativeTime extracted в utility, оба компонента (CommentItem + NotificationItem) импортят из `@/lib/utils/format-time`
- Bell отображается в Header на каждой /(main)/* странице
- Polling 60c работает + pause при document.hidden (DevTools Network tab проверка)
- Click на bell → dropdown открывается + markSeen вызван + badge counter сбрасывается
- /notifications page рендерится с пагинацией; filter=unread в URL применяется
- Click на NotificationItem → переход на ctaUrl + markRead вызван
- Empty states корректные для двух фильтров
</verification>

<success_criteria>
- [ ] `apps/web/src/lib/utils/format-time.ts` существует, экспортирует formatRelativeTime
- [ ] CommentItem.tsx больше НЕ определяет formatRelativeTime (импортит из util) + re-export для backward compat
- [ ] NotificationBell.tsx существует и встроен в layout.tsx перед HelpCircleButton
- [ ] NotificationItem.tsx переиспользуется в Bell + /notifications page, импортит formatRelativeTime из util (НЕ из CommentItem)
- [ ] /notifications page работает с infinite scroll + filter all|unread + markAllRead
- [ ] Polling 60s + pause при document.hidden
- [ ] markSeen on dropdown open
- [ ] Empty states по D-15
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm build` exits 0 (smoke check для Next.js Edge runtime compat)
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-05-SUMMARY.md`:
- formatRelativeTime extracted, shared utility работает
- Bell + Item + /notifications работают end-to-end
- Polling + Page Visibility pause подтверждены
- Артефакты для плана 06: NotificationItem можно переиспользовать; layout NEW pattern для подстраницы /profile/notifications
- Известные ограничения: emoji-style icons (Phase 52 заменит на SVG accent при ADMIN_COMMENT_REPLY)
</output>
</content>
</invoke>