---
phase: 51-notification-center-foundation
plan: 03
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - packages/api/src/routers/notifications.ts
  - packages/api/src/root.ts
  - packages/api/src/routers/__tests__/notifications.test.ts
autonomous: true
requirements: [REQ-51-05]

must_haves:
  truths:
    - "tRPC router notifications зарегистрирован в appRouter"
    - "list procedure возвращает 20/page с cursor pagination и фильтром all|unread"
    - "unreadCount procedure учитывает UserProfile.lastNotificationsSeenAt"
    - "markRead отказывает с FORBIDDEN если notification принадлежит другому юзеру"
    - "markAllRead обновляет ВСЕ непрочитанные текущего юзера"
    - "markSeen ставит UserProfile.lastNotificationsSeenAt = NOW()"
    - "getPreferences возвращает все 7 типов с дефолтами если row отсутствует"
    - "updatePreference upserts row для (userId, type)"
  artifacts:
    - path: "packages/api/src/routers/notifications.ts"
      provides: "notificationsRouter: 7 procedures"
      exports: ["notificationsRouter"]
      min_lines: 150
    - path: "packages/api/src/root.ts"
      provides: "appRouter с notifications: notificationsRouter"
      contains: "notifications: notificationsRouter"
    - path: "packages/api/src/routers/__tests__/notifications.test.ts"
      provides: "Vitest tests for permissions + pagination + unreadCount edge cases"
      min_lines: 100
  key_links:
    - from: "packages/api/src/root.ts"
      to: "packages/api/src/routers/notifications.ts"
      via: "import { notificationsRouter } from './routers/notifications'"
    - from: "packages/api/src/routers/notifications.ts"
      to: "@mpstats/shared ALL_NOTIFICATION_TYPES + DEFAULT_IN_APP_PREFS"
      via: "import for getPreferences default fallback"
---

<objective>
Создать tRPC router `notifications` с 7 procedures (list, unreadCount, markRead, markAllRead, markSeen, getPreferences, updatePreference) — все `protectedProcedure` с ownership checks. Зарегистрировать в `appRouter`. Покрыть unit-тестами на permission boundaries (FORBIDDEN при доступе к чужим), cursor pagination, и unreadCount учёт `lastNotificationsSeenAt` (D-07 гибрид mark-as-read).

Purpose: API контракт для NotificationBell (unreadCount, markSeen), /notifications page (list, markRead, markAllRead), /profile/notifications (getPreferences, updatePreference). Без этого router'а UI не имеет источника данных.

Output: 7 procedures + tests + регистрация в appRouter. Готово для UI consumption через `trpc.notifications.*`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@packages/api/src/routers/comments.ts
@packages/api/src/utils/db-errors.ts

<interfaces>
<!-- Existing tRPC infrastructure -->

From packages/api/src/trpc.ts (re-imported pattern from comments.ts):
```typescript
export const router: typeof t.router;
export const protectedProcedure: t.Procedure<...>; // auto-injects ctx.user with .id, throws UNAUTHORIZED
```

From packages/api/src/utils/db-errors.ts:
```typescript
export function handleDatabaseError(error: unknown): never;
// throws TRPCError with appropriate code (PRECONDITION_FAILED, BAD_REQUEST, etc.)
```

Cursor pagination shape (comments.ts:51-91):
```typescript
const items = await ctx.prisma.X.findMany({
  where, orderBy: { createdAt: 'desc' }, take: PAGE_SIZE,
  ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
});
const nextCursor = items.length === PAGE_SIZE ? items[items.length-1].id : null;
return { items, nextCursor, totalCount };
```

Ownership check pattern (comments.ts:159-184):
```typescript
const row = await ctx.prisma.X.findUnique({ where: { id }, select: { userId: true } });
if (!row) throw new TRPCError({ code: 'NOT_FOUND', ... });
if (row.userId !== ctx.user.id) throw new TRPCError({ code: 'FORBIDDEN', ... });
```

From @mpstats/shared:
```typescript
export const ALL_NOTIFICATION_TYPES = [...] as const;
export const DEFAULT_IN_APP_PREFS: Record<string, boolean>;
```

From appRouter (packages/api/src/root.ts current — to be extended):
```typescript
export const appRouter = router({
  profile: profileRouter,
  // ... existing routers ...
  material: materialRouter,
  // <-- add notifications: notificationsRouter
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать packages/api/src/routers/notifications.ts с 7 procedures и Zod input schemas</name>
  <files>packages/api/src/routers/notifications.ts</files>

  <behavior>
    - list({ filter: 'all' | 'unread', cursor? }): возвращает { items: Notification[], nextCursor: string|null, totalCount: number } для ctx.user.id, 20/page
    - unreadCount(): { count: number } — COUNT WHERE userId=me AND readAt IS NULL AND createdAt > lastNotificationsSeenAt
    - markRead({ notificationId }): UPDATE readAt=NOW() — FORBIDDEN если userId !== ctx.user.id
    - markAllRead(): updateMany WHERE userId=me AND readAt IS NULL → returns { count: N }
    - markSeen(): UPDATE UserProfile.lastNotificationsSeenAt = NOW() для ctx.user.id
    - getPreferences(): возвращает массив из 7 prefs с defaults (DEFAULT_IN_APP_PREFS) если row отсутствует
    - updatePreference({ type, inApp?, email? }): upsert row (userId, type) с patch значениями
  </behavior>

  <read_first>
    - packages/api/src/routers/comments.ts (целиком — паттерны cursor pagination, FORBIDDEN, handleDatabaseError)
    - packages/api/src/routers/promo.ts (для multi-procedure CRUD pattern)
    - packages/api/src/utils/db-errors.ts (handleDatabaseError signature)
    - packages/api/src/trpc.ts (router/protectedProcedure exports)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 262-407 (router skeleton + getPreferences default fallback)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-07, D-08, D-09, D-10
    - .planning/phases/51-notification-center-foundation/51-SPEC.md req 5
  </read_first>

  <action>
    Создать файл `packages/api/src/routers/notifications.ts`:

    ```typescript
    /**
     * Notifications Router — In-app notification center (Phase 51).
     *
     * Procedures (all protectedProcedure):
     * - list: Paginated notifications (cursor 20/page, filter all|unread)
     * - unreadCount: Lightweight COUNT for badge polling (uses lastNotificationsSeenAt)
     * - markRead: Mark single notification as read (ownership check)
     * - markAllRead: Mark all current user's notifications as read
     * - markSeen: Set UserProfile.lastNotificationsSeenAt = NOW (D-07 hybrid mark-as-read)
     * - getPreferences: Return all 7 NotificationType prefs with defaults if row missing
     * - updatePreference: Upsert (userId, type) with inApp/email patch
     */

    import { z } from 'zod';
    import { TRPCError } from '@trpc/server';
    import { router, protectedProcedure } from '../trpc';
    import { handleDatabaseError } from '../utils/db-errors';
    import {
      ALL_NOTIFICATION_TYPES,
      DEFAULT_IN_APP_PREFS,
      type NotificationTypeName,
    } from '@mpstats/shared';

    const NOTIFICATIONS_PER_PAGE = 20;

    const NotificationTypeEnum = z.enum(
      ALL_NOTIFICATION_TYPES as unknown as [NotificationTypeName, ...NotificationTypeName[]],
    );

    export const notificationsRouter = router({
      // ─── LIST ─────────────────────────────────────────────────────────────
      list: protectedProcedure
        .input(
          z.object({
            filter: z.enum(['all', 'unread']).default('all'),
            cursor: z.string().optional(),
          }),
        )
        .query(async ({ ctx, input }) => {
          try {
            const { filter, cursor } = input;
            const where = {
              userId: ctx.user.id,
              ...(filter === 'unread' ? { readAt: null } : {}),
            };

            const [totalCount, items] = await Promise.all([
              ctx.prisma.notification.count({ where }),
              ctx.prisma.notification.findMany({
                where,
                orderBy: { createdAt: 'desc' },
                take: NOTIFICATIONS_PER_PAGE,
                ...(cursor ? { cursor: { id: cursor }, skip: 1 } : {}),
              }),
            ]);

            const nextCursor =
              items.length === NOTIFICATIONS_PER_PAGE
                ? items[items.length - 1].id
                : null;

            return { items, nextCursor, totalCount };
          } catch (error) {
            handleDatabaseError(error);
          }
        }),

      // ─── UNREAD COUNT (badge polling, lightweight) ────────────────────────
      unreadCount: protectedProcedure.query(async ({ ctx }) => {
        try {
          const profile = await ctx.prisma.userProfile.findUnique({
            where: { id: ctx.user.id },
            select: { lastNotificationsSeenAt: true },
          });
          const seenAt = profile?.lastNotificationsSeenAt;

          const count = await ctx.prisma.notification.count({
            where: {
              userId: ctx.user.id,
              readAt: null,
              ...(seenAt ? { createdAt: { gt: seenAt } } : {}),
            },
          });
          return { count };
        } catch (error) {
          handleDatabaseError(error);
        }
      }),

      // ─── MARK READ (single, with ownership check) ─────────────────────────
      markRead: protectedProcedure
        .input(z.object({ notificationId: z.string() }))
        .mutation(async ({ ctx, input }) => {
          try {
            const notif = await ctx.prisma.notification.findUnique({
              where: { id: input.notificationId },
              select: { userId: true, readAt: true },
            });

            if (!notif) {
              throw new TRPCError({
                code: 'NOT_FOUND',
                message: 'Notification not found',
              });
            }
            if (notif.userId !== ctx.user.id) {
              throw new TRPCError({
                code: 'FORBIDDEN',
                message: "Cannot mark others' notifications as read",
              });
            }
            if (notif.readAt) return { alreadyRead: true };

            await ctx.prisma.notification.update({
              where: { id: input.notificationId },
              data: { readAt: new Date() },
            });
            return { success: true };
          } catch (error) {
            if (error instanceof TRPCError) throw error;
            handleDatabaseError(error);
          }
        }),

      // ─── MARK ALL READ (current user's unread notifications) ──────────────
      markAllRead: protectedProcedure.mutation(async ({ ctx }) => {
        try {
          const result = await ctx.prisma.notification.updateMany({
            where: { userId: ctx.user.id, readAt: null },
            data: { readAt: new Date() },
          });
          return { count: result.count };
        } catch (error) {
          handleDatabaseError(error);
        }
      }),

      // ─── MARK SEEN (D-07: clears badge counter, dropdown открыт) ──────────
      markSeen: protectedProcedure.mutation(async ({ ctx }) => {
        try {
          await ctx.prisma.userProfile.update({
            where: { id: ctx.user.id },
            data: { lastNotificationsSeenAt: new Date() },
          });
          return { success: true };
        } catch (error) {
          handleDatabaseError(error);
        }
      }),

      // ─── GET PREFERENCES (returns all 7 types with defaults) ──────────────
      getPreferences: protectedProcedure.query(async ({ ctx }) => {
        try {
          const existing = await ctx.prisma.notificationPreference.findMany({
            where: { userId: ctx.user.id },
          });
          const map = new Map(existing.map((p) => [p.type, p]));

          return ALL_NOTIFICATION_TYPES.map((type) => {
            const found = map.get(type);
            if (found) return found;
            return {
              userId: ctx.user.id,
              type,
              inApp: DEFAULT_IN_APP_PREFS[type] ?? true,
              email: false,
            };
          });
        } catch (error) {
          handleDatabaseError(error);
        }
      }),

      // ─── UPDATE PREFERENCE (upsert by composite (userId, type)) ───────────
      updatePreference: protectedProcedure
        .input(
          z.object({
            type: NotificationTypeEnum,
            inApp: z.boolean().optional(),
            email: z.boolean().optional(),
          }),
        )
        .mutation(async ({ ctx, input }) => {
          try {
            const { type, inApp, email } = input;

            // Resolve current values via existing or defaults to populate upsert correctly
            const existing = await ctx.prisma.notificationPreference.findUnique({
              where: { userId_type: { userId: ctx.user.id, type } },
            });

            const nextInApp = inApp ?? existing?.inApp ?? (DEFAULT_IN_APP_PREFS[type] ?? true);
            const nextEmail = email ?? existing?.email ?? false;

            const upserted = await ctx.prisma.notificationPreference.upsert({
              where: { userId_type: { userId: ctx.user.id, type } },
              create: {
                userId: ctx.user.id,
                type,
                inApp: nextInApp,
                email: nextEmail,
              },
              update: {
                ...(inApp !== undefined ? { inApp } : {}),
                ...(email !== undefined ? { email } : {}),
              },
            });

            return upserted;
          } catch (error) {
            handleDatabaseError(error);
          }
        }),
    });
    ```

    Особенности:
    - `markRead` НЕ позволяет помечать чужие notifications — explicit FORBIDDEN throw
    - `markSeen` обновляет UserProfile, не Notification — это D-07 hybrid: счётчик в badge обнуляется, но bg-blue-50 на items сохраняется до явного click'а
    - `getPreferences` возвращает массив из 7 объектов всегда, включая пользовательские override'ы
    - `updatePreference` использует upsert с правильным фолбэком для НЕ-указанных полей (inApp без email или vice versa)
    - `NotificationTypeEnum` — Zod enum, построенный из ALL_NOTIFICATION_TYPES (single source of truth с shared package)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -c "protectedProcedure" packages/api/src/routers/notifications.ts</automated>
  </verify>

  <done>
    - Файл существует с 7 procedures
    - `grep -c "protectedProcedure" packages/api/src/routers/notifications.ts` returns >= 7
    - `grep -n "FORBIDDEN" packages/api/src/routers/notifications.ts` находит ownership check в markRead
    - `grep -n "lastNotificationsSeenAt" packages/api/src/routers/notifications.ts` находит use в unreadCount + markSeen
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Зарегистрировать notificationsRouter в packages/api/src/root.ts</name>
  <files>packages/api/src/root.ts</files>

  <read_first>
    - packages/api/src/root.ts (целиком — текущий список routers)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1144-1170
  </read_first>

  <action>
    В `packages/api/src/root.ts`:

    1. Добавить импорт после существующих router imports:
       ```typescript
       import { notificationsRouter } from './routers/notifications';
       ```

    2. Добавить ключ `notifications: notificationsRouter` в объект `appRouter` (после `material: materialRouter` или в любом месте — порядок не важен функционально, но для consistency держать ближе к концу со другими новыми routers).

    Не трогать остальные routers. Не менять export type AppRouter.
  </action>

  <verify>
    <automated>cd MAAL && grep -n "notificationsRouter" packages/api/src/root.ts && pnpm typecheck</automated>
  </verify>

  <done>
    - `grep -c "notificationsRouter" packages/api/src/root.ts` returns >= 2 (import + use)
    - `grep -n "notifications: notificationsRouter" packages/api/src/root.ts` находит ключ
    - `pnpm typecheck` exits 0 (TS infer правильно подхватит router в clientstrokes)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit-тесты на permission + pagination + getPreferences defaults</name>
  <files>packages/api/src/routers/__tests__/notifications.test.ts</files>

  <behavior>
    - markRead other user's notification → throws FORBIDDEN
    - markRead non-existent → throws NOT_FOUND
    - markRead own already-read → returns { alreadyRead: true } без update call
    - list with cursor returns nextCursor when items.length === PAGE_SIZE
    - list filter='unread' добавляет readAt: null where clause
    - unreadCount with seenAt → adds createdAt > seenAt filter
    - unreadCount without seenAt (null) → no createdAt filter
    - getPreferences с 0 rows в БД → возвращает 7 объектов из defaults (WEEKLY_DIGEST inApp=false, остальные true)
    - getPreferences с 2 override rows → они в результате с правильными values
  </behavior>

  <read_first>
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1226-1278 (testing pattern)
    - packages/api/src/routers/__tests__/material.test.ts если существует, или другой router test для синтаксиса (Glob check)
    - packages/api/src/routers/notifications.ts (созданный в Task 1)
  </read_first>

  <action>
    Сначала проверить структуру существующих router тестов:
    ```bash
    ls packages/api/src/routers/__tests__/
    ```

    Создать `packages/api/src/routers/__tests__/notifications.test.ts`:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';
    import { TRPCError } from '@trpc/server';
    import { notificationsRouter } from '../notifications';

    function makeCtx(overrides: any = {}) {
      const userId = overrides.userId ?? 'user-a';
      return {
        user: { id: userId },
        prisma: {
          notification: {
            findUnique: vi.fn(),
            findMany: vi.fn().mockResolvedValue([]),
            count: vi.fn().mockResolvedValue(0),
            update: vi.fn().mockResolvedValue({}),
            updateMany: vi.fn().mockResolvedValue({ count: 0 }),
          },
          notificationPreference: {
            findUnique: vi.fn().mockResolvedValue(null),
            findMany: vi.fn().mockResolvedValue([]),
            upsert: vi.fn(),
          },
          userProfile: {
            findUnique: vi.fn().mockResolvedValue({ lastNotificationsSeenAt: null }),
            update: vi.fn(),
          },
          ...overrides.prisma,
        },
      };
    }

    function caller(ctx: any) {
      return notificationsRouter.createCaller(ctx);
    }

    beforeEach(() => {
      vi.clearAllMocks();
    });

    describe('notifications.markRead', () => {
      it('throws FORBIDDEN when notification belongs to another user', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.findUnique.mockResolvedValue({
          userId: 'user-b',
          readAt: null,
        });

        await expect(
          caller(ctx).markRead({ notificationId: 'n-1' }),
        ).rejects.toThrow(/FORBIDDEN|Cannot mark/);
        expect(ctx.prisma.notification.update).not.toHaveBeenCalled();
      });

      it('throws NOT_FOUND when notification does not exist', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.findUnique.mockResolvedValue(null);

        await expect(
          caller(ctx).markRead({ notificationId: 'missing' }),
        ).rejects.toThrow(/NOT_FOUND|not found/i);
      });

      it('returns alreadyRead without update when notification.readAt is set', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.findUnique.mockResolvedValue({
          userId: 'user-a',
          readAt: new Date('2026-01-01'),
        });

        const result = await caller(ctx).markRead({ notificationId: 'n-1' });
        expect(result).toEqual({ alreadyRead: true });
        expect(ctx.prisma.notification.update).not.toHaveBeenCalled();
      });

      it('updates readAt and returns success when own unread notification', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.findUnique.mockResolvedValue({
          userId: 'user-a',
          readAt: null,
        });

        const result = await caller(ctx).markRead({ notificationId: 'n-1' });
        expect(result).toEqual({ success: true });
        expect(ctx.prisma.notification.update).toHaveBeenCalledTimes(1);
      });
    });

    describe('notifications.list', () => {
      it('adds readAt: null filter when filter=unread', async () => {
        const ctx = makeCtx();
        await caller(ctx).list({ filter: 'unread' });

        const findManyArgs = ctx.prisma.notification.findMany.mock.calls[0][0];
        expect(findManyArgs.where).toMatchObject({ userId: 'user-a', readAt: null });
      });

      it('returns nextCursor when items length equals PAGE_SIZE (20)', async () => {
        const ctx = makeCtx();
        const items = Array.from({ length: 20 }, (_, i) => ({
          id: `n-${i}`, userId: 'user-a', type: 'COMMENT_REPLY',
          payload: {}, ctaUrl: null, readAt: null, createdAt: new Date(), broadcastId: null,
        }));
        ctx.prisma.notification.findMany.mockResolvedValue(items);
        ctx.prisma.notification.count.mockResolvedValue(50);

        const result = await caller(ctx).list({ filter: 'all' });
        expect(result.nextCursor).toBe('n-19');
        expect(result.totalCount).toBe(50);
      });

      it('returns nextCursor=null when items length less than PAGE_SIZE', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.findMany.mockResolvedValue([
          { id: 'n-1', userId: 'user-a', type: 'COMMENT_REPLY', payload: {}, ctaUrl: null, readAt: null, createdAt: new Date(), broadcastId: null },
        ]);

        const result = await caller(ctx).list({ filter: 'all' });
        expect(result.nextCursor).toBeNull();
      });
    });

    describe('notifications.unreadCount', () => {
      it('does not add createdAt filter when lastNotificationsSeenAt is null', async () => {
        const ctx = makeCtx();
        ctx.prisma.userProfile.findUnique.mockResolvedValue({ lastNotificationsSeenAt: null });
        ctx.prisma.notification.count.mockResolvedValue(3);

        const result = await caller(ctx).unreadCount();
        expect(result).toEqual({ count: 3 });

        const countArgs = ctx.prisma.notification.count.mock.calls[0][0];
        expect(countArgs.where).not.toHaveProperty('createdAt');
        expect(countArgs.where).toMatchObject({ userId: 'user-a', readAt: null });
      });

      it('adds createdAt > seenAt filter when lastNotificationsSeenAt is set', async () => {
        const seenAt = new Date('2026-04-01');
        const ctx = makeCtx();
        ctx.prisma.userProfile.findUnique.mockResolvedValue({ lastNotificationsSeenAt: seenAt });
        ctx.prisma.notification.count.mockResolvedValue(1);

        await caller(ctx).unreadCount();

        const countArgs = ctx.prisma.notification.count.mock.calls[0][0];
        expect(countArgs.where.createdAt).toEqual({ gt: seenAt });
      });
    });

    describe('notifications.getPreferences', () => {
      it('returns 7 default prefs when no rows exist', async () => {
        const ctx = makeCtx();
        ctx.prisma.notificationPreference.findMany.mockResolvedValue([]);

        const result = await caller(ctx).getPreferences();
        expect(result).toHaveLength(7);

        const weekly = result.find((p: any) => p.type === 'WEEKLY_DIGEST');
        expect(weekly?.inApp).toBe(false); // SPEC req 2 default
        expect(weekly?.email).toBe(false);

        const commentReply = result.find((p: any) => p.type === 'COMMENT_REPLY');
        expect(commentReply?.inApp).toBe(true);
        expect(commentReply?.email).toBe(false);
      });

      it('merges user override rows with defaults', async () => {
        const ctx = makeCtx();
        ctx.prisma.notificationPreference.findMany.mockResolvedValue([
          { userId: 'user-a', type: 'COMMENT_REPLY', inApp: false, email: false },
          { userId: 'user-a', type: 'WEEKLY_DIGEST', inApp: true, email: true },
        ]);

        const result = await caller(ctx).getPreferences();
        const cr = result.find((p: any) => p.type === 'COMMENT_REPLY');
        const wd = result.find((p: any) => p.type === 'WEEKLY_DIGEST');
        expect(cr?.inApp).toBe(false);
        expect(wd?.inApp).toBe(true);
        expect(wd?.email).toBe(true);
      });
    });

    describe('notifications.markSeen', () => {
      it('updates UserProfile.lastNotificationsSeenAt with NOW()', async () => {
        const ctx = makeCtx();
        await caller(ctx).markSeen();

        expect(ctx.prisma.userProfile.update).toHaveBeenCalledWith({
          where: { id: 'user-a' },
          data: { lastNotificationsSeenAt: expect.any(Date) },
        });
      });
    });

    describe('notifications.markAllRead', () => {
      it('updateMany only own unread notifications', async () => {
        const ctx = makeCtx();
        ctx.prisma.notification.updateMany.mockResolvedValue({ count: 5 });

        const result = await caller(ctx).markAllRead();
        expect(result).toEqual({ count: 5 });
        expect(ctx.prisma.notification.updateMany).toHaveBeenCalledWith({
          where: { userId: 'user-a', readAt: null },
          data: { readAt: expect.any(Date) },
        });
      });
    });
    ```

    **Note about caller():** Если `notificationsRouter.createCaller` не работает напрямую (зависит от tRPC версии в проекте), использовать `notificationsRouter._def.procedures.markRead` либо invoke через любой существующий router-test pattern. Проверить как сделано в существующих router tests перед написанием.
  </action>

  <verify>
    <automated>cd MAAL && pnpm test --run packages/api/src/routers/__tests__/notifications.test.ts</automated>
  </verify>

  <done>
    - Все ~10 unit-тестов PASS
    - `grep -c "it(" packages/api/src/routers/__tests__/notifications.test.ts` returns >= 10
    - Покрытие: FORBIDDEN, NOT_FOUND, alreadyRead, list pagination, unread filter, unreadCount with/without seenAt, getPreferences defaults + overrides, markSeen, markAllRead
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → tRPC | Все procedures протектед, ctx.user.id auto-injected; ownership check для markRead |
| markRead → DB | Двухступенчатая проверка: findUnique with select userId, затем update where id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-03-01 | E (Elevation) | markRead другого юзера | mitigate | findUnique → if userId !== ctx.user.id throw FORBIDDEN; explicit unit test |
| T-51-03-02 | E (Elevation) | markSeen другого юзера | mitigate | UPDATE WHERE id = ctx.user.id — невозможно затронуть чужой row |
| T-51-03-03 | I (Info disclosure) | list возвращает чужие notifications | mitigate | WHERE userId = ctx.user.id — неизменяемое условие |
| T-51-03-04 | T (Tampering) | updatePreference переключает чужую pref | mitigate | upsert WHERE userId_type = (ctx.user.id, type) — нельзя targeting'ом другого юзера |
| T-51-03-05 | D (DoS) | markAllRead на 10K rows | accept | updateMany — single SQL UPDATE; 90d retention + 500/user cap (план 07) ограничивает scale |
| T-51-03-06 | I (Info disclosure) | NOT_FOUND vs FORBIDDEN leak | accept | NOT_FOUND для missing rows + FORBIDDEN для existing rows другого юзера — стандартная REST практика, минимальный leak |
</threat_model>

<verification>
- 7 procedures работают и регистрированы в appRouter
- Permission boundaries проверены unit-тестами
- Cursor pagination возвращает корректный nextCursor
- getPreferences возвращает 7 объектов всегда
- updatePreference upsert'ит без потери уже существующих полей
</verification>

<success_criteria>
- [ ] `packages/api/src/routers/notifications.ts` экспортирует `notificationsRouter` с 7 procedures
- [ ] `packages/api/src/root.ts` регистрирует `notifications: notificationsRouter`
- [ ] Все unit-тесты в `__tests__/notifications.test.ts` PASS
- [ ] `pnpm typecheck` exits 0
- [ ] markRead проверяет ownership через FORBIDDEN
- [ ] unreadCount учитывает lastNotificationsSeenAt
- [ ] getPreferences fallback работает для всех 7 типов
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-03-SUMMARY.md`:
- 7 procedures + сигнатуры (input/output)
- TRPC ключ `notifications` теперь доступен в frontend через `trpc.notifications.*`
- Артефакты для UI планов (5, 6): list, unreadCount, markRead, markAllRead, markSeen, getPreferences, updatePreference
</output>
