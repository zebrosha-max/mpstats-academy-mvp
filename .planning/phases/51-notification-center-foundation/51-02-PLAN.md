---
phase: 51-notification-center-foundation
plan: 02
type: execute
wave: 2
depends_on: [51-01]
files_modified:
  - apps/web/src/lib/notifications/notify.ts
  - apps/web/src/lib/notifications/__tests__/notify.test.ts
autonomous: true
requirements: [REQ-51-04]

must_haves:
  truths:
    - "notify() создаёт Notification row если NotificationPreference.inApp = true"
    - "notify() НЕ создаёт row если inApp = false, но CQ event всё равно стреляет"
    - "notify() возвращается early если payload.actorUserId === userId (anti-self-notify)"
    - "notify() try/catch с Sentry — caller (route handler) не падает при ошибке CQ"
    - "notifyMany() bulk-insert через createMany"
    - "notifyCommentReply() helper резолвит lessonTitle и зовёт notify()"
  artifacts:
    - path: "apps/web/src/lib/notifications/notify.ts"
      provides: "notify, notifyMany, notifyCommentReply"
      exports: ["notify", "notifyMany", "notifyCommentReply"]
      min_lines: 80
    - path: "apps/web/src/lib/notifications/__tests__/notify.test.ts"
      provides: "Vitest unit tests, 3+ scenarios for notify(), 1 for notifyCommentReply()"
      exports: ["describe blocks"]
  key_links:
    - from: "apps/web/src/lib/notifications/notify.ts"
      to: "apps/web/src/lib/carrotquest/client.ts"
      via: "import { cq } from '@/lib/carrotquest/client'"
    - from: "apps/web/src/lib/notifications/notify.ts"
      to: "@mpstats/db prisma client"
      via: "import { prisma } from '@mpstats/db/client'"
    - from: "apps/web/src/lib/notifications/notify.ts"
      to: "@mpstats/shared NotificationPayload + DEFAULT_IN_APP_PREFS"
      via: "import { NotificationPayload, DEFAULT_IN_APP_PREFS } from '@mpstats/shared'"
---

<objective>
Создать centralized service `notify()` в `apps/web/src/lib/notifications/notify.ts` (D-01 critical_architecture: расположение НЕ в packages/api/src/services из-за workspace dependency direction). Сервис: (a) проверяет NotificationPreference.inApp, (b) создаёт Notification row если разрешено, (c) ВСЕГДА триггерит CQ event `pa_notif_<type>`, (d) anti-self-notify через `payload.actorUserId === userId`, (e) ошибки fire-and-forget через Sentry. Плюс `notifyMany()` для bulk и `notifyCommentReply()` helper который сам fetch'ит parent comment + lesson title.

Purpose: Без этого сервиса COMMENT_REPLY trigger некуда подключить, broadcast в Phase 54 негде создавать. Фундамент для всех последующих типов уведомлений.

Output: Модуль `notify.ts` + Vitest unit tests с 3+ scenarios (D-08 anti-self-notify, inApp=false skip row but fire CQ, inApp=true happy path), плюс отдельный test для `notifyCommentReply` (резолвит lessonTitle).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@.planning/phases/51-notification-center-foundation/51-01-SUMMARY.md
@apps/web/src/lib/carrotquest/emails.ts
@apps/web/src/lib/carrotquest/client.ts

<interfaces>
<!-- Reusable: cq singleton + setUserProps + trackEvent + isEmailEnabled -->

From apps/web/src/lib/carrotquest/client.ts:
```typescript
export const cq = {
  setUserProps(userId: string, props: Record<string, string>): Promise<void>;
  trackEvent(userId: string, eventName: CQEventName, props?: Record<string, string>): Promise<void>;
};
```

From apps/web/src/lib/carrotquest/emails.ts:17-23 (reportError pattern):
```typescript
function reportEmailError(stage: string, userId: string, error: unknown): void {
  console.error(`[Email] ${stage} error for ${userId}:`, error);
  Sentry.captureException(error, {
    tags: { area: 'carrotquest-email', stage },
    extra: { userId },
  });
}
```

From apps/web/src/lib/carrotquest/emails.ts:64-83 (sendPaymentSuccessEmail — analog для notify shape):
```typescript
export async function sendPaymentSuccessEmail(userId: string, data: { ... }): Promise<void> {
  try {
    if (!(await isEmailEnabled())) return;
    await cq.setUserProps(userId, { pa_amount: ..., pa_period_end: ... });
    await cq.trackEvent(userId, 'pa_payment_success');
  } catch (error) {
    reportEmailError('sendPaymentSuccessEmail', userId, error);
  }
}
```

From @mpstats/shared (created в плане 01):
```typescript
export type NotificationPayload = { type: 'COMMENT_REPLY'; commentId: string; lessonId: string; lessonTitle: string; replyAuthorName: string; preview: string; actorUserId: string; } | { type: 'ADMIN_COMMENT_REPLY'; ... } | ...;
export const DEFAULT_IN_APP_PREFS: Record<string, boolean>;
```

From @mpstats/db (Prisma client после плана 01):
```typescript
prisma.notification.create({ data: { userId, type, payload, ctaUrl?, broadcastId? } });
prisma.notification.createMany({ data: [...] });
prisma.notificationPreference.findUnique({ where: { userId_type: { userId, type } } });
prisma.lessonComment.findUnique({ where: { id }, include: { user: true } });
prisma.lesson.findUnique({ where: { id }, select: { title: true } });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Создать apps/web/src/lib/notifications/notify.ts с notify() + notifyMany() + Sentry fire-and-forget</name>
  <files>apps/web/src/lib/notifications/notify.ts</files>

  <behavior>
    - notify(userId='userA', type='COMMENT_REPLY', payload={ ..., actorUserId: 'userB' }):
        - findUnique pref returns { inApp: true } → create row + setUserProps + trackEvent
    - notify(userId='userA', type='COMMENT_REPLY', payload={ ..., actorUserId: 'userB' }):
        - findUnique pref returns { inApp: false } → NO row created, BUT trackEvent still fires
    - notify(userId='userA', type='COMMENT_REPLY', payload={ ..., actorUserId: 'userA' }):
        - actorUserId === userId → return early, NO row, NO event
    - notify with pref undefined (no row in DB) → use DEFAULT_IN_APP_PREFS[type] (true for COMMENT_REPLY, false for WEEKLY_DIGEST)
    - notify with cq.trackEvent throwing → Sentry.captureException called, function does NOT throw
    - notify with type='CONTENT_UPDATE' (no actorUserId in payload) → anti-self-notify NOT triggered (correctly bypassed since broadcast-style notifications)
    - notifyMany(userIds=['a','b','c'], type, buildPayload, opts) → calls createMany once + trackEvent 3 times sequentially
  </behavior>

  <read_first>
    - apps/web/src/lib/carrotquest/emails.ts (целиком — паттерн try/catch + reportError + setUserProps + trackEvent)
    - apps/web/src/lib/carrotquest/client.ts (export cq singleton)
    - apps/web/src/lib/carrotquest/types.ts (CQEventName union с pa_notif_*)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 130-258 (notify pattern detail)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md секции D-08 (mark-as-read), DC-08 (anti-self-notify), DC-09 (notifyMany rate-limit)
    - .planning/phases/51-notification-center-foundation/51-SPEC.md req 4
  </read_first>

  <action>
    Создать файл `apps/web/src/lib/notifications/notify.ts`:

    ```typescript
    /**
     * Notification service — centralized notify() for all in-app + CQ events.
     *
     * Phase 51: COMMENT_REPLY is the only live trigger.
     * Phases 52-54 add ADMIN_COMMENT_REPLY, CONTENT_UPDATE, retention types, BROADCAST.
     *
     * Behavior:
     * 1. Anti-self-notify: if payload.actorUserId === userId → return early (no row, no event).
     * 2. Check NotificationPreference.inApp; default = DEFAULT_IN_APP_PREFS[type] if no row.
     * 3. If inApp enabled → create Notification row.
     * 4. Always fire CQ event `pa_notif_<type_lowercase>` (CQ-rule decides email delivery).
     * 5. Errors are captured via Sentry but never thrown (fire-and-forget).
     */

    import * as Sentry from '@sentry/nextjs';
    import { prisma } from '@mpstats/db/client';
    import {
      DEFAULT_IN_APP_PREFS,
      type NotificationPayload,
      type NotificationTypeName,
    } from '@mpstats/shared';
    import { cq } from '@/lib/carrotquest/client';
    import type { CQEventName } from '@/lib/carrotquest/types';

    function reportNotifyError(stage: string, userId: string, error: unknown): void {
      console.error(`[Notify] ${stage} error for ${userId}:`, error);
      Sentry.captureException(error, {
        tags: { area: 'notifications', stage },
        extra: { userId },
      });
    }

    function eventNameFor(type: NotificationTypeName): CQEventName {
      return `pa_notif_${type.toLowerCase()}` as CQEventName;
    }

    export interface NotifyOpts {
      ctaUrl?: string;
      broadcastId?: string;
    }

    export async function notify(
      userId: string,
      type: NotificationTypeName,
      payload: NotificationPayload,
      opts: NotifyOpts = {},
    ): Promise<void> {
      try {
        // 1) Anti-self-notify (DC-08): не уведомлять автора об ответе на свой коммент
        if ('actorUserId' in payload && payload.actorUserId === userId) {
          return;
        }

        // 2) Check preference (default fallback if row absent)
        const pref = await prisma.notificationPreference.findUnique({
          where: { userId_type: { userId, type } },
        });
        const inAppEnabled = pref ? pref.inApp : (DEFAULT_IN_APP_PREFS[type] ?? true);

        // 3) Create Notification row if in-app enabled
        if (inAppEnabled) {
          await prisma.notification.create({
            data: {
              userId,
              type,
              payload: payload as unknown as object, // Prisma Json
              ctaUrl: opts.ctaUrl ?? null,
              broadcastId: opts.broadcastId ?? null,
            },
          });
        }

        // 4) Always fire CQ event (CQ rule decides email delivery)
        try {
          // setUserProps for preview/lead-level fields (CQ rules read from lead, NOT event params)
          if ('preview' in payload && typeof payload.preview === 'string') {
            await cq.setUserProps(userId, {
              pa_notif_preview: payload.preview,
              pa_notif_type: type,
            });
          }
          await cq.trackEvent(userId, eventNameFor(type));
        } catch (cqError) {
          reportNotifyError('cq', userId, cqError);
          // продолжаем — DB row уже создан, не ронять caller
        }
      } catch (error) {
        reportNotifyError('notify', userId, error);
        // Fire-and-forget: caller (route handler) НЕ должен падать
      }
    }

    /**
     * Bulk variant for retention/broadcast (Phase 53/54).
     * Uses createMany for performance; fires CQ events sequentially (rate-limit aware).
     */
    export async function notifyMany(
      userIds: string[],
      type: NotificationTypeName,
      buildPayload: (userId: string) => NotificationPayload,
      opts: NotifyOpts = {},
    ): Promise<void> {
      if (userIds.length === 0) return;

      try {
        const rows = userIds.map((userId) => ({
          userId,
          type,
          payload: buildPayload(userId) as unknown as object,
          ctaUrl: opts.ctaUrl ?? null,
          broadcastId: opts.broadcastId ?? null,
        }));
        await prisma.notification.createMany({ data: rows });
      } catch (error) {
        reportNotifyError('notifyMany.createMany', 'bulk', error);
      }

      // CQ events sequentially (50 events/sec CQ rate limit per memory)
      for (const userId of userIds) {
        try {
          await cq.trackEvent(userId, eventNameFor(type));
        } catch (error) {
          reportNotifyError('notifyMany.cq', userId, error);
        }
      }
    }

    /**
     * Helper для COMMENT_REPLY триггера. Resolve parent comment + lesson title,
     * затем зовёт notify().
     *
     * Usage (called from /api/notifications/notify-reply route):
     *   await notifyCommentReply({ replyCommentId, actorUserId });
     */
    export interface NotifyCommentReplyArgs {
      replyCommentId: string;
      actorUserId: string;
    }

    export async function notifyCommentReply(args: NotifyCommentReplyArgs): Promise<void> {
      try {
        const reply = await prisma.lessonComment.findUnique({
          where: { id: args.replyCommentId },
          select: {
            id: true,
            content: true,
            parentId: true,
            lessonId: true,
            user: { select: { name: true } },
          },
        });
        if (!reply || !reply.parentId) return; // not a reply or comment gone

        const parent = await prisma.lessonComment.findUnique({
          where: { id: reply.parentId },
          select: { id: true, userId: true },
        });
        if (!parent) return; // parent deleted

        // Anti-self-notify additional guard (notify() also checks via payload.actorUserId)
        if (parent.userId === args.actorUserId) return;

        const lesson = await prisma.lesson.findUnique({
          where: { id: reply.lessonId },
          select: { title: true },
        });
        const lessonTitle = lesson?.title ?? 'Урок';

        const replyAuthorName = reply.user.name ?? 'Пользователь';
        const preview = reply.content.slice(0, 120);

        await notify(
          parent.userId,
          'COMMENT_REPLY',
          {
            type: 'COMMENT_REPLY',
            commentId: reply.id,
            lessonId: reply.lessonId,
            lessonTitle,
            replyAuthorName,
            preview,
            actorUserId: args.actorUserId,
          },
          {
            ctaUrl: `/learn/${reply.lessonId}#comment-${reply.id}`,
          },
        );
      } catch (error) {
        reportNotifyError('notifyCommentReply', args.actorUserId, error);
      }
    }
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - НЕ импортировать `cq` из `packages/api/...` — workspace dep direction (apps/web → packages, не наоборот)
    - НЕ throw'ать из notify() — caller (route handler) не должен падать. Только Sentry.captureException
    - NotificationPreference.inApp default fallback = `DEFAULT_IN_APP_PREFS[type]` (из shared package)
    - **Anti-self-notify check.** TypeScript discriminated union гарантирует `actorUserId` присутствует ТОЛЬКО в `COMMENT_REPLY` и `ADMIN_COMMENT_REPLY` variants — `'actorUserId' in payload` корректно type-narrows и фильтрует. Для `CONTENT_UPDATE` / `INACTIVITY_RETURN` / `WEEKLY_DIGEST` / `BROADCAST` / `PROGRESS_NUDGE` оператор вернёт `false` (поля нет в типе), anti-self-notify не триггерится — это корректно, поскольку это broadcast/system-инициированные уведомления (нет «автора», есть только получатель).
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -c "export async function" apps/web/src/lib/notifications/notify.ts</automated>
  </verify>

  <done>
    - Файл `apps/web/src/lib/notifications/notify.ts` существует
    - `grep -c "export async function" apps/web/src/lib/notifications/notify.ts` returns 3 (notify, notifyMany, notifyCommentReply)
    - `grep -n "actorUserId === userId" apps/web/src/lib/notifications/notify.ts` находит anti-self check
    - `grep -n "DEFAULT_IN_APP_PREFS\[type\]" apps/web/src/lib/notifications/notify.ts` находит default fallback
    - `grep -n "Sentry.captureException" apps/web/src/lib/notifications/notify.ts` находит >= 1 захват
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать unit-тесты apps/web/src/lib/notifications/__tests__/notify.test.ts с 3+ scenarios + notifyCommentReply</name>
  <files>apps/web/src/lib/notifications/__tests__/notify.test.ts</files>

  <behavior>
    - Test 1 (happy path): notify with inApp=true → prisma.notification.create called once, cq.trackEvent called once with 'pa_notif_comment_reply'
    - Test 2 (inApp=false): notify with pref { inApp: false } → prisma.notification.create NOT called, cq.trackEvent STILL called
    - Test 3 (anti-self-notify): notify with payload.actorUserId === userId → neither prisma.notification.create NOR cq.trackEvent called
    - Test 4 (default fallback): notify with pref=null + type='COMMENT_REPLY' → uses DEFAULT_IN_APP_PREFS['COMMENT_REPLY']=true → row created
    - Test 5 (default fallback WEEKLY_DIGEST): notify with pref=null + type='WEEKLY_DIGEST' → DEFAULT_IN_APP_PREFS=false → no row, but CQ fires
    - Test 6 (CQ error doesn't break): notify where cq.trackEvent throws → Sentry.captureException called, notify() resolves successfully
    - Test 7 (CONTENT_UPDATE bypasses anti-self-notify): notify with type='CONTENT_UPDATE' (payload без actorUserId) → row created + CQ event fires НОРМАЛЬНО (anti-self-notify check вернул false → не сработал → код прошёл дальше)
    - Test 8 (INACTIVITY_RETURN bypasses anti-self-notify): same as above для INACTIVITY_RETURN
    - Test 9 (notifyCommentReply happy path): mocks parent + reply + lesson, asserts notify called with correct payload + ctaUrl
    - Test 10 (notifyCommentReply self-reply skip): parent.userId === actorUserId → no notification created
  </behavior>

  <read_first>
    - .planning/codebase/TESTING.md
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1226-1278 (Testing Pattern)
    - apps/web/src/lib/notifications/notify.ts (созданный в Task 1)
    - Любой существующий vitest unit test в `apps/web/src/lib/__tests__/` или `packages/api/src/routers/__tests__/` для синтаксиса моков
  </read_first>

  <action>
    Создать файл `apps/web/src/lib/notifications/__tests__/notify.test.ts`:

    ```typescript
    import { describe, it, expect, vi, beforeEach } from 'vitest';

    // Hoisted mocks (vi.mock is hoisted to top)
    vi.mock('@mpstats/db/client', () => ({
      prisma: {
        notification: {
          create: vi.fn().mockResolvedValue({ id: 'notif-1' }),
          createMany: vi.fn().mockResolvedValue({ count: 0 }),
        },
        notificationPreference: {
          findUnique: vi.fn(),
        },
        lessonComment: {
          findUnique: vi.fn(),
        },
        lesson: {
          findUnique: vi.fn(),
        },
      },
    }));

    vi.mock('@/lib/carrotquest/client', () => ({
      cq: {
        setUserProps: vi.fn().mockResolvedValue(undefined),
        trackEvent: vi.fn().mockResolvedValue(undefined),
      },
    }));

    vi.mock('@sentry/nextjs', () => ({
      captureException: vi.fn(),
    }));

    import { notify, notifyCommentReply } from '../notify';
    import { prisma } from '@mpstats/db/client';
    import { cq } from '@/lib/carrotquest/client';
    import * as Sentry from '@sentry/nextjs';

    const COMMENT_REPLY_PAYLOAD = {
      type: 'COMMENT_REPLY' as const,
      commentId: 'reply-1',
      lessonId: 'lesson-1',
      lessonTitle: 'Test Lesson',
      replyAuthorName: 'User B',
      preview: 'Test reply content',
      actorUserId: 'user-b',
    };

    const CONTENT_UPDATE_PAYLOAD = {
      type: 'CONTENT_UPDATE' as const,
      courseId: 'course-1',
      courseTitle: 'Аналитика',
      lessonIds: ['l1', 'l2'],
    };

    const INACTIVITY_RETURN_PAYLOAD = {
      type: 'INACTIVITY_RETURN' as const,
      daysSinceLastActive: 14,
    };

    beforeEach(() => {
      vi.clearAllMocks();
    });

    describe('notify()', () => {
      it('creates Notification row + fires CQ event when inApp=true', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({
          userId: 'user-a',
          type: 'COMMENT_REPLY',
          inApp: true,
          email: false,
        });

        await notify('user-a', 'COMMENT_REPLY', COMMENT_REPLY_PAYLOAD, {
          ctaUrl: '/learn/lesson-1#comment-reply-1',
        });

        expect(prisma.notification.create).toHaveBeenCalledTimes(1);
        expect(prisma.notification.create).toHaveBeenCalledWith({
          data: expect.objectContaining({
            userId: 'user-a',
            type: 'COMMENT_REPLY',
            ctaUrl: '/learn/lesson-1#comment-reply-1',
          }),
        });
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_comment_reply');
      });

      it('skips row creation but STILL fires CQ event when inApp=false', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({
          userId: 'user-a',
          type: 'COMMENT_REPLY',
          inApp: false,
          email: false,
        });

        await notify('user-a', 'COMMENT_REPLY', COMMENT_REPLY_PAYLOAD);

        expect(prisma.notification.create).not.toHaveBeenCalled();
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_comment_reply');
      });

      it('returns early without row or CQ event when actorUserId === userId (anti-self)', async () => {
        await notify(
          'user-b',
          'COMMENT_REPLY',
          { ...COMMENT_REPLY_PAYLOAD, actorUserId: 'user-b' },
        );

        expect(prisma.notificationPreference.findUnique).not.toHaveBeenCalled();
        expect(prisma.notification.create).not.toHaveBeenCalled();
        expect(cq.trackEvent).not.toHaveBeenCalled();
      });

      it('uses DEFAULT_IN_APP_PREFS=true for COMMENT_REPLY when no preference row exists', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue(null);

        await notify('user-a', 'COMMENT_REPLY', COMMENT_REPLY_PAYLOAD);

        expect(prisma.notification.create).toHaveBeenCalledTimes(1);
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_comment_reply');
      });

      it('uses DEFAULT_IN_APP_PREFS=false for WEEKLY_DIGEST when no preference row exists', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue(null);

        await notify('user-a', 'WEEKLY_DIGEST', {
          type: 'WEEKLY_DIGEST',
          newLessonsCount: 3,
          activityCount: 12,
        });

        expect(prisma.notification.create).not.toHaveBeenCalled();
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_weekly_digest');
      });

      it('captures Sentry exception when CQ trackEvent fails but does not throw', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({ inApp: true });
        (cq.trackEvent as any).mockRejectedValueOnce(new Error('CQ down'));

        await expect(
          notify('user-a', 'COMMENT_REPLY', COMMENT_REPLY_PAYLOAD),
        ).resolves.toBeUndefined();

        expect(Sentry.captureException).toHaveBeenCalled();
      });

      it('does NOT trigger anti-self-notify for CONTENT_UPDATE (no actorUserId in payload)', async () => {
        // payload без actorUserId → 'actorUserId' in payload === false → не early return
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({ inApp: true });

        await notify('user-a', 'CONTENT_UPDATE', CONTENT_UPDATE_PAYLOAD);

        expect(prisma.notification.create).toHaveBeenCalledTimes(1);
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_content_update');
      });

      it('does NOT trigger anti-self-notify for INACTIVITY_RETURN (no actorUserId in payload)', async () => {
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({ inApp: true });

        await notify('user-a', 'INACTIVITY_RETURN', INACTIVITY_RETURN_PAYLOAD);

        expect(prisma.notification.create).toHaveBeenCalledTimes(1);
        expect(cq.trackEvent).toHaveBeenCalledWith('user-a', 'pa_notif_inactivity_return');
      });
    });

    describe('notifyCommentReply()', () => {
      it('resolves parent + lesson and calls notify with COMMENT_REPLY payload + ctaUrl', async () => {
        (prisma.lessonComment.findUnique as any).mockImplementation(({ where }: any) => {
          if (where.id === 'reply-1') {
            return Promise.resolve({
              id: 'reply-1',
              content: 'Hello back',
              parentId: 'parent-1',
              lessonId: 'lesson-1',
              user: { name: 'User B' },
            });
          }
          if (where.id === 'parent-1') {
            return Promise.resolve({ id: 'parent-1', userId: 'user-a' });
          }
          return Promise.resolve(null);
        });
        (prisma.lesson.findUnique as any).mockResolvedValue({ title: 'Урок про аналитику' });
        (prisma.notificationPreference.findUnique as any).mockResolvedValue({ inApp: true });

        await notifyCommentReply({ replyCommentId: 'reply-1', actorUserId: 'user-b' });

        expect(prisma.notification.create).toHaveBeenCalledWith({
          data: expect.objectContaining({
            userId: 'user-a',
            type: 'COMMENT_REPLY',
            ctaUrl: '/learn/lesson-1#comment-reply-1',
          }),
        });
      });

      it('skips when parent.userId === actorUserId (replied to self)', async () => {
        (prisma.lessonComment.findUnique as any).mockImplementation(({ where }: any) => {
          if (where.id === 'reply-1') {
            return Promise.resolve({
              id: 'reply-1',
              content: 'self reply',
              parentId: 'parent-1',
              lessonId: 'lesson-1',
              user: { name: 'User B' },
            });
          }
          if (where.id === 'parent-1') {
            return Promise.resolve({ id: 'parent-1', userId: 'user-b' }); // same as actor
          }
          return Promise.resolve(null);
        });

        await notifyCommentReply({ replyCommentId: 'reply-1', actorUserId: 'user-b' });

        expect(prisma.notification.create).not.toHaveBeenCalled();
        expect(cq.trackEvent).not.toHaveBeenCalled();
      });
    });
    ```

    **Подключение к Vitest конфигу** — если уже есть `vitest.config.ts` в `apps/web`, новый тест автоматически подхватится по glob. Если нет — настроить config (вероятно уже есть, проверить в репе).
  </action>

  <verify>
    <automated>cd MAAL && pnpm test --run apps/web/src/lib/notifications/__tests__/notify.test.ts</automated>
  </verify>

  <done>
    - Все 10 тестов PASS (8 для notify(), 2 для notifyCommentReply)
    - `pnpm typecheck` exits 0
    - `grep -c "it(" apps/web/src/lib/notifications/__tests__/notify.test.ts` returns >= 10
    - Тест-файл проверяет все SPEC scenarios (req 4 acceptance): inApp=true, inApp=false, anti-self-notify, CQ event always fires, broadcast types bypass anti-self-notify
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (route handler) → notify() | Caller предоставляет userId и payload — должны быть pre-validated, но notify() не должен падать на bad input |
| notify() → CQ | CQ failure не должен блокировать DB row creation |
| notify() → DB | DB write — single transaction; rollback не нужен (если CQ упал, row остаётся) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02-01 | S (Spoofing) | actorUserId in payload | mitigate | notifyCommentReply() читает actorUserId из server-trusted args (route handler verifies via Supabase auth → ctx.user.id, см. план 04). Caller НЕ может подделать |
| T-51-02-02 | I (Info disclosure) | preview content | accept | preview ограничен 120 символов; не содержит email/PII в стандартных кейсах; CQ event level — не публичный канал |
| T-51-02-03 | D (DoS) | notifyMany N users | mitigate | createMany одним запросом + sequential CQ — наша CQ-rate-limit (50/sec) защищает; Phase 53/54 могут добавить chunking |
| T-51-02-04 | T (Tampering) | Notification.payload Json | accept | Payload — server-controlled output, не user input; type-safe через discriminated union |
| T-51-02-05 | R (Repudiation) | Anti-self-notify bypass | mitigate | Two layers: (1) notifyCommentReply() проверяет parent.userId === actorUserId; (2) notify() проверяет payload.actorUserId === userId |
</threat_model>

<verification>
- `notify()` создаёт row только когда inApp=true (или default по типу)
- `notify()` всегда фаерит CQ event (даже если inApp=false)
- `notify()` не создаёт row и не фаерит event при self-notify (только для типов с actorUserId)
- `notify()` с broadcast-типами (CONTENT_UPDATE etc.) НЕ early-return'ит (anti-self-notify не применим)
- `notify()` ловит ошибки и не пробрасывает наверх
- `notifyCommentReply()` корректно резолвит зависимые сущности и формирует ctaUrl
- 10 unit-тестов покрывают все ветки
</verification>

<success_criteria>
- [ ] Файл `apps/web/src/lib/notifications/notify.ts` экспортирует `notify`, `notifyMany`, `notifyCommentReply`
- [ ] Все 10 unit-тестов в `__tests__/notify.test.ts` PASS
- [ ] `pnpm typecheck` exits 0
- [ ] Импорты в notify.ts: `prisma` из `@mpstats/db/client`, `cq` из `@/lib/carrotquest/client`, types из `@mpstats/shared`, `Sentry` из `@sentry/nextjs`
- [ ] notifyCommentReply резолвит parent + lesson + user без circular dependency
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-02-SUMMARY.md`:
- Module path + exports
- Test scenarios покрытые
- Артефакты для следующих волн (notify, notifyCommentReply — для route handler в плане 04; notify в общем — для Phases 52-54)
</output>
</content>
</invoke>