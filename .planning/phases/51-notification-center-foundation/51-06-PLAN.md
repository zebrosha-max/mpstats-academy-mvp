---
phase: 51-notification-center-foundation
plan: 06
type: execute
wave: 3
depends_on: [51-03]
files_modified:
  - apps/web/src/app/(main)/profile/notifications/page.tsx
  - apps/web/src/app/(main)/profile/page.tsx
  - apps/web/src/components/comments/CommentItem.tsx
  - apps/web/src/app/(main)/learn/[id]/page.tsx
  - apps/web/src/app/globals.css
autonomous: true
requirements: [REQ-51-09, REQ-51-10]

must_haves:
  truths:
    - "/profile/notifications показывает таблицу 7 типов x 2 канала (in-app, email)"
    - "Toggle in-app сохраняется через updatePreference mutation с optimistic update"
    - "Email Switch disabled (Скоро) на этой фазе"
    - "/profile содержит блок Уведомления со ссылкой Настроить -> /profile/notifications"
    - "CommentItem рендерит id=comment-${id} на root div c scroll-mt-20"
    - "/learn/[id] при URL hash #comment-X скроллит к div + добавляет класс notification-highlight на 1.5с"
    - "Fallback при удалённом комменте: silent no-scroll, без error"
  artifacts:
    - path: "apps/web/src/app/(main)/profile/notifications/page.tsx"
      provides: "Settings page для toggle preferences"
      exports: ["default page component"]
    - path: "apps/web/src/components/comments/CommentItem.tsx (modified)"
      provides: "id=comment-id + scroll-mt-20 на root div"
      contains: "comment-"
    - path: "apps/web/src/app/(main)/learn/[id]/page.tsx (modified)"
      provides: "useEffect anchor scroll + highlight"
      contains: "notification-highlight"
    - path: "apps/web/src/app/globals.css (modified)"
      provides: ".notification-highlight CSS class"
      contains: ".notification-highlight"
  key_links:
    - from: "/profile/notifications/page.tsx"
      to: "trpc.notifications.getPreferences + updatePreference"
      via: "useQuery + useMutation с optimistic update (onMutate cancel+setData → onError rollback → onSettled invalidate)"
    - from: "/learn/[id]/page.tsx useEffect"
      to: "DOM element id=comment-X"
      via: "document.getElementById + scrollIntoView + classList.add/remove"
---

<objective>
Вторая часть UI: страница `/profile/notifications` для toggle preferences (7 типов x 2 канала, email disabled до Phase 52+), интеграция ссылки из существующего `/profile`, deep-link инфраструктура (id-anchor на CommentItem + auto-scroll/highlight на /learn/[id]).

Purpose: Без preferences юзеры не могут отключить in-app для определённых типов. Без anchor scroll click на bell-item не приводит к подсвеченному коменту — теряется ценность COMMENT_REPLY.

Output: Settings page работает end-to-end. Click из bell на notification ведёт на /learn/lessonX#comment-Y и подсвечивает коммент на 1.5с.

**Note по объёму:** план содержит 5 tasks — это borderline scope, но REQ-51-09 (preferences) и REQ-51-10 (anchor scroll) тесно связаны на уровне общего UX flow click-в-bell → подсвеченный комментарий → решение «отключить тип». Разделение увеличивает context overhead для executor'а (нужно поднимать оба под-плана).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@apps/web/src/components/comments/CommentItem.tsx
@apps/web/src/app/(main)/learn/[id]/page.tsx

<interfaces>
From plan 03 tRPC router:
- trpc.notifications.getPreferences.useQuery() returns array of 7 prefs { userId, type, inApp, email }
- trpc.notifications.updatePreference.useMutation({ type, inApp?, email? })

Existing UI primitives:
- @/components/ui/switch (Switch from shadcn — radix wrapper)
- @/components/ui/card (Card)

Existing pattern (apps/web/src/app/(admin)/admin/settings/page.tsx):
const flags = trpc.admin.getFeatureFlags.useQuery(undefined, { retry: false });
const toggle = trpc.admin.toggleFeatureFlag.useMutation({ onSuccess: () => flags.refetch() });
<Switch checked={flag.enabled} onCheckedChange={() => toggle.mutate({ key: flag.key })} disabled={toggle.isPending} />

Existing CommentItem.tsx structure (around line 157+):
return (
  <div>
    <div className="flex gap-3">
      ... user avatar, name, content, replies ...
    </div>
  </div>
);

Existing pattern для hash-scroll (apps/web/src/app/(main)/learn/page.tsx:124-134):
useEffect(() => {
  const hash = window.location.hash.slice(1);
  if (hash && courses?.some((c) => c.id === hash)) {
    setTimeout(() => {
      document.getElementById(`course-${hash}`)?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }, 100);
  }
}, [courses]);

From shared package:
- NOTIFICATION_TYPE_DESCRIPTIONS: Record<string, string>  // Russian labels (D-16)
- ALL_NOTIFICATION_TYPES: readonly array — 7 types in order
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать /profile/notifications page с toggles (in-app working, email disabled)</name>
  <files>apps/web/src/app/(main)/profile/notifications/page.tsx</files>

  <read_first>
    - apps/web/src/app/(admin)/admin/settings/page.tsx (Switch + mutation pattern)
    - apps/web/src/components/ui/switch.tsx (для Switch API)
    - apps/web/src/components/ui/card.tsx (для Card)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 742-818
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-14, D-15, D-16
  </read_first>

  <action>
    Создать `apps/web/src/app/(main)/profile/notifications/page.tsx` со следующим полным содержимым:

    ```typescript
    'use client';

    import { trpc } from '@/lib/trpc/client';
    import { Card } from '@/components/ui/card';
    import { Switch } from '@/components/ui/switch';
    import { NOTIFICATION_TYPE_DESCRIPTIONS, type NotificationTypeName } from '@mpstats/shared';

    /**
     * Phase 51 — /profile/notifications
     *
     * Страница настройки in-app + email уведомлений по 7 типам.
     * Email Switch — hard-disabled на этой фазе (Phase 52+ запустит реальную доставку
     * через CQ-шаблоны, тогда disabled снимется per-type).
     *
     * Optimistic update: переключение Switch'а сразу применяется локально (setData),
     * при ошибке — rollback (setData previous), независимо от исхода — invalidate.
     */

    type NotifType = NotificationTypeName;

    /** Короткие русские названия (D-14 tone «ты»). NOTIFICATION_TYPE_DESCRIPTIONS — длинные подписи (D-16). */
    const TYPE_LABELS: Record<NotifType, { label: string; description: string }> = {
      COMMENT_REPLY: {
        label: 'Ответы на комментарии',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.COMMENT_REPLY,
      },
      ADMIN_COMMENT_REPLY: {
        label: 'Ответы методологов',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.ADMIN_COMMENT_REPLY,
      },
      CONTENT_UPDATE: {
        label: 'Новые уроки и материалы',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.CONTENT_UPDATE,
      },
      PROGRESS_NUDGE: {
        label: 'Напоминания о прогрессе',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.PROGRESS_NUDGE,
      },
      INACTIVITY_RETURN: {
        label: 'Возвращение после паузы',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.INACTIVITY_RETURN,
      },
      WEEKLY_DIGEST: {
        label: 'Недельный дайджест',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.WEEKLY_DIGEST,
      },
      BROADCAST: {
        label: 'Анонсы и новости',
        description: NOTIFICATION_TYPE_DESCRIPTIONS.BROADCAST,
      },
    };

    interface PreferenceRow {
      type: NotifType;
      inApp: boolean;
      email: boolean;
    }

    export default function NotificationsPreferencesPage() {
      const utils = trpc.useUtils();
      const { data: prefs, isLoading } = trpc.notifications.getPreferences.useQuery();

      const updatePreference = trpc.notifications.updatePreference.useMutation({
        // Optimistic update — мгновенный UX (D-14 tone «всё под контролем»)
        onMutate: async (variables) => {
          await utils.notifications.getPreferences.cancel();
          const previous = utils.notifications.getPreferences.getData();

          if (previous) {
            const next = previous.map((p: PreferenceRow) =>
              p.type === variables.type
                ? {
                    ...p,
                    ...(variables.inApp !== undefined && { inApp: variables.inApp }),
                    ...(variables.email !== undefined && { email: variables.email }),
                  }
                : p,
            );
            utils.notifications.getPreferences.setData(undefined, next);
          }

          return { previous };
        },
        onError: (_err, _vars, ctx) => {
          // Rollback при ошибке
          if (ctx?.previous) {
            utils.notifications.getPreferences.setData(undefined, ctx.previous);
          }
        },
        onSettled: () => {
          utils.notifications.getPreferences.invalidate();
        },
      });

      // Loading skeleton
      if (isLoading || !prefs) {
        return (
          <div className="max-w-2xl mx-auto px-4 py-8 space-y-6">
            <div className="h-8 w-2/3 bg-mp-gray-100 rounded animate-pulse" />
            <Card className="p-4 space-y-3">
              {[0, 1, 2, 3].map((i) => (
                <div key={i} className="h-16 bg-mp-gray-100 rounded animate-pulse" />
              ))}
            </Card>
          </div>
        );
      }

      return (
        <div className="max-w-2xl mx-auto px-4 py-8 space-y-6">
          <div>
            <h1 className="text-2xl font-bold text-mp-gray-900">
              Настрой, как хочешь получать уведомления.
            </h1>
            <p className="text-sm text-mp-gray-500 mt-1">
              Выбери, какие уведомления показывать и куда отправлять.
            </p>
          </div>

          <Card className="overflow-hidden">
            {/* Header row */}
            <div className="grid grid-cols-[1fr_auto_auto] gap-4 px-4 py-2 bg-mp-gray-50 text-[11px] uppercase tracking-wide text-mp-gray-500 font-semibold">
              <span>Тип</span>
              <span className="text-center w-16">In-app</span>
              <span className="text-center w-16">Email</span>
            </div>

            <div className="divide-y divide-mp-gray-100">
              {prefs.map((pref: PreferenceRow) => {
                const meta = TYPE_LABELS[pref.type] ?? {
                  label: pref.type,
                  description: '',
                };

                return (
                  <div
                    key={pref.type}
                    className="grid grid-cols-[1fr_auto_auto] gap-4 px-4 py-3 items-center"
                  >
                    {/* Колонка 1: Title + description */}
                    <div className="min-w-0">
                      <p className="text-sm font-semibold text-mp-gray-900">{meta.label}</p>
                      {meta.description && (
                        <p className="text-xs text-mp-gray-500 mt-0.5">{meta.description}</p>
                      )}
                    </div>

                    {/* Колонка 2: In-app toggle (живой) */}
                    <div className="flex justify-center w-16">
                      <Switch
                        checked={pref.inApp}
                        onCheckedChange={(value) =>
                          updatePreference.mutate({ type: pref.type, inApp: value })
                        }
                        disabled={updatePreference.isPending}
                        aria-label={`В приложении: ${meta.label}`}
                      />
                    </div>

                    {/* Колонка 3: Email toggle (hard-disabled на Phase 51) */}
                    <div className="flex justify-center w-16" title="Скоро">
                      <Switch
                        checked={pref.email}
                        disabled
                        aria-label={`По email: ${meta.label} (скоро)`}
                      />
                    </div>
                  </div>
                );
              })}
            </div>
          </Card>

          <p className="text-xs text-mp-gray-400">
            Email-уведомления появятся скоро — мы работаем над шаблонами.
          </p>
        </div>
      );
    }
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - `'use client'` — обязательно, используем хуки
    - Optimistic update через `onMutate` cancel + getData snapshot + setData; `onError` rollback; `onSettled` invalidate. **НЕ** use `onSuccess: refetch` (rolled out в Phase 49 как лучший pattern для UX)
    - Email Switch обёрнут в `<div title="Скоро">` для нативного HTML tooltip + `disabled` prop на Switch — нельзя toggle (D-15 «честно говорим, когда что-то будет позже»)
    - aria-label на каждом Switch для screen reader'ов (REQ accessibility)
    - TYPE_LABELS использует `NOTIFICATION_TYPE_DESCRIPTIONS` из shared — единый источник правды для длинных описаний (D-16)
    - НЕ хардкодить `inApp: true` для PreferenceRow — getPreferences возвращает фактические значения из БД (с дефолтами из DEFAULT_IN_APP_PREFS если row отсутствует)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck</automated>
  </verify>

  <done>
    - Файл `apps/web/src/app/(main)/profile/notifications/page.tsx` существует
    - `grep -n "Настрой, как хочешь" apps/web/src/app/\(main\)/profile/notifications/page.tsx` находит header
    - `grep -n "title=\"Скоро\"" apps/web/src/app/\(main\)/profile/notifications/page.tsx` находит email tooltip
    - `grep -n "NOTIFICATION_TYPE_DESCRIPTIONS" apps/web/src/app/\(main\)/profile/notifications/page.tsx` находит import + use
    - `grep -n "onMutate\|onError\|onSettled" apps/web/src/app/\(main\)/profile/notifications/page.tsx` находит все 3 (optimistic pattern)
    - `grep -c "aria-label" apps/web/src/app/\(main\)/profile/notifications/page.tsx` returns >= 14 (7 типов × 2 канала)
    - `pnpm typecheck` exits 0
    - Manual smoke check: открытие /profile/notifications в dev → видна таблица 7 типов; toggle in-app COMMENT_REPLY → optimistic update; reload → состояние сохранено
  </done>
</task>

<task type="auto">
  <name>Task 2: Добавить блок Уведомления со ссылкой Настроить в /profile/page.tsx</name>
  <files>apps/web/src/app/(main)/profile/page.tsx</files>

  <read_first>
    - apps/web/src/app/(main)/profile/page.tsx (целиком — найти место для нового блока, обычно ниже существующих секций)
  </read_first>

  <action>
    В `apps/web/src/app/(main)/profile/page.tsx` добавить новый блок "Уведомления" после последней существующей секции (например, после блока подписки/billing, либо после "Обо мне"):

    ```tsx
    {/* Phase 51 — link to /profile/notifications */}
    <Card className="p-6">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h2 className="text-lg font-semibold text-mp-gray-900">Уведомления</h2>
          <p className="text-sm text-mp-gray-500 mt-1">
            Выбери, какие уведомления получать и куда отправлять.
          </p>
        </div>
        <Link
          href="/profile/notifications"
          className="text-sm text-mp-blue-600 hover:underline whitespace-nowrap"
        >
          Настроить →
        </Link>
      </div>
    </Card>
    ```

    Если в файле уже импортирован Link и Card — переиспользовать. Если нет — добавить:
    ```typescript
    import Link from 'next/link';
    import { Card } from '@/components/ui/card';
    ```

    КРИТИЧЕСКИ ВАЖНО: НЕ менять существующие секции (имя, email, подписка, billing). Только добавить НОВЫЙ блок.

    Если /profile использует не Card, а другой контейнер (div с borders) — следовать тому же паттерну.
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck</automated>
  </verify>

  <done>
    - grep `/profile/notifications` в profile/page.tsx находит ссылку
    - grep `Настроить` находит CTA
    - Существующие секции профиля сохранены
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Добавить id и scroll-mt-20 на root div CommentItem.tsx</name>
  <files>apps/web/src/components/comments/CommentItem.tsx</files>

  <read_first>
    - apps/web/src/components/comments/CommentItem.tsx (целиком — найти точно строки где открывается root div в return)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1020-1042
  </read_first>

  <action>
    В `apps/web/src/components/comments/CommentItem.tsx`:

    Найти outermost root `<div>` в return statement (около строк 157-160). Текущий вид:
    ```tsx
    return (
      <div>
        <div className="flex gap-3">
        ...
    ```

    Заменить на:
    ```tsx
    return (
      <div id={`comment-${comment.id}`} className="scroll-mt-20">
        <div className="flex gap-3">
        ...
    ```

    Объяснение:
    - `scroll-mt-20` = scroll-margin-top: 5rem (~80px), компенсирует sticky header (h-16 = 4rem) при scrollIntoView с block:start. При block:center менее критично, но не повредит
    - `id=comment-${comment.id}` обеспечивает корректный target для anchor scroll

    Replies рекурсивно используют `<CommentItem comment={reply} />` — id для reply придёт автоматически.

    Если у root div уже есть className — объединить: className="scroll-mt-20 [existing classes]". Если был только children без className — просто добавить.

    НИЧЕГО ДРУГОГО не менять. Не трогать avatar, name, content, reply button, edit/delete, formatRelativeTime export.
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck</automated>
  </verify>

  <done>
    - grep на template literal с comment- находит match в CommentItem.tsx
    - grep `scroll-mt-20` находит class
    - existing reply-mapping логика не сломана
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 4: Добавить useEffect anchor scroll + notification-highlight в /learn/[id]/page.tsx</name>
  <files>apps/web/src/app/(main)/learn/[id]/page.tsx</files>

  <read_first>
    - apps/web/src/app/(main)/learn/[id]/page.tsx (целиком — найти existing useEffects, добавить НОВЫЙ после)
    - apps/web/src/app/(main)/learn/page.tsx строки 124-134 (existing hash-scroll pattern в проекте)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 1045-1100
  </read_first>

  <action>
    В `apps/web/src/app/(main)/learn/[id]/page.tsx` добавить НОВЫЙ useEffect (после существующих, обычно после Yandex Metrika tracking effect):

    ```typescript
    // Phase 51 — anchor scroll к комменту с highlight (D-11, D-12)
    useEffect(() => {
      if (typeof window === 'undefined') return;
      const hash = window.location.hash.slice(1); // 'comment-cuid123'
      if (!hash.startsWith('comment-')) return;

      // Comments load async via tRPC — retry до 5 раз с интервалом 300ms
      const tryHighlight = (): boolean => {
        const el = document.getElementById(hash);
        if (!el) return false;
        el.scrollIntoView({ behavior: 'smooth', block: 'center' });
        el.classList.add('notification-highlight');
        setTimeout(() => el.classList.remove('notification-highlight'), 1500);
        return true;
      };

      if (tryHighlight()) return;
      let attempts = 0;
      const interval = setInterval(() => {
        attempts++;
        if (tryHighlight() || attempts >= 5) clearInterval(interval);
      }, 300);
      return () => clearInterval(interval);
    }, [data?.lesson?.id]); // Re-run when lesson loads
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - dependency `[data?.lesson?.id]` — useEffect должен срабатывать после загрузки урока
    - retry-логика обязательна: comments fetched через tRPC уже после mount, target div появляется чуть позже
    - max 5 attempts × 300ms = 1.5s window
    - `block: 'center'` — целевой коммент в центре viewport
    - D-13 fallback: если el не найден после 5 попыток — silent no-op (interval cleared, ничего не сломано)

    Не модифицировать другие useEffect или fetch logic.

    NOTE: реальный variable name для loaded lesson может быть не `data?.lesson?.id` — проверить в файле и адаптировать (возможно `lesson?.id` или `lessonData?.id`). Главное — re-run effect когда lesson грузится.
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck</automated>
  </verify>

  <done>
    - grep `notification-highlight` в learn/[id]/page.tsx находит >= 2 occurrences (add + remove)
    - grep `scrollIntoView` находит call с behavior: smooth и block: center
    - grep `attempts >= 5` находит retry guard
    - existing useEffects не удалены
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 5: Добавить .notification-highlight CSS class в globals.css</name>
  <files>apps/web/src/app/globals.css</files>

  <read_first>
    - apps/web/src/app/globals.css (целиком — найти куда добавить, обычно после @layer base/components, либо в конец файла)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md DC-04 (CSS approach)
  </read_first>

  <action>
    Добавить в `apps/web/src/app/globals.css` (в конец файла, либо в @layer components если такой layer существует):

    ```css
    /* Phase 51 — notification deep-link highlight (D-11, D-12) */
    .notification-highlight {
      background-color: rgb(239 246 255 / 1); /* Tailwind blue-50 / mp-blue-50 */
      transition: background-color 1500ms ease-out;
    }
    ```

    Объяснение:
    - RGB `rgb(239 246 255)` соответствует Tailwind `blue-50` — fallback если custom mp-blue-50 не определён глобально как CSS variable
    - Transition 1500ms — окно во время которого useEffect задерживает класс перед removeClass
    - При remove'е background вернётся к стандартному (наследуется), 1.5s transition fade out — visual smooth fade

    Альтернатива через @apply (если проект Tailwind v4 поддерживает):
    ```css
    .notification-highlight {
      @apply bg-mp-blue-50;
      transition: background-color 1500ms ease-out;
    }
    ```

    Выбрать тот вариант, который согласуется с другими custom CSS classes в globals.css. Если их нет — использовать прямой RGB.
  </action>

  <verify>
    <automated>cd MAAL && grep -n "notification-highlight" apps/web/src/app/globals.css</automated>
  </verify>

  <done>
    - grep `notification-highlight` в globals.css находит class definition
    - grep `transition.*background-color` находит transition rule
    - Existing CSS не удалён (file size grew, не сжался)
    - `pnpm build` (smoke check) exits 0 — Tailwind не упадёт на новом custom class
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → tRPC updatePreference | Юзер может toggle ТОЛЬКО свои preferences (router enforces userId=ctx.user.id) |
| URL hash → DOM lookup | hash под контролем юзера, но `getElementById(hash)` безопасен — нет XSS вектора |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-06-01 | E (Elevation) | updatePreference чужого юзера | mitigate | Plan 03: upsert WHERE userId=ctx.user.id (нельзя targeting'ом) |
| T-51-06-02 | I (Info disclosure) | URL hash раскрывает comment id | accept | Comment IDs — cuid, безопасно публиковать; access к коменту проверяется через lesson access (lock на /learn page) |
| T-51-06-03 | T (Tampering) | Малишес hash вроде #comment-<script> | mitigate | hash startsWith('comment-') filter + getElementById возвращает null если такого id нет → silent fallback |
| T-51-06-04 | I (Info disclosure) | Highlight на закрытом коменте | mitigate | Комменты к locked-урокам не рендерятся (CommentSection не маунтится) → нечего highlight'ить → silent no-op (D-13) |
| T-51-06-05 | D (DoS) | retry interval 5 attempts × 300ms на бесконечном hash | mitigate | Hard guard `attempts >= 5` → stop interval; total wall time = 1.5s max |
</threat_model>

<verification>
- /profile/notifications работает end-to-end: toggle in-app персистится, email disabled с tooltip
- /profile содержит ссылку на новую страницу
- CommentItem рендерит id-anchor на каждом коменте/реплае
- /learn/[id] подсвечивает целевой коммент при наличии URL hash
- Removal класса notification-highlight через 1.5s
- Silent fallback при отсутствии target element
</verification>

<success_criteria>
- [ ] /profile/notifications работает с optimistic update toggles
- [ ] Email Switch на странице disabled с title="Скоро"
- [ ] /profile содержит блок "Уведомления" с CTA на /profile/notifications
- [ ] CommentItem.tsx root div имеет id={comment-${id}} и scroll-mt-20
- [ ] /learn/[id] useEffect делает scrollIntoView + classList toggle при URL hash
- [ ] globals.css содержит .notification-highlight с 1500ms transition
- [ ] `pnpm typecheck` + `pnpm build` exit 0
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-06-SUMMARY.md`:
- /profile/notifications + /profile/page link готовы
- Anchor scroll + highlight инфраструктура работает
- Артефакты для плана 07: end-to-end flow готов для Playwright E2E (reply → bell → click → /learn/[id]#comment-X → highlight)
- Известные ограничения: email Switch'и hard-disabled на этой фазе; per-type включение через follow-up миграцию когда CQ-шаблоны готовы (Phase 52+)
</output>
</content>
</invoke>