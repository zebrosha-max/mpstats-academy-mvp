---
phase: 51-notification-center-foundation
plan: 07
type: execute
wave: 4
depends_on: [51-04, 51-05, 51-06]
files_modified:
  - apps/web/src/app/api/cron/notifications-cleanup/route.ts
  - .github/workflows/notifications-cleanup.yml
  - apps/web/tests/e2e/notifications.spec.ts
autonomous: true
requirements: [REQ-51-11]

must_haves:
  truths:
    - "POST/GET /api/cron/notifications-cleanup защищён CRON_SECRET Bearer auth"
    - "Cleanup удаляет Notification rows старше 90 дней"
    - "Cleanup ограничивает каждого юзера до 500 rows (top 500 по createdAt DESC, остальные DELETE)"
    - "Sentry checkin slug=notifications-cleanup, margin 180 минут"
    - ".github/workflows/notifications-cleanup.yml schedule 0 0 * * * (00:00 UTC = 03:00 МСК)"
    - "Playwright E2E проходит полный flow: A reply → B sees badge → click → markRead → highlight"
    - "GitHub Actions secrets TEST_USER_A_EMAIL/PASSWORD, TEST_USER_B_EMAIL/PASSWORD, TEST_LESSON_ID существуют — phase NOT shipped без них"
  artifacts:
    - path: "apps/web/src/app/api/cron/notifications-cleanup/route.ts"
      provides: "GET + POST handlers с Sentry checkin + 90-day delete + 500-cap"
      exports: ["GET", "POST"]
    - path: ".github/workflows/notifications-cleanup.yml"
      provides: "Daily cron 00:00 UTC, curl с CRON_SECRET"
    - path: "apps/web/tests/e2e/notifications.spec.ts"
      provides: "Playwright E2E reply→bell→read flow"
  key_links:
    - from: ".github/workflows/notifications-cleanup.yml"
      to: "/api/cron/notifications-cleanup"
      via: "curl с Bearer CRON_SECRET"
    - from: "/api/cron/notifications-cleanup"
      to: "Sentry monitor 'notifications-cleanup'"
      via: "captureCheckIn (margin 180)"
---

<objective>
Финальная волна: cron-cleanup для retention (90 дней + 500/user cap) + GitHub Actions workflow + Playwright E2E смонтированный full-flow тест. Plus финальная проверка деплой-готовности.

Purpose: Без cleanup'а БД растёт безгранично (500-1000 active users × notifications/неделю); SPEC req 11 требует ежедневный cron. E2E тест — единственная гарантия что все 6 предыдущих волн соединены корректно (роут + DB + service + bell polling + click handler + markRead).

Output: Cron работает (Sentry monitor visible), E2E PASS, smoke check всего phase end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@apps/web/src/app/api/cron/orphan-materials/route.ts
@.github/workflows/orphan-materials-cleanup.yml
@apps/web/tests/e2e/lesson-materials.spec.ts
@apps/web/src/components/comments/CommentInput.tsx

<interfaces>
Existing cron pattern (apps/web/src/app/api/cron/orphan-materials/route.ts:30-153):
```typescript
export const dynamic = 'force-dynamic';

async function handle(request: NextRequest) {
  const auth = request.headers.get('authorization');
  if (!process.env.CRON_SECRET || auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  const checkInId = Sentry.captureCheckIn(
    { monitorSlug: 'orphan-materials', status: 'in_progress' },
    { schedule: { type: 'crontab', value: '0 3 * * *' }, checkinMargin: 180, maxRuntime: 30, timezone: 'UTC' },
  );

  try {
    // ... cleanup logic ...
    Sentry.captureCheckIn({ checkInId, monitorSlug: 'orphan-materials', status: 'ok' });
    return NextResponse.json({ ok: true, ... });
  } catch (error) {
    Sentry.captureException(error);
    Sentry.captureCheckIn({ checkInId, monitorSlug: 'orphan-materials', status: 'error' });
    return NextResponse.json({ error: ... }, { status: 500 });
  }
}

export async function GET(request) { return handle(request); }
export async function POST(request) { return handle(request); }
```

Existing GitHub Actions workflow pattern (.github/workflows/orphan-materials-cleanup.yml):
```yaml
name: Orphan Materials Cleanup
on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch: {}
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -fsSL --max-time 600 \
            -H "Authorization: Bearer $CRON_SECRET" \
            "$SITE_URL/api/cron/orphan-materials"
        env:
          SITE_URL: ${{ secrets.SITE_URL }}
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
```

<!-- VERIFIED — apps/web/tests/e2e/lesson-materials.spec.ts pattern (Phase 49 reference) -->
```typescript
import { test, expect } from '@playwright/test';

const TEST_EMAIL = 'tester@mpstats.academy';
const TEST_PASSWORD = process.env.TEST_USER_PASSWORD || 'TestUser2024';

async function login(page, email, password) {
  await page.goto('/login');
  await page.fill('input[type="email"]', email);
  await page.fill('input[type="password"]', password);
  await page.click('button[type="submit"]');
  await page.waitForURL(/\/(dashboard|learn|diagnostic)/, { timeout: 15000 });
}

test('subscriber sees materials section', async ({ page }) => {
  const lessonId = process.env.TEST_LESSON_WITH_MATERIALS;
  test.skip(!lessonId, '...');
  await login(page, TEST_EMAIL, TEST_PASSWORD);
  await page.goto(`/learn/${lessonId}`);
  // ...
});
```
**Шаблон для Phase 51:** переиспользуем `login` helper, скопировав его в новый spec.

<!-- VERIFIED — apps/web/src/components/comments/CommentInput.tsx (line 146 — placeholder) -->
```jsx
placeholder={parentId ? 'Напишите ответ...' : 'Напишите комментарий...'}
```
**Селекторы для E2E:**
- Textarea для root comment: `getByPlaceholder('Напишите комментарий...')`
- Textarea для reply: `getByPlaceholder('Напишите ответ...')`
- Submit button: `getByRole('button', { name: 'Отправить' })` (CommentInput.tsx:172, текст «Отправить»)
- Reply trigger button label: проверь в `CommentItem.tsx` — Phase 12 spec говорит «Ответить»
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать /api/cron/notifications-cleanup route с 90-day delete + 500-cap + Sentry checkin</name>
  <files>apps/web/src/app/api/cron/notifications-cleanup/route.ts</files>

  <read_first>
    - apps/web/src/app/api/cron/orphan-materials/route.ts (целиком — паттерн 1:1 для нового cron)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 822-911
    - .planning/phases/51-notification-center-foundation/51-SPEC.md req 11
    - .claude/memory/feedback_cron_false_positive.md если есть (либо MAAL-PLATFORM-1 учёт margin 180)
  </read_first>

  <action>
    Создать `apps/web/src/app/api/cron/notifications-cleanup/route.ts`:

    ```typescript
    import { NextRequest, NextResponse } from 'next/server';
    import * as Sentry from '@sentry/nextjs';
    import { prisma } from '@mpstats/db/client';

    export const dynamic = 'force-dynamic';

    /**
     * Cron: Cleanup Notification rows per retention policy (Phase 51 req 11).
     *
     * Strategy:
     *  - Delete rows older than 90 days
     *  - For users with > 500 rows: keep top 500 by createdAt DESC, delete rest
     *
     * Auth: CRON_SECRET Bearer header.
     * Schedule: GitHub Action 00:00 UTC daily (03:00 МСК).
     * Sentry checkin slug: 'notifications-cleanup', margin 180 min (GitHub schedules drift up to 60-100 min).
     */

    const RETENTION_DAYS = 90;
    const PER_USER_CAP = 500;

    async function handle(request: NextRequest) {
      const auth = request.headers.get('authorization');
      if (!process.env.CRON_SECRET || auth !== `Bearer ${process.env.CRON_SECRET}`) {
        return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
      }

      const checkInId = Sentry.captureCheckIn(
        {
          monitorSlug: 'notifications-cleanup',
          status: 'in_progress',
        },
        {
          schedule: { type: 'crontab', value: '0 0 * * *' }, // 00:00 UTC = 03:00 МСК
          checkinMargin: 180,
          maxRuntime: 30,
          timezone: 'UTC',
        },
      );

      try {
        // 1) Delete rows older than RETENTION_DAYS days
        const cutoff = new Date();
        cutoff.setDate(cutoff.getDate() - RETENTION_DAYS);

        const oldDeleted = await prisma.notification.deleteMany({
          where: { createdAt: { lt: cutoff } },
        });

        // 2) Hard cap PER_USER_CAP rows per user — delete extras (raw SQL для эффективности)
        const overflowDeleted: number = await prisma.$executeRaw`
          DELETE FROM "Notification"
          WHERE id IN (
            SELECT id FROM (
              SELECT id, ROW_NUMBER() OVER (PARTITION BY "userId" ORDER BY "createdAt" DESC) AS rn
              FROM "Notification"
            ) ranked
            WHERE rn > ${PER_USER_CAP}
          )
        `;

        console.log(
          `[notifications-cleanup] retention=${oldDeleted.count} overflow=${overflowDeleted}`,
        );

        Sentry.captureCheckIn({
          checkInId,
          monitorSlug: 'notifications-cleanup',
          status: 'ok',
        });

        return NextResponse.json({
          ok: true,
          cleaned: { retention: oldDeleted.count, overflow: overflowDeleted },
        });
      } catch (error) {
        Sentry.captureException(error, {
          tags: { area: 'cron', stage: 'notifications-cleanup' },
        });
        Sentry.captureCheckIn({
          checkInId,
          monitorSlug: 'notifications-cleanup',
          status: 'error',
        });
        console.error('[notifications-cleanup] error:', error);
        return NextResponse.json(
          { error: error instanceof Error ? error.message : 'Internal error' },
          { status: 500 },
        );
      }
    }

    export async function GET(request: NextRequest) {
      return handle(request);
    }

    export async function POST(request: NextRequest) {
      return handle(request);
    }
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - `checkinMargin: 180` — обязательно (recurring lesson MAAL-PLATFORM-1; GitHub schedules drift)
    - `crontab '0 0 * * *'` = 00:00 UTC = 03:00 МСК (per SPEC req 11)
    - `$executeRaw` для overflow delete — Prisma не поддерживает window functions напрямую через query API; raw SQL необходим
    - Имя таблицы в raw SQL — `"Notification"` (с capital N в кавычках, Postgres case-sensitive)
    - GET и POST handlers — оба нужны (workflow_dispatch + scheduled run могут использовать разные методы)
    - Идемпотентность: повторный запуск через 1 минуту → 0 удалений (всё уже подчищено)
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && grep -n "notifications-cleanup\|monitorSlug" apps/web/src/app/api/cron/notifications-cleanup/route.ts</automated>
  </verify>

  <done>
    - Файл существует
    - grep `monitorSlug.*notifications-cleanup` находит >= 3 occurrences (in_progress, ok, error)
    - grep `checkinMargin: 180` находит margin
    - grep `RETENTION_DAYS = 90` + `PER_USER_CAP = 500` находят constants
    - grep `Bearer.*CRON_SECRET` находит auth check
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать .github/workflows/notifications-cleanup.yml</name>
  <files>.github/workflows/notifications-cleanup.yml</files>

  <read_first>
    - .github/workflows/orphan-materials-cleanup.yml (паттерн 1:1)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 915-938
  </read_first>

  <action>
    Создать `.github/workflows/notifications-cleanup.yml`:

    ```yaml
    name: Notifications Cleanup
    on:
      schedule:
        - cron: '0 0 * * *' # 00:00 UTC daily = 03:00 МСК
      workflow_dispatch: {}

    jobs:
      cleanup:
        runs-on: ubuntu-latest
        steps:
          - name: Trigger notifications-cleanup cron endpoint
            run: |
              curl -fsSL --max-time 600 \
                -H "Authorization: Bearer $CRON_SECRET" \
                "$SITE_URL/api/cron/notifications-cleanup" \
                || echo "Warning: notifications-cleanup failed"
            env:
              SITE_URL: ${{ secrets.SITE_URL }}
              CRON_SECRET: ${{ secrets.CRON_SECRET }}
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - cron expression `0 0 * * *` — 00:00 UTC = 03:00 МСК (НЕ 0 3 * * * — это 06:00 МСК)
    - workflow_dispatch enabled — для manual smoke testing после деплоя
    - Не fail action при curl failure (`|| echo`) — Sentry checkin alert надёжнее GitHub Actions exit code (false-positive lesson MAAL-PLATFORM-1)
    - Secrets `SITE_URL` и `CRON_SECRET` уже настроены в GitHub repo (используются другими cron'ами)
  </action>

  <verify>
    <automated>cd MAAL && grep -n "0 0 \* \* \*\|workflow_dispatch" .github/workflows/notifications-cleanup.yml</automated>
  </verify>

  <done>
    - Файл `.github/workflows/notifications-cleanup.yml` существует
    - grep `cron: '0 0 \* \* \*'` находит schedule
    - grep `workflow_dispatch` находит manual trigger
    - grep `CRON_SECRET` находит env var
    - YAML валиден (можно проверить через `yq` или GitHub Actions linter)
  </done>
</task>

<task type="auto">
  <name>Task 3: Playwright E2E тест полного flow reply→bell→click→markRead</name>
  <files>apps/web/tests/e2e/notifications.spec.ts</files>

  <read_first>
    - apps/web/tests/e2e/lesson-materials.spec.ts (Phase 49 reference — login helper pattern + env-var gate pattern)
    - apps/web/tests/e2e/auth-flow.spec.ts (login form selectors — `input[type="email"]`, `input[type="password"]`, `button[type="submit"]`)
    - apps/web/playwright.config.ts (для baseURL и project setup)
    - apps/web/src/components/comments/CommentInput.tsx (verified placeholders: «Напишите комментарий...» / «Напишите ответ...», submit button «Отправить» — CommentInput.tsx:146,172)
    - apps/web/src/components/comments/CommentItem.tsx (Reply button label)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md DC-05
    - .planning/phases/51-notification-center-foundation/51-SPEC.md req 7 acceptance (E2E test scenario)
  </read_first>

  <action>
    Создать `apps/web/tests/e2e/notifications.spec.ts`. Использовать те же селекторы и login pattern что в `lesson-materials.spec.ts`:

    ```typescript
    import { test, expect, type Page } from '@playwright/test';

    /**
     * Phase 51 — Notification Center E2E
     *
     * Полный flow:
     * 1. Юзер A создаёт root коммент на уроке X
     * 2. Юзер B логинится, открывает урок X, отвечает на коммент A
     * 3. Юзер A логинится, ждёт badge (≤90s polling tolerance)
     * 4. Открывает dropdown → видит уведомление от B
     * 5. Click на item → переход на /learn/X#comment-Y → highlight visible
     * 6. После reload bell badge = 0 (читал)
     *
     * Required env vars (set in GitHub Actions secrets / .env.test):
     *   - TEST_USER_A_EMAIL              email юзера A (получатель уведомления)
     *   - TEST_USER_A_PASSWORD           пароль юзера A
     *   - TEST_USER_B_EMAIL              email юзера B (отвечающий)
     *   - TEST_USER_B_PASSWORD           пароль юзера B
     *   - TEST_LESSON_ID                 ID free-tier урока, доступного обоим юзерам
     *
     * **Phase 51 NOT shipped until все 5 secrets выставлены** (см. acceptance criteria SPEC req 7).
     * Если secrets отсутствуют — тест явно падает с инструкцией, НЕ skip'ает.
     */

    const USER_A_EMAIL = process.env.TEST_USER_A_EMAIL;
    const USER_A_PASSWORD = process.env.TEST_USER_A_PASSWORD;
    const USER_B_EMAIL = process.env.TEST_USER_B_EMAIL;
    const USER_B_PASSWORD = process.env.TEST_USER_B_PASSWORD;
    const TEST_LESSON_ID = process.env.TEST_LESSON_ID;

    /** Пере-используемый login helper (тот же pattern что в lesson-materials.spec.ts:26-32) */
    async function login(page: Page, email: string, password: string) {
      await page.goto('/login');
      await page.fill('input[type="email"]', email);
      await page.fill('input[type="password"]', password);
      await page.click('button[type="submit"]');
      await page.waitForURL(/\/(dashboard|learn|diagnostic)/, { timeout: 15000 });
    }

    test.describe('Phase 51 — Notification Center COMMENT_REPLY flow', () => {
      test.beforeAll(() => {
        // Explicit fail (NOT skip) — phase NOT shipped without these secrets
        const missing: string[] = [];
        if (!USER_A_EMAIL) missing.push('TEST_USER_A_EMAIL');
        if (!USER_A_PASSWORD) missing.push('TEST_USER_A_PASSWORD');
        if (!USER_B_EMAIL) missing.push('TEST_USER_B_EMAIL');
        if (!USER_B_PASSWORD) missing.push('TEST_USER_B_PASSWORD');
        if (!TEST_LESSON_ID) missing.push('TEST_LESSON_ID');
        if (missing.length > 0) {
          throw new Error(
            `Missing GitHub Actions secrets: ${missing.join(', ')}. ` +
              `Set via gh secret set <NAME> или Settings → Secrets and variables → Actions. ` +
              `Phase 51 NOT shipped until all 5 secrets configured.`,
          );
        }
      });

      test('reply → bell badge → click → markRead end-to-end', async ({ browser }) => {
        // Setup: два независимых browser context'а (clean cookies)
        const ctxA = await browser.newContext();
        const ctxB = await browser.newContext();
        const pageA = await ctxA.newPage();
        const pageB = await ctxB.newPage();

        try {
          // 1. Юзер A — login + create root comment
          await login(pageA, USER_A_EMAIL!, USER_A_PASSWORD!);
          await pageA.goto(`/learn/${TEST_LESSON_ID}`);

          const commentText = `e2e test root ${Date.now()}`;
          // VERIFIED selectors (CommentInput.tsx:146,172)
          await pageA.getByPlaceholder('Напишите комментарий...').fill(commentText);
          await pageA.getByRole('button', { name: 'Отправить' }).click();
          await expect(pageA.getByText(commentText)).toBeVisible({ timeout: 5000 });

          // 2. Юзер B — login и reply на коммент A
          await login(pageB, USER_B_EMAIL!, USER_B_PASSWORD!);
          await pageB.goto(`/learn/${TEST_LESSON_ID}`);

          // CommentItem root div имеет id="comment-<cuid>" (Phase 51 plan 06 Task 3)
          // Find comment A через текст и нажми "Ответить" в его контексте
          const commentAItem = pageB
            .locator('[id^="comment-"]')
            .filter({ hasText: commentText })
            .first();
          await commentAItem.getByRole('button', { name: /Ответить/ }).click();

          const replyText = `e2e reply ${Date.now()}`;
          await pageB.getByPlaceholder('Напишите ответ...').fill(replyText);
          await pageB.getByRole('button', { name: 'Отправить' }).click();
          await expect(pageB.getByText(replyText)).toBeVisible({ timeout: 5000 });

          // 3. Юзер A — открыть любую (main)/* страницу и ждать badge ≤ 90с
          // (polling 60c + первый refetch может произойти при mount instantly)
          await pageA.goto('/dashboard');
          const bellButton = pageA.getByRole('button', { name: 'Уведомления' });
          await expect(bellButton).toBeVisible();
          // Badge — span внутри bellButton (NotificationBell.tsx:382-389)
          const bellBadge = bellButton.locator('span').first();
          await expect(bellBadge).toContainText(/[1-9]/, { timeout: 90_000 });

          // 4. Click на bell → dropdown открывается → click первое item
          await bellButton.click();
          // Popover content виден (Radix popper-content)
          const dropdown = pageA.locator('[data-radix-popper-content-wrapper]').first();
          await expect(dropdown).toBeVisible({ timeout: 5000 });

          // Первое уведомление — Link c ctaUrl (NotificationItem.tsx если ctaUrl: <Link>)
          const firstNotif = dropdown.locator('a').first();
          await expect(firstNotif).toContainText(replyText.slice(0, 50));
          await firstNotif.click();

          // 5. Должен быть переход на /learn/X#comment-Y (ctaUrl format из notify.ts)
          await pageA.waitForURL(/\/learn\/.*#comment-/);
          const url = pageA.url();
          expect(url).toMatch(/#comment-/);

          // 6. Reload — bell badge должен исчезнуть (markRead сработал при click)
          await pageA.reload();
          const badgeAfter = pageA.getByRole('button', { name: 'Уведомления' }).locator('span').first();
          await expect(badgeAfter).not.toBeVisible({ timeout: 30_000 });
        } finally {
          await ctxA.close();
          await ctxB.close();
        }
      });
    });
    ```

    КРИТИЧЕСКИ ВАЖНО:
    - **Explicit fail в `test.beforeAll`** (НЕ `test.skip`) — phase ne shipped без secrets. Это enforced acceptance criterion (SPEC req 7).
    - Селекторы verified против реального CommentInput.tsx и lesson-materials.spec.ts pattern
    - 90с timeout для badge — соответствует SPEC requirement (polling 60c + первый refetch instant + render)
    - URL hash check (`/#comment-/`) — гарантирует что ctaUrl сформирован корректно через notify.ts → ctaUrl pattern `/learn/X#comment-Y`
    - `try { ... } finally { ctxA.close(); ctxB.close(); }` — гарантирует cleanup даже при fail
    - Используется `getByRole('button', { name: 'Уведомления' })` — соответствует `aria-label="Уведомления"` в NotificationBell.tsx:377
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck && ls apps/web/tests/e2e/notifications.spec.ts</automated>
  </verify>

  <done>
    - Файл `apps/web/tests/e2e/notifications.spec.ts` существует
    - `grep -n "test.beforeAll\|throw new Error" apps/web/tests/e2e/notifications.spec.ts` находит explicit fail (НЕ test.skip)
    - `grep -n "Phase 51 NOT shipped" apps/web/tests/e2e/notifications.spec.ts` находит сообщение об ошибке
    - `grep -n "Напишите комментарий\|Напишите ответ\|Отправить" apps/web/tests/e2e/notifications.spec.ts` находит verified селекторы (минимум 3)
    - 90s timeout на badge appearance
    - URL hash check на /learn/X#comment-Y
    - `pnpm typecheck` exits 0
    - Manual run (с настроенными env-vars): `pnpm test:e2e --grep "COMMENT_REPLY flow"` → PASS
    - GitHub Actions secrets выставлены: `gh secret list` показывает `TEST_USER_A_EMAIL`, `TEST_USER_A_PASSWORD`, `TEST_USER_B_EMAIL`, `TEST_USER_B_PASSWORD`, `TEST_LESSON_ID`
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Финальная manual проверка end-to-end на staging/dev</name>

  <what-built>
    После выполнения волн 1-4: Phase 51 готова к деплою на прод.

    Все артефакты:
    - DB schema (Notification, NotificationPreference, NotificationType + UserProfile.lastNotificationsSeenAt)
    - Service notify() + notifyCommentReply()
    - tRPC router notifications с 7 procedures
    - Route handler /api/notifications/notify-reply
    - NotificationBell в Header с polling pause + badge + dropdown
    - /notifications page с фильтром + пагинацией
    - /profile/notifications c toggle preferences
    - Anchor scroll + highlight на /learn/[id]
    - Cron notifications-cleanup + GitHub Actions workflow
    - Unit tests + E2E test
  </what-built>

  <how-to-verify>
    Manual smoke test на staging.platform.mpstats.academy (или локально):

    **Pre-flight: GitHub Actions secrets для E2E (BLOCKING для shipping):**
    ```bash
    gh secret list | grep -E "TEST_USER_A|TEST_USER_B|TEST_LESSON_ID"
    # Должны быть все 5 secrets. Если нет — установить:
    # gh secret set TEST_USER_A_EMAIL --body "..."
    # gh secret set TEST_USER_A_PASSWORD --body "..."
    # gh secret set TEST_USER_B_EMAIL --body "..."
    # gh secret set TEST_USER_B_PASSWORD --body "..."
    # gh secret set TEST_LESSON_ID --body "<id-бесплатного-урока>"
    ```

    **A. Bell + polling (юзер A — твой тестовый аккаунт):**
    1. Логин юзером A
    2. Открыть любую (main)/* страницу — bell виден в Header перед HelpCircleButton
    3. Badge скрыт если 0 уведомлений
    4. Открыть dropdown — увидеть копию "Пока тихо. Здесь появятся ответы..."

    **B. COMMENT_REPLY end-to-end (нужны 2 аккаунта — A и B):**
    1. С юзера A написать root коммент на любом уроке
    2. С юзера B (другой браузер/incognito) — войти, открыть тот же урок, ответить на коммент A
    3. Вернуться к юзеру A (ту же вкладку оставить открытой; не выходить)
    4. В течение ~60 секунд увидеть badge "1" в bell'е
    5. Click bell → dropdown → видеть item "<имя B> ответил на твой комментарий" + preview reply'а
    6. Click item → перейти на /learn/lessonX#comment-Y → коммент B виден + bg-mp-blue-50 highlight ~1.5с
    7. Вернуться на (main) page → badge стал 0

    **C. Anti-self-notify:**
    1. Юзер A reply на свой собственный коммент → НЕ получает уведомление (ни row, ни CQ event)

    **D. Preferences:**
    1. /profile → видеть блок "Уведомления" + ссылку "Настроить →"
    2. Click → /profile/notifications → таблица 7 типов
    3. Toggle In-app для COMMENT_REPLY → off
    4. Юзер B reply'ает → юзер A НЕ получает row (badge не растёт), но в CQ dashboard видим что pa_notif_comment_reply event всё равно стрельнул

    **E. Polling pause:**
    1. Открыть DevTools Network, filter unreadCount
    2. Запросы каждые 60с
    3. Свернуть/спрятать вкладку (Cmd+T или другая вкладка)
    4. Подождать 60+с — запросов НЕТ
    5. Вернуться на вкладку — запрос ВОЗОБНОВЛЯЕТСЯ

    **F. Cron (smoke):**
    1. На staging вручную через GitHub UI запустить workflow `Notifications Cleanup` (workflow_dispatch)
    2. Sentry → Crons → найти monitor `notifications-cleanup` → последний checkin = ok
    3. Логи route'а в Vercel/Docker logs: `[notifications-cleanup] retention=N overflow=M`

    **G. Playwright E2E (если secrets выставлены):**
    1. Локально: `pnpm test:e2e --grep "COMMENT_REPLY flow"` → PASS
    2. На GitHub Actions: workflow проходит без `Missing GitHub Actions secrets` error
  </how-to-verify>

  <resume-signal>
    После успешной верификации → "approved" + готов к prod-deploy.
    Если что-то сломано → описать симптом + step где упал → новый план гап-закрытия.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → /api/cron/notifications-cleanup | CRON_SECRET Bearer auth |
| Cron route → DB | Trusted server, full DELETE permissions через Prisma |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-07-01 | S (Spoofing) | Anonym hits /api/cron/notifications-cleanup | mitigate | CRON_SECRET Bearer check first thing in handler; 401 if missing/wrong |
| T-51-07-02 | T (Tampering) | Raw SQL injection в overflow delete | mitigate | $executeRaw template literal с Prisma — safe binding для PER_USER_CAP (number); table name hardcoded; нет user input |
| T-51-07-03 | D (DoS) | maxRuntime > 30s blocks subsequent runs | accept | maxRuntime: 30 в Sentry config; cron once per day, idempotent |
| T-51-07-04 | I (Info disclosure) | DELETE стирает чужие notifications | accept | Это intended behavior — retention cleanup для всех юзеров; нет leak'а в response (только cleaned counts) |
| T-51-07-05 | R (Repudiation) | Mass deletion без audit log | accept | Sentry checkin + console logs обеспечивают forensics; возможные mass-deletion инциденты ловятся через rate-limit на Sentry monitor frequency |
</threat_model>

<verification>
- Cron route отвечает 401 без auth
- Cron route отвечает 200 + cleaned counts при правильном CRON_SECRET
- GitHub Actions workflow тригерит daily 00:00 UTC + supports workflow_dispatch
- Sentry monitor `notifications-cleanup` визуализирован после первого запуска
- E2E reply→bell→click→markRead end-to-end PASS
- Все 5 GitHub Actions secrets для E2E установлены (TEST_USER_A_*, TEST_USER_B_*, TEST_LESSON_ID)
- Manual smoke checks A-G (см. checkpoint task) — все зелёные
</verification>

<success_criteria>
- [ ] /api/cron/notifications-cleanup работает с auth + Sentry checkin + 90d delete + 500-cap
- [ ] .github/workflows/notifications-cleanup.yml schedule + workflow_dispatch
- [ ] Playwright E2E тест существует с **explicit fail** (НЕ skip) при отсутствии secrets
- [ ] GitHub Actions secrets установлены (gh secret list проходит)
- [ ] E2E PASS локально (с настроенными env-vars)
- [ ] Manual smoke checks A-G все зелёные
- [ ] Sentry monitor отображается и принимает checkins
- [ ] `pnpm typecheck` + `pnpm test --run` + `pnpm build` все exit 0
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-07-SUMMARY.md`:
- Cron + GitHub workflow + E2E готовы
- Phase 51 закрыта; готова к деплою на prod
- Зафиксировать в SUMMARY: имена 5 GitHub Actions secrets для E2E + что они должны содержать
- Известные ограничения для phase 52+:
  * Email Switch'и hard-disabled — снимется per-type когда CQ-шаблоны готовы
  * No real-time WS/SSE — polling 60s достаточен
  * No notification grouping — Phase 52 для CONTENT_UPDATE
  * No ADMIN_COMMENT_REPLY accent — Phase 52
- Артефакты для phase 52+: notify() + tRPC router расширяемы; новый тип = enum + payload variant + триггер, инфраструктура не меняется
</output>
</content>
</invoke>