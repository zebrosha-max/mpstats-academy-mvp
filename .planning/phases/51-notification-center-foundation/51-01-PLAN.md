---
phase: 51-notification-center-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/shared/src/notifications.ts
  - packages/shared/src/index.ts
  - apps/web/src/lib/carrotquest/types.ts
autonomous: true
requirements: [REQ-51-01, REQ-51-02, REQ-51-03]

must_haves:
  truths:
    - "Notification + NotificationPreference таблицы в Supabase отвечают на Prisma queries (smoke `prisma.notification.count()` работает)"
    - "UserProfile.lastNotificationsSeenAt колонка существует в БД (nullable timestamptz)"
    - "Downstream waves (02-07) могут импортить NotificationPayload + DEFAULT_IN_APP_PREFS из @mpstats/shared без typecheck-ошибок"
    - "NotificationType enum содержит 7 значений в SPEC-порядке (порядок не меняется впредь — D-10)"
    - "CQ trackEvent(`pa_notif_*`) принимает 7 новых event names без TS-ошибок"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "Notification + NotificationPreference + NotificationType enum + UserProfile.lastNotificationsSeenAt"
      contains: "model Notification"
    - path: "packages/shared/src/notifications.ts"
      provides: "NotificationPayload discriminated union, NOTIFICATION_TYPE_DESCRIPTIONS map"
      exports: ["NotificationPayload", "NOTIFICATION_TYPE_DESCRIPTIONS"]
    - path: "packages/shared/src/index.ts"
      provides: "re-export './notifications'"
    - path: "apps/web/src/lib/carrotquest/types.ts"
      provides: "CQEventName extended with pa_notif_*"
  key_links:
    - from: "packages/db/prisma/schema.prisma"
      to: "UserProfile model"
      via: "Notification.user relation + NotificationPreference.user relation + lastNotificationsSeenAt field"
    - from: "packages/shared/src/index.ts"
      to: "packages/shared/src/notifications.ts"
      via: "export *"
---

<objective>
Заложить фундамент Phase 51 — расширить Prisma schema (Notification, NotificationPreference, NotificationType, UserProfile.lastNotificationsSeenAt), создать TypeScript discriminated union NotificationPayload в shared package, расширить CQEventName 7 новыми событиями. Применить миграцию через `pnpm db:push` ДО любого rebuild docker (recurring Phase 28 lesson).

Purpose: Все нижестоящие плановые волны (notify service, tRPC router, UI) зависят от этих типов и таблиц. Без них typecheck падает в первой же строке.

Output: Prisma client сгенерирован с новыми моделями, БД содержит таблицы и индексы, shared package экспортирует payload-типы, CQ типы готовы для notify().

**Wave dependency note (важно для executor'а):** schema push выполняется ОДИН РАЗ — внутри `<verify>` Task 1 (`pnpm db:generate && pnpm db:push`). Дальнейшие waves (02-07) НЕ должны повторять `pnpm db:push` — это уже сделано здесь. Wave dependency `depends_on: [51-01]` гарантирует, что upstream waves стартуют только после успешного завершения этого плана. Если schema push fails в Task 1 — план не считается завершённым, downstream wave 2 не запустится.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/51-notification-center-foundation/51-SPEC.md
@.planning/phases/51-notification-center-foundation/51-CONTEXT.md
@.planning/phases/51-notification-center-foundation/51-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Existing patterns в schema.prisma — нужны для consistency -->

Pattern 1 — model + multi-column index (LessonComment, schema.prisma:369-386):
```prisma
model LessonComment {
  id        String   @id @default(cuid())
  lessonId  String
  userId    String
  // ...
  user     UserProfile    @relation(fields: [userId], references: [id], onDelete: Cascade)
  @@index([lessonId, createdAt(sort: Desc)])
}
```

Pattern 2 — composite PK (PromoActivation:323):
```prisma
@@unique([promoCodeId, userId])
```

Pattern 3 — enum (MaterialType:390-396):
```prisma
enum MaterialType {
  PRESENTATION
  CALCULATION_TABLE
  // ...
}
```

Existing CQEventName union (apps/web/src/lib/carrotquest/types.ts):
```typescript
export type CQEventName =
  | 'pa_payment_success'
  | 'pa_payment_failed'
  // ...existing pa_* events
  | 'pa_email_change';
```

Existing shared package barrel (packages/shared/src/index.ts):
```typescript
export * from './types';
export * from './cloudpayments';
```

Reference for enum-as-const + types pattern (packages/shared/src/types/index.ts:3-11):
```typescript
export const SkillCategory = { ANALYTICS: 'ANALYTICS', ... } as const;
export type SkillCategory = (typeof SkillCategory)[keyof typeof SkillCategory];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Расширить Prisma schema (Notification + NotificationPreference + enum + UserProfile field) + applied schema push</name>
  <files>packages/db/prisma/schema.prisma</files>

  <read_first>
    - packages/db/prisma/schema.prisma (целиком — для понимания UserProfile relations и существующих моделей; обратить внимание на строки 26-46 = UserProfile, 369-386 = LessonComment, 390-396 = MaterialType enum, 312-324 = PromoActivation)
    - .planning/phases/51-notification-center-foundation/51-SPEC.md разделы requirements 1-3
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 32-128 (schema patterns)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md D-07 (lastNotificationsSeenAt)
    - .claude/memory/feedback_schema_migration_order.md (миграция ПЕРЕД rebuild)
  </read_first>

  <action>
    Внести в `packages/db/prisma/schema.prisma` три блока:

    **1. Добавить enum NotificationType** (после существующих enum'ов, рядом с MaterialType):
    ```prisma
    enum NotificationType {
      COMMENT_REPLY
      ADMIN_COMMENT_REPLY
      CONTENT_UPDATE
      PROGRESS_NUDGE
      INACTIVITY_RETURN
      WEEKLY_DIGEST
      BROADCAST
    }
    ```
    Порядок значений ВАЖЕН — менять нельзя (per D-10 + SPEC req 3). 7 значений ровно.

    **2. Добавить две модели в конец файла** (после Material / других моделей):
    ```prisma
    model Notification {
      id          String           @id @default(cuid())
      userId      String
      type        NotificationType
      payload     Json
      ctaUrl      String?
      readAt      DateTime?
      createdAt   DateTime         @default(now())
      broadcastId String?

      user UserProfile @relation(fields: [userId], references: [id], onDelete: Cascade)

      @@index([userId, readAt, createdAt(sort: Desc)])
    }

    model NotificationPreference {
      userId String
      type   NotificationType
      inApp  Boolean @default(true)
      email  Boolean @default(false)

      user UserProfile @relation(fields: [userId], references: [id], onDelete: Cascade)

      @@id([userId, type])
    }
    ```

    Внимание: SPEC req 2 + D-15 говорят `inApp default = true для всех типов кроме WEEKLY_DIGEST`. На уровне Prisma `@default(true)` стоит для всех — override для WEEKLY_DIGEST реализуется в коде getPreferences (план 03), не на уровне схемы (нельзя задать per-enum-value default в Prisma).

    **3. Расширить модель UserProfile** (apps/web/src/...не там, в packages/db/prisma/schema.prisma строки ~26-46):
    - Добавить поле `lastNotificationsSeenAt DateTime?` (per D-07)
    - Добавить две relation-обратки:
      ```prisma
      notifications     Notification[]
      notificationPrefs NotificationPreference[]
      ```

    Не трогать остальные поля UserProfile.

    **Применить миграцию** (CRITICAL — recurring Phase 28 lesson; ЭТО ЕДИНСТВЕННЫЙ db:push в фазе):
    ```bash
    cd MAAL
    pnpm db:generate    # генерирует Prisma client с новыми типами
    pnpm db:push        # применяет схему в БД (Supabase prod-shared)
    ```

    После `db:push` Prisma client должен содержать:
    - `prisma.notification.*` методы
    - `prisma.notificationPreference.*` методы
    - тип `NotificationType` экспортируется из `@mpstats/db` (или `@prisma/client` через re-export)
    - `prisma.userProfile` имеет поле `lastNotificationsSeenAt`

    Если db:push спрашивает про potential data loss — НЕ потерять данные UserProfile (мы только добавляем nullable column + reverse relations, это аддитивно и data-safe).
  </action>

  <verify>
    <automated>cd MAAL && pnpm db:generate && pnpm db:push && pnpm typecheck && node -e "const { prisma } = require('@mpstats/db/client'); prisma.notification.count().then(c => console.log('Notification table OK, rows:', c)).catch(e => { console.error('FAIL', e.message); process.exit(1); })"</automated>
  </verify>

  <done>
    - `pnpm db:push` exits 0 без потери данных
    - `pnpm db:generate` exits 0
    - `grep -n "model Notification {" packages/db/prisma/schema.prisma` находит модель
    - `grep -n "enum NotificationType" packages/db/prisma/schema.prisma` находит enum с 7 значениями
    - `grep -n "lastNotificationsSeenAt" packages/db/prisma/schema.prisma` находит поле в UserProfile
    - `pnpm typecheck` exits 0 (нет ошибок в коде, использующем UserProfile/Prisma)
    - Smoke node check выводит `Notification table OK, rows: 0`
  </done>
</task>

<task type="auto">
  <name>Task 2: Создать packages/shared/src/notifications.ts с NotificationPayload discriminated union + NOTIFICATION_TYPE_DESCRIPTIONS</name>
  <files>packages/shared/src/notifications.ts, packages/shared/src/index.ts</files>

  <read_first>
    - packages/shared/src/index.ts (текущий barrel — 2 строки)
    - packages/shared/src/types/index.ts (для паттерна enum-as-const + types)
    - .planning/phases/51-notification-center-foundation/51-CONTEXT.md секция D-15, D-16, DC-01
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 411-492 (shared notifications skeleton)
  </read_first>

  <action>
    Создать новый файл `packages/shared/src/notifications.ts` со следующим содержимым:

    ```typescript
    /**
     * Notification payload types — discriminated union per NotificationType.
     *
     * Stored as Prisma `Json` in Notification.payload column.
     * Runtime validation via Zod schemas (see apps/web/src/lib/notifications/notify.ts).
     */

    export type NotificationPayload =
      | {
          type: 'COMMENT_REPLY';
          commentId: string;
          lessonId: string;
          lessonTitle: string;
          replyAuthorName: string;
          preview: string;
          actorUserId: string; // for anti-self-notify check
        }
      | {
          type: 'ADMIN_COMMENT_REPLY';
          commentId: string;
          lessonId: string;
          lessonTitle: string;
          replyAuthorName: string;
          preview: string;
          actorUserId: string;
        }
      | {
          type: 'CONTENT_UPDATE';
          courseId: string;
          courseTitle: string;
          lessonIds: string[];
        }
      | {
          type: 'PROGRESS_NUDGE';
          lessonId: string;
          lessonTitle: string;
        }
      | {
          type: 'INACTIVITY_RETURN';
          daysSinceLastActive: number;
        }
      | {
          type: 'WEEKLY_DIGEST';
          newLessonsCount: number;
          activityCount: number;
        }
      | {
          type: 'BROADCAST';
          title: string;
          body: string;
          ctaText?: string;
        };

    /**
     * Russian labels for /profile/notifications descriptions (D-16).
     * Tone: «ты» + дружелюбный (D-14).
     */
    export const NOTIFICATION_TYPE_DESCRIPTIONS: Record<string, string> = {
      COMMENT_REPLY: 'Ответы на твои комментарии в уроках',
      ADMIN_COMMENT_REPLY: 'Ответы методологов на твои вопросы',
      CONTENT_UPDATE: 'Новые уроки и материалы в твоих курсах',
      PROGRESS_NUDGE: 'Напоминания о незавершённых уроках',
      INACTIVITY_RETURN: 'Если давно не заходил — расскажем что нового',
      WEEKLY_DIGEST: 'Дайджест по пятницам — новинки и активность',
      BROADCAST: 'Анонсы курсов и важные новости платформы',
    };

    /**
     * Default in-app preference per type (SPEC req 2):
     * - WEEKLY_DIGEST = false (opt-in)
     * - All others = true
     */
    export const DEFAULT_IN_APP_PREFS: Record<string, boolean> = {
      COMMENT_REPLY: true,
      ADMIN_COMMENT_REPLY: true,
      CONTENT_UPDATE: true,
      PROGRESS_NUDGE: true,
      INACTIVITY_RETURN: true,
      WEEKLY_DIGEST: false,
      BROADCAST: true,
    };

    export const ALL_NOTIFICATION_TYPES = [
      'COMMENT_REPLY',
      'ADMIN_COMMENT_REPLY',
      'CONTENT_UPDATE',
      'PROGRESS_NUDGE',
      'INACTIVITY_RETURN',
      'WEEKLY_DIGEST',
      'BROADCAST',
    ] as const;

    export type NotificationTypeName = (typeof ALL_NOTIFICATION_TYPES)[number];
    ```

    Затем добавить в `packages/shared/src/index.ts` третью строку:
    ```typescript
    export * from './types';
    export * from './cloudpayments';
    export * from './notifications';
    ```
  </action>

  <verify>
    <automated>cd MAAL && pnpm typecheck</automated>
  </verify>

  <done>
    - Файл `packages/shared/src/notifications.ts` существует
    - `grep -n "export type NotificationPayload" packages/shared/src/notifications.ts` находит union
    - `grep -n "export const NOTIFICATION_TYPE_DESCRIPTIONS" packages/shared/src/notifications.ts` находит map с 7 ключами
    - `grep -n "export \* from './notifications'" packages/shared/src/index.ts` находит реэкспорт
    - `pnpm typecheck` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Расширить CQEventName в apps/web/src/lib/carrotquest/types.ts 7 новыми pa_notif_* событиями</name>
  <files>apps/web/src/lib/carrotquest/types.ts</files>

  <read_first>
    - apps/web/src/lib/carrotquest/types.ts (целиком — текущий CQEventName union)
    - .claude/memory/cq-integration.md (gotchas: form-encoded НЕ JSON, pa_ prefix)
    - .planning/phases/51-notification-center-foundation/51-PATTERNS.md строки 942-959
  </read_first>

  <action>
    Найти в `apps/web/src/lib/carrotquest/types.ts` блок `export type CQEventName = | 'pa_*' | ...` и **добавить 7 новых строк В КОНЕЦ union** (сохранив существующие):

    ```typescript
    export type CQEventName =
      // ... ВСЕ существующие pa_* события — НЕ удалять ...
      // Notifications (Phase 51)
      | 'pa_notif_comment_reply'
      | 'pa_notif_admin_comment_reply'
      | 'pa_notif_content_update'
      | 'pa_notif_progress_nudge'
      | 'pa_notif_inactivity_return'
      | 'pa_notif_weekly_digest'
      | 'pa_notif_broadcast';
    ```

    КРИТИЧЕСКИ ВАЖНО: использовать lowercase + underscore mapping (`COMMENT_REPLY` → `pa_notif_comment_reply`), это совпадает с тем как `notify()` будет вычислять имя события через `type.toLowerCase()` (план 02).

    Не модифицировать другие файлы в `apps/web/src/lib/carrotquest/` в этой задаче — `client.ts` и `emails.ts` остаются нетронутыми (notify() будет переиспользовать `cq` singleton).
  </action>

  <verify>
    <automated>cd MAAL && grep -c "pa_notif_" apps/web/src/lib/carrotquest/types.ts && pnpm typecheck</automated>
  </verify>

  <done>
    - `grep -c "pa_notif_" apps/web/src/lib/carrotquest/types.ts` returns 7
    - Каждое из 7 имён присутствует ровно один раз: `pa_notif_comment_reply`, `pa_notif_admin_comment_reply`, `pa_notif_content_update`, `pa_notif_progress_nudge`, `pa_notif_inactivity_return`, `pa_notif_weekly_digest`, `pa_notif_broadcast`
    - Существующие события (pa_payment_*, pa_email_*, etc.) НЕ удалены
    - `pnpm typecheck` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Prisma client → DB | Schema migration пишется в prod БД (Supabase shared) — must be additive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01-01 | T (Tampering) | schema.prisma migration | mitigate | Все изменения аддитивны (новые таблицы + nullable column); `pnpm db:push` стопится при потенциальной data loss; verify через `pnpm db:generate` ДО push'а |
| T-51-01-02 | I (Info disclosure) | NotificationPayload typing | accept | Payload — `Json` в Prisma, не индексируется и не searchable снаружи; CQ event firing решает доставку email'а отдельно |
| T-51-01-03 | E (Elevation) | Migration data loss | mitigate | Schema changes only ADD models/columns; UserProfile.lastNotificationsSeenAt nullable; reverse relations не требуют backfill; existing data не трогается |
</threat_model>

<verification>
- Schema migration применена в БД без data loss
- TypeScript types из `@mpstats/db` экспортируют `NotificationType` enum, `prisma.notification`, `prisma.notificationPreference`
- TypeScript types из `@mpstats/shared` экспортируют `NotificationPayload`, `NOTIFICATION_TYPE_DESCRIPTIONS`, `DEFAULT_IN_APP_PREFS`, `ALL_NOTIFICATION_TYPES`
- CQ types содержат 7 новых `pa_notif_*` событий
- `pnpm typecheck` зелёный во всём монорепо
</verification>

<success_criteria>
- [ ] `model Notification` существует в schema.prisma c индексом `(userId, readAt, createdAt DESC)`
- [ ] `model NotificationPreference` существует с composite PK `@@id([userId, type])`
- [ ] `enum NotificationType` содержит 7 значений в SPEC-порядке
- [ ] `UserProfile.lastNotificationsSeenAt DateTime?` добавлено
- [ ] `packages/shared/src/notifications.ts` экспортирует `NotificationPayload` discriminated union + `NOTIFICATION_TYPE_DESCRIPTIONS` + `DEFAULT_IN_APP_PREFS` + `ALL_NOTIFICATION_TYPES`
- [ ] `packages/shared/src/index.ts` re-export'ит `./notifications`
- [ ] `apps/web/src/lib/carrotquest/types.ts` `CQEventName` union содержит 7 новых pa_notif_* событий
- [ ] `pnpm db:generate` + `pnpm db:push` + `pnpm typecheck` exit 0
- [ ] Smoke check `prisma.notification.count()` работает
</success_criteria>

<output>
После завершения создать `.planning/phases/51-notification-center-foundation/51-01-SUMMARY.md`:
- Какие модели/типы/events добавлены
- Команды миграции (для записи в memory если будут проблемы на VPS)
- Артефакты для следующих волн (Notification, NotificationPreference, NotificationPayload, ALL_NOTIFICATION_TYPES, DEFAULT_IN_APP_PREFS, CQEventName extension)
- **Подтверждение что schema push выполнен** — downstream waves НЕ должны его повторять
</output>
</content>
</invoke>