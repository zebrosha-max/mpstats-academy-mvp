---
phase: 38-diagnostic-ux-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ui/tooltip.tsx
  - apps/web/src/app/(main)/diagnostic/results/page.tsx
  - apps/web/src/app/(main)/learn/page.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10

must_haves:
  truths:
    - "Заголовок зон развития показывает количество ВСЕХ gaps с gap > 0, а не только HIGH"
    - "Badge'и показывают Высокий/Средний/Низкий с tooltip-пояснениями"
    - "На мобилке badge'и не обрезаются (flex-wrap)"
    - "Пустые секции трека скрыты, при всех пустых — placeholder"
    - "При отсутствии результатов показывается понятное сообщение с кнопкой перезагрузки"
  artifacts:
    - path: "apps/web/src/components/ui/tooltip.tsx"
      provides: "shadcn/ui Tooltip component"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
    - path: "apps/web/src/app/(main)/diagnostic/results/page.tsx"
      provides: "Fixed zones counter, badge labels, tooltips, error state"
      contains: "pluralizeZones"
    - path: "apps/web/src/app/(main)/learn/page.tsx"
      provides: "Empty section filtering"
      contains: "filteredLessons"
  key_links:
    - from: "apps/web/src/app/(main)/diagnostic/results/page.tsx"
      to: "apps/web/src/components/ui/tooltip.tsx"
      via: "import Tooltip components"
      pattern: "from.*components/ui/tooltip"
---

<objective>
Исправить отображение результатов диагностики: корректный подсчёт зон развития, понятные badge'и с tooltip, скрытие пустых секций трека, улучшенный error state.

Purpose: Пользователи не понимают результаты диагностики — заголовок показывает "1 зону" при 4 рекомендациях, badge "Приоритет" ничего не говорит, пустые секции трека путают, а "Результаты не найдены" — тупик без действия.
Output: Исправленные results/page.tsx и learn/page.tsx + новый tooltip.tsx компонент.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-diagnostic-ux-fix/38-CONTEXT.md
@.planning/phases/38-diagnostic-ux-fix/38-RESEARCH.md
@apps/web/src/app/(main)/diagnostic/results/page.tsx
@apps/web/src/app/(main)/learn/page.tsx

<interfaces>
<!-- Tooltip component will follow standard shadcn/ui pattern, identical to other Radix wrappers in the project -->

From apps/web/src/components/ui/badge.tsx (existing):
```typescript
export interface BadgeProps extends React.HTMLAttributes<HTMLDivElement> {
  variant?: 'default' | 'secondary' | 'destructive' | 'outline' | 'warning' | 'success';
}
export function Badge({ className, variant, ...props }: BadgeProps): JSX.Element;
```

Current PRIORITY_STYLES (results/page.tsx line 16-19):
```typescript
const PRIORITY_STYLES = {
  HIGH: { badge: 'destructive' as const, label: 'Приоритет' },
  MEDIUM: { badge: 'warning' as const, label: 'Средний' },
  LOW: { badge: 'success' as const, label: 'Низкий' },
};
```

Current zones counter (results/page.tsx line 127):
```typescript
{results.gaps.filter(g => g.priority === 'HIGH').length}
```

Current error state (results/page.tsx line 69-87):
```typescript
if (!results) {
  // Shows "Результаты не найдены" with button to /diagnostic
}
```

Learn page section rendering (learn/page.tsx line 547-584):
```typescript
{recommendedPath.sections!.map((section) => {
  const completedInSection = section.lessons.filter(...).length;
  // renders section with completedInSection/section.lessons.length counter
  // lessons filtered by filterLesson inside .map() but counter uses unfiltered count
})}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Radix Tooltip + create shadcn/ui tooltip component</name>
  <files>apps/web/src/components/ui/tooltip.tsx, apps/web/package.json</files>
  <read_first>
    - apps/web/src/components/ui/popover.tsx (reference for shadcn/ui Radix wrapper pattern)
    - apps/web/package.json (verify @radix-ui/react-tooltip not installed)
  </read_first>
  <action>
1. Install `@radix-ui/react-tooltip` in web workspace:
   ```bash
   pnpm add @radix-ui/react-tooltip --filter web
   ```

2. Create `apps/web/src/components/ui/tooltip.tsx` following the same pattern as existing `popover.tsx`:
   - "use client" directive
   - Import `* as TooltipPrimitive from "@radix-ui/react-tooltip"`
   - Export `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent`
   - `TooltipContent` as forwardRef with className:
     ```
     "z-50 overflow-hidden rounded-md bg-mp-gray-900 px-3 py-1.5 text-xs text-white shadow-md animate-in fade-in-0 zoom-in-95"
     ```
   - Default `sideOffset={4}`
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsc --noEmit apps/web/src/components/ui/tooltip.tsx 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/src/components/ui/tooltip.tsx` exists
    - Exports: `TooltipProvider`, `Tooltip`, `TooltipTrigger`, `TooltipContent`
    - `@radix-ui/react-tooltip` in `apps/web/package.json` dependencies
  </acceptance_criteria>
  <done>Tooltip component created with shadcn/ui pattern, package installed</done>
</task>

<task type="auto">
  <name>Task 2: Fix results page — zones counter, badge labels+tooltips, mobile layout, error state</name>
  <files>apps/web/src/app/(main)/diagnostic/results/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/diagnostic/results/page.tsx (full file — already read)
    - apps/web/src/components/ui/tooltip.tsx (just created in Task 1)
  </read_first>
  <action>
All changes in `apps/web/src/app/(main)/diagnostic/results/page.tsx`:

**1. Import Tooltip (top of file, per D-04):**
```typescript
import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from '@/components/ui/tooltip';
```

**2. Update PRIORITY_STYLES (line 16-19, per D-03 + D-04):**
```typescript
const PRIORITY_STYLES = {
  HIGH: { badge: 'destructive' as const, label: 'Высокий', tooltip: 'Большой разрыв с целью — рекомендуем начать с этой темы' },
  MEDIUM: { badge: 'warning' as const, label: 'Средний', tooltip: 'Есть потенциал для улучшения' },
  LOW: { badge: 'success' as const, label: 'Низкий', tooltip: 'Близко к цели — поддерживайте уровень' },
};
```

**3. Add pluralization helper (after PRIORITY_STYLES, per D-02):**
```typescript
function pluralizeZones(n: number): string {
  if (n % 10 === 1 && n % 100 !== 11) return `${n} зона`;
  if ([2, 3, 4].includes(n % 10) && ![12, 13, 14].includes(n % 100)) return `${n} зоны`;
  return `${n} зон`;
}
```

**4. Fix zones counter (line 126-130, per D-01 + D-02):**
Replace:
```tsx
<div className="text-display font-bold text-mp-blue-500">
  {results.gaps.filter(g => g.priority === 'HIGH').length}
</div>
<div className="text-body-sm text-mp-gray-500 mt-1">Зон для развития</div>
```
With:
```tsx
<div className="text-display font-bold text-mp-blue-500">
  {results.gaps.filter(g => g.gap > 0).length}
</div>
<div className="text-body-sm text-mp-gray-500 mt-1">
  {pluralizeZones(results.gaps.filter(g => g.gap > 0).length)} для развития
</div>
```

**5. Add "Приоритет изучения" subheading (per D-05):**
Inside the "Рекомендации по развитию" CardHeader, after CardDescription, add:
```tsx
<p className="text-body-sm font-medium text-mp-gray-700 mt-3">Приоритет изучения</p>
```

**6. Fix badge with tooltip + mobile flex-wrap (lines 173-206, per D-04 + D-06):**
Wrap the entire gaps list in `<TooltipProvider>`.
On each gap row div (line 176), change className:
```
"flex items-center justify-between p-4 ..."
```
to:
```
"flex flex-wrap items-center gap-2 sm:gap-0 sm:justify-between p-4 ..."
```

Replace the plain Badge (line 203-205):
```tsx
<Badge variant={PRIORITY_STYLES[gap.priority].badge}>
  {PRIORITY_STYLES[gap.priority].label}
</Badge>
```
With tooltip-wrapped Badge:
```tsx
<Tooltip>
  <TooltipTrigger asChild>
    <span tabIndex={0} className="cursor-help shrink-0">
      <Badge variant={PRIORITY_STYLES[gap.priority].badge} className="text-xs">
        {PRIORITY_STYLES[gap.priority].label}
      </Badge>
    </span>
  </TooltipTrigger>
  <TooltipContent>
    <p>{PRIORITY_STYLES[gap.priority].tooltip}</p>
  </TooltipContent>
</Tooltip>
```

**7. Fix error state (lines 69-87, per D-10):**
Replace the text "Результаты не найдены" (line 79) with:
```tsx
<p className="text-body text-mp-gray-500">
  Произошла ошибка при загрузке результатов. Попробуйте перезагрузить страницу.
</p>
<Button variant="outline" className="mt-4" onClick={() => window.location.reload()}>
  Перезагрузить
</Button>
<Button variant="ghost" className="mt-2" onClick={() => router.push('/diagnostic')}>
  Пройти диагностику заново
</Button>
```
Remove the existing "Пройти диагностику" button (line 80-82) since it's replaced above.

**8. Add retry to getResults query (per D-09 race condition mitigation):**
On the `trpc.diagnostic.getResults.useQuery` call (line 27-30), add retry:
```typescript
{ enabled: !!sessionId, retry: 2, retryDelay: 1000 }
```
This handles the potential race condition where session is still completing when results page loads.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `results.gaps.filter(g => g.gap > 0).length` in zones counter (not `g.priority === 'HIGH'`)
    - `pluralizeZones` function exists and used in zones display
    - PRIORITY_STYLES contains `label: 'Высокий'` and `tooltip:` fields for all three levels
    - `TooltipProvider` wraps gaps list, each Badge wrapped in Tooltip
    - `flex-wrap` on gap row container
    - Error state shows "Произошла ошибка" text and reload button
    - `retry: 2` on getResults query
  </acceptance_criteria>
  <done>Zones counter shows all gaps > 0, badges have Russian labels with tooltips, mobile layout wraps, error state is actionable, query has retry</done>
</task>

<task type="auto">
  <name>Task 3: Fix empty sections in learn page track view</name>
  <files>apps/web/src/app/(main)/learn/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/learn/page.tsx (lines 540-600 — section rendering)
  </read_first>
  <action>
All changes in `apps/web/src/app/(main)/learn/page.tsx`:

**1. Fix section rendering to filter empty sections (per D-07, around line 547):**
Replace:
```tsx
{recommendedPath.sections!.map((section) => {
  const style = SECTION_STYLES[section.id] || SECTION_STYLES.growth;
  const isOpen = expandedSections.has(section.id);
  const completedInSection = section.lessons.filter((l: { status: string }) => l.status === 'COMPLETED').length;
```
With:
```tsx
{recommendedPath.sections!
  .map(section => ({
    ...section,
    _filteredLessons: section.lessons.filter((l: any) => filterLesson(l as LessonWithProgress)),
  }))
  .filter(section => section._filteredLessons.length > 0)
  .map((section) => {
  const style = SECTION_STYLES[section.id] || SECTION_STYLES.growth;
  const isOpen = expandedSections.has(section.id);
  const completedInSection = section._filteredLessons.filter((l: { status: string }) => l.status === 'COMPLETED').length;
```

**2. Fix section counter (line 570):**
Replace:
```tsx
{completedInSection}/{section.lessons.length}
```
With:
```tsx
{completedInSection}/{section._filteredLessons.length}
```

**3. Fix lesson rendering inside section (line 582-583):**
Replace:
```tsx
{section.lessons
  .filter((lesson) => filterLesson(lesson as LessonWithProgress))
  .map((lesson, idx: number) => (
```
With (use pre-filtered list):
```tsx
{section._filteredLessons
  .map((lesson: any, idx: number) => (
```

**4. Add "all sections empty" placeholder (per D-08):**
After the sections `.map()` closing, before the next sibling element, add:
```tsx
{recommendedPath.sections!.every(section =>
  section.lessons.filter((l: any) => filterLesson(l as LessonWithProgress)).length === 0
) && (
  <Card className="shadow-mp-card">
    <CardContent className="py-8 text-center">
      <div className="w-16 h-16 rounded-2xl bg-mp-green-100 flex items-center justify-center mx-auto mb-4">
        <svg className="w-8 h-8 text-mp-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" />
        </svg>
      </div>
      <p className="text-body font-medium text-mp-green-700">Отличный результат! Все темы освоены.</p>
    </CardContent>
  </Card>
)}
```
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `_filteredLessons` computed before section rendering
    - `.filter(section => section._filteredLessons.length > 0)` present in sections chain
    - Counter uses `section._filteredLessons.length` not `section.lessons.length`
    - "Отличный результат! Все темы освоены." placeholder exists for all-empty case
  </acceptance_criteria>
  <done>Empty sections hidden from track view, counter shows filtered count, all-empty shows congratulation placeholder</done>
</task>

</tasks>

<verification>
1. `pnpm build` passes without errors
2. Zones counter on results page shows count of ALL gaps with gap > 0
3. Badge labels: "Высокий", "Средний", "Низкий" (not "Приоритет")
4. Hover on badge shows tooltip text
5. Mobile: badges wrap without overflow
6. Empty track sections hidden; all-empty shows placeholder
7. "Результаты не найдены" replaced with actionable error + reload button
8. getResults query retries on failure (race condition mitigation)
</verification>

<success_criteria>
- Build passes
- All 10 decisions (D-01 through D-10) implemented
- No regressions in existing diagnostic or learning flows
</success_criteria>

<output>
After completion, create `.planning/phases/38-diagnostic-ux-fix/38-01-SUMMARY.md`
</output>
