---
phase: 56-entry-flow-redesign
plan: 04
type: execute
wave: 4
depends_on: [56-02, 56-03]
files_modified:
  - apps/web/src/components/learning/DiagnosticGateBanner.tsx
  - apps/web/src/app/(main)/learn/[id]/page.tsx
  - apps/web/src/app/(main)/profile/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Без пройденной диагностики пользователь смотрит все уроки в рамках своей подписки"
    - "Жёсткий гейт диагностики на странице урока снят, подписочный LockOverlay сохранён"
    - "DiagnosticGateBanner — ненавязчивый закрываемый хинт над плеером, не блокирует видео"
    - "После закрытия хинт не возвращается (localStorage-флаг)"
    - "Квалификацию (маркетплейсы / опыт / цели / текст) можно отредактировать в /profile"
  artifacts:
    - path: "apps/web/src/components/learning/DiagnosticGateBanner.tsx"
      provides: "Закрываемая хинт-карточка диагностики"
      contains: "diagnosticHintDismissed"
    - path: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      provides: "Урок без жёсткого гейта диагностики"
    - path: "apps/web/src/app/(main)/profile/page.tsx"
      provides: "Редактирование квалификации через onboarding"
  key_links:
    - from: "learn/[id]/page.tsx"
      to: "DiagnosticGateBanner"
      via: "non-blocking render above player"
      pattern: "hasDiagnostic === false &&"
    - from: "DiagnosticGateBanner.tsx"
      to: "localStorage"
      via: "diagnosticHintDismissed flag"
      pattern: "localStorage"
    - from: "profile/page.tsx"
      to: "trpc.onboarding"
      via: "getState query + complete mutation"
      pattern: "onboarding"
---

<objective>
Снять жёсткий гейт диагностики на странице урока, переделать `DiagnosticGateBanner` из блокирующего баннера в закрываемый хинт, и добавить редактирование квалификации на странице `/profile`.

Purpose: Завершает де-гейтинг (Success Criteria 4) — урок доступен на подписке без диагностики; диагностика становится необязательной подсказкой. Редактирование в `/profile` (Success Criteria 6) даёт пользователю контроль над собранными данными.
Output: переделанный `DiagnosticGateBanner`, правка `learn/[id]/page.tsx`, секция квалификации в `/profile`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-entry-flow-redesign/56-RESEARCH.md
@.planning/phases/56-entry-flow-redesign/56-PATTERNS.md
@.planning/phases/56-entry-flow-redesign/56-UI-SPEC.md

<interfaces>
<!-- Контракты для executor. -->

tRPC (план 56-02):
  `trpc.onboarding.getState.useQuery()` → `{ onboardingCompletedAt, marketplaces, experienceLevel, goals, goalText }`
  `trpc.onboarding.complete.useMutation()` принимает `{ marketplaces, experienceLevel, goals, goalText }`

Компоненты опций (план 56-03): `apps/web/src/components/welcome/options.ts` экспортирует
  `GOAL_OPTIONS`, `MARKETPLACE_OPTIONS`, `EXPERIENCE_OPTIONS` — переиспользуемы в /profile.

Существующее (page.tsx урока):
  `hasDiagnostic` ← `trpc.diagnostic.hasCompletedDiagnostic.useQuery()` (строка ~302) — процедуру НЕ менять.
  `lesson.locked` → `<LockOverlay/>` — независимый подписочный гейт, НЕ трогать (STATE [20-02]).
  `DiagnosticGateBanner` import — строка 11. `DiagnosticHint` (строка 12) — ДРУГОЙ компонент, не затрагивается.

UI-SPEC раздел «Хинт-карточка диагностики на уроке» — авторитет по визуалу хинта.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Переделать DiagnosticGateBanner в закрываемый хинт + снять жёсткий гейт урока</name>
  <read_first>
    - apps/web/src/components/learning/DiagnosticGateBanner.tsx — текущий блокирующий баннер (полный файл, ~32 строки)
    - apps/web/src/app/(main)/learn/[id]/page.tsx — строки 641-645 (тернарник гейта), строка 302 (`hasDiagnostic`), строка 11 (импорт)
    - .planning/phases/56-entry-flow-redesign/56-PATTERNS.md — разделы «DiagnosticGateBanner.tsx (repurpose)» и «learn/[id]/page.tsx — lines 641-645»
    - .planning/phases/56-entry-flow-redesign/56-UI-SPEC.md — раздел «Хинт-карточка диагностики на уроке», Copywriting Contract (строки хинта)
  </read_first>
  <action>
    Переделать `apps/web/src/components/learning/DiagnosticGateBanner.tsx` (НЕ удалять — импортируется в page.tsx:11): из блокирующего `py-12` баннера в компактную закрываемую хинт-карточку. `Card` с `border-l-4 border-mp-blue-500 bg-mp-blue-50`, padding `p-4`. Раскладка `flex items-start gap-3`: `lucide Sparkles` (`text-mp-blue-600`) слева, текст в центре, кнопка-закрытие справа. Заголовок `text-heading-sm` «Пройди диагностику — соберём персональный трек», подтекст `text-body-sm text-mp-gray-600` «Точная карта навыков и уроки под ваши слабые места.», inline-CTA `Button variant="link"` size sm «Пройти →» (ссылка на `/diagnostic`). Кнопка-закрытие `Button variant="ghost" size="icon"` с `lucide X`, `aria-label="Закрыть подсказку"`. Заменить inline-SVG на `lucide-react`.
    Dismissal: `useState` + `useEffect` читают `localStorage` флаг `diagnosticHintDismissed`; если `=== 'true'` — компонент возвращает `null`. Кнопка-закрытие ставит флаг и скрывает карточку.
    Правка `apps/web/src/app/(main)/learn/[id]/page.tsx` строки ~641-645: убрать ветку `hasDiagnostic === false ? <DiagnosticGateBanner/> :` из тернарника. Урок рендерится всегда, кроме `lesson.locked`. Перенести `<DiagnosticGateBanner/>` внутрь левой колонки НАД `<Card>` плеера, обёрнутым в `{hasDiagnostic === false && <DiagnosticGateBanner />}`. Ветку `lesson.locked → <LockOverlay/>` НЕ трогать.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `DiagnosticGateBanner.tsx` — компактная карточка с `border-l-4 border-mp-blue-500`, padding `p-4` (grep `py-12` — 0 совпадений)
    - Содержит кнопку-закрытие с `aria-label="Закрыть подсказку"` и `lucide X`; иконка-искра `lucide Sparkles` (grep inline `<svg` — 0 совпадений)
    - Читает/пишет `localStorage` ключ `diagnosticHintDismissed`; возвращает `null` когда флаг установлен
    - `learn/[id]/page.tsx` — тернарник не содержит ветку `hasDiagnostic === false ? <DiagnosticGateBanner` (блокирующая ветка убрана)
    - `<DiagnosticGateBanner/>` рендерится над плеером через `hasDiagnostic === false &&`
    - Ветка `lesson.locked → <LockOverlay/>` присутствует без изменений
    - `pnpm --filter web typecheck` проходит
  </acceptance_criteria>
  <done>Жёсткий гейт снят: урок доступен всем кроме платного; хинт-карточка ненавязчива и закрывается навсегда.</done>
</task>

<task type="auto">
  <name>Task 2: Секция редактирования квалификации в /profile</name>
  <read_first>
    - apps/web/src/app/(main)/profile/page.tsx — паттерн секций профиля; `updateProfile` mutation, `useState` для name (строки ~216, 272-274), структура Card-секций
    - apps/web/src/components/welcome/options.ts — GOAL_OPTIONS / MARKETPLACE_OPTIONS / EXPERIENCE_OPTIONS (план 56-03)
    - apps/web/src/components/welcome/StepMarketplaces.tsx, StepExperience.tsx, StepIntent.tsx — переиспользуемые UI-паттерны чипов/карточек
    - .planning/phases/56-entry-flow-redesign/56-UI-SPEC.md — Color/Typography/Copywriting (значения переиспользуют те же наборы)
  </read_first>
  <action>
    Добавить в `apps/web/src/app/(main)/profile/page.tsx` новую `Card`-секцию «Квалификация» (или «О вашем бизнесе»). Загрузить текущие значения через `trpc.onboarding.getState.useQuery()`. Рендерить редактируемые контролы для `marketplaces` (multi-select чипы/карточки из `MARKETPLACE_OPTIONS`), `experienceLevel` (single-select из `EXPERIENCE_OPTIONS`), `goals` (multi-select чипы из `GOAL_OPTIONS`), `goalText` (`Textarea`). Локальный `useState` для редактируемых значений, инициализируемый из `getState`.
    Кнопка «Сохранить» вызывает `trpc.onboarding.complete.useMutation()` с `{ marketplaces, experienceLevel, goals, goalText }`. На успех — `toast.success` (или существующий паттерн профиля), на ошибку — `toast.error`. После сохранения инвалидировать `onboarding.getState` query.
    Переиспользовать существующие welcome-компоненты или их визуальные паттерны (чипы/карточки) — НЕ дублировать стили. Все строки — русские. Заметка: `complete` повторно ставит `onboardingCompletedAt = now()` — это безопасно (пользователь уже прошёл онбординг), но допустимо.
  </action>
  <verify>
    <automated>pnpm --filter web typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `profile/page.tsx` содержит новую секцию с контролами для `marketplaces`, `experienceLevel`, `goals`, `goalText`
    - Секция загружает значения через `trpc.onboarding.getState`
    - Кнопка «Сохранить» вызывает `trpc.onboarding.complete` с 4 полями квалификации
    - При успехе/ошибке показывается toast; `onboarding.getState` инвалидируется после сохранения
    - Контролы переиспользуют `GOAL_OPTIONS`/`MARKETPLACE_OPTIONS`/`EXPERIENCE_OPTIONS` (нет хардкода списков)
    - Все user-facing строки — русские
    - `pnpm --filter web typecheck` проходит
  </acceptance_criteria>
  <done>В /profile есть рабочая секция квалификации: значения подгружаются, редактируются, сохраняются через onboarding.complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → tRPC onboarding (из /profile) | Повторная отправка квалификации при редактировании |
| client → localStorage | Хранение dismissal-флага хинта (не security-критично) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-12 | Elevation of Privilege | редактирование чужого UserProfile из /profile | mitigate | `onboarding.complete` (план 56-02) использует `protectedProcedure` + `where: { id: ctx.user.id }` — редактирование возможно только своего профиля, независимо от вызова из /welcome или /profile. |
| T-56-13 | Tampering | подписочный гейт обойдён при де-гейтинге | mitigate | Удаляется ТОЛЬКО ветка `hasDiagnostic === false`; ветка `lesson.locked → <LockOverlay/>` остаётся нетронутой (acceptance criteria Task 1 это проверяет). Подписочный гейт независим (STATE [20-02]). |
| T-56-14 | Tampering (XSS) | goalText рендеринг в /profile | mitigate | `goalText` рендерится в `Textarea` value / как текст — React-эскейпинг, без `dangerouslySetInnerHTML`. |
| T-56-15 | Tampering | манипуляция localStorage diagnosticHintDismissed | accept | Флаг управляет только показом необязательной подсказки — нет доступа к данным/функциям. Низкий риск, не security boundary. |
</threat_model>

<verification>
- `pnpm --filter web typecheck` зелёный
- Ручная проверка: урок без диагностики показывает видео + закрываемый хинт; платный урок показывает `LockOverlay`
- Ручная проверка: `/profile` → секция квалификации загружает и сохраняет значения
- E2E из плана 56-03 (`phase-56-entry-flow`) после этого плана должен покрывать де-гейтинг — при необходимости расширить там
</verification>

<success_criteria>
- Жёсткий гейт диагностики снят, подписочный `LockOverlay` сохранён
- `DiagnosticGateBanner` — закрываемый ненавязчивый хинт с localStorage-флагом
- `/profile` позволяет редактировать всю квалификацию через `onboarding`
</success_criteria>

<output>
After completion, create `.planning/phases/56-entry-flow-redesign/56-04-SUMMARY.md`
</output>
