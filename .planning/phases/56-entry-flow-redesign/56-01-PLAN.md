---
phase: 56-entry-flow-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/prisma/schema.prisma
  - packages/db/prisma/migrations/20260518000000_add_onboarding_fields/migration.sql
autonomous: false
requirements: []
must_haves:
  truths:
    - "Квалификация (marketplaces / experienceLevel / goals / goalText) может быть сохранена в UserProfile"
    - "onboardingCompletedAt существует на UserProfile как признак прохождения визарда"
    - "Существующие ~158 prod-пользователей сохраняют все данные после миграции (additive, zero data-loss)"
  artifacts:
    - path: "packages/db/prisma/schema.prisma"
      provides: "5 новых полей на model UserProfile"
      contains: "onboardingCompletedAt"
    - path: "packages/db/prisma/migrations/20260518000000_add_onboarding_fields/migration.sql"
      provides: "Additive ALTER TABLE migration для 5 колонок"
      contains: "ALTER TABLE \"UserProfile\" ADD COLUMN"
  key_links:
    - from: "schema.prisma"
      to: "Prisma Client"
      via: "pnpm db:generate"
      pattern: "onboardingCompletedAt"
    - from: "migration.sql"
      to: "prod Supabase UserProfile"
      via: "manual ALTER on prod before rebuild"
      pattern: "ADD COLUMN"
---

<objective>
Добавить 5 новых полей квалификации на модель `UserProfile` и применить их к prod-БД безопасной additive-миграцией.

Purpose: Эти поля — фундамент всей фазы. `onboarding` router (план 02) пишет в них, гард `(main)`-layout (план 03) читает `onboardingCompletedAt`, `/profile` (план 04) их редактирует. Без них typecheck/build падает у всех downstream-планов.
Output: изменённый `schema.prisma`, новый каталог миграции, регенерированный Prisma Client, применённая на prod миграция.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-entry-flow-redesign/56-RESEARCH.md
@.planning/phases/56-entry-flow-redesign/56-PATTERNS.md

# CRITICAL: PROD DATABASE SAFETY — прочитать перед любой работой с БД
@MAAL/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Добавить 5 полей квалификации в model UserProfile</name>
  <read_first>
    - packages/db/prisma/schema.prisma — текущая model UserProfile; строка ~34 `toursCompleted String[] @default([])` — прецедент array-поля
    - .planning/phases/56-entry-flow-redesign/56-PATTERNS.md — раздел «schema.prisma — UserProfile +5 fields», точные типы и комментарии
    - .planning/phases/56-entry-flow-redesign/56-CONTEXT.md — раздел Data Model: стабильные строковые ключи
  </read_first>
  <action>
    В `packages/db/prisma/schema.prisma`, внутри `model UserProfile`, сразу после поля `toursCompleted`, добавить 5 полей:
    `onboardingCompletedAt DateTime?` (комментарий: null = показать визард; now() при прохождении развилки),
    `marketplaces String[] @default([])` (комментарий: WB|OZON|YANDEX|ALIEXPRESS|MEGAMARKET|OWN_SHOP|OTHER),
    `experienceLevel String?` (комментарий: PROSPECTING|BEGINNER|STABLE|ADVANCED),
    `goals String[] @default([])` (комментарий: SALES|ADS|CONTENT|ANALYTICS|OPERATIONS|FINANCE|NEW_MARKETPLACE),
    `goalText String?` (комментарий: свободный текст с шага 1).
    Использовать `String[]` (НЕ Prisma enum) — locked решение CONTEXT.md, повторяет `toursCompleted`.
    После правки запустить `pnpm db:generate` для регенерации Prisma Client. НЕ запускать `prisma db push` и НЕ запускать `prisma migrate` против БД в этой задаче — это делает Task 2.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `schema.prisma` содержит ровно 5 новых полей в `model UserProfile`: `onboardingCompletedAt`, `marketplaces`, `experienceLevel`, `goals`, `goalText`
    - `onboardingCompletedAt` имеет тип `DateTime?`; `experienceLevel` и `goalText` — `String?`; `marketplaces` и `goals` — `String[] @default([])`
    - `pnpm db:generate` завершается без ошибок; Prisma Client знает о новых полях
    - `pnpm typecheck` проходит (ни одно поле не NOT NULL без default — additive)
  </acceptance_criteria>
  <done>Все 5 полей объявлены с корректными типами, Prisma Client регенерирован, typecheck зелёный.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Применить additive-миграцию на prod Supabase</name>
  <read_first>
    - packages/db/prisma/migrations/20260512000000_add_lesson_metadata/migration.sql — паттерн ручного ALTER на prod (одна строка ADD COLUMN + комментарий)
    - MAAL/CLAUDE.md — раздел «PROD DATABASE SAFETY»: zero-exception правила, recovery procedure
    - .planning/phases/56-entry-flow-redesign/56-RESEARCH.md — Pitfall 1 (data-loss) и Pitfall 2 (migration перед rebuild)
  </read_first>
  <what-built>
    Claude автоматизировал: создал каталог `packages/db/prisma/migrations/20260518000000_add_onboarding_fields/` с файлом `migration.sql`, содержащим 5 additive-операторов:
    `ALTER TABLE "UserProfile" ADD COLUMN "onboardingCompletedAt" TIMESTAMP(3);`
    `ALTER TABLE "UserProfile" ADD COLUMN "marketplaces" TEXT[] DEFAULT ARRAY[]::TEXT[];`
    `ALTER TABLE "UserProfile" ADD COLUMN "experienceLevel" TEXT;`
    `ALTER TABLE "UserProfile" ADD COLUMN "goals" TEXT[] DEFAULT ARRAY[]::TEXT[];`
    `ALTER TABLE "UserProfile" ADD COLUMN "goalText" TEXT;`
    Файл начинается комментарием: цель + «Nullable / DEFAULT — additive, backwards-compatible, zero data-loss».
    Все 5 колонок nullable или с DEFAULT — это безопасный additive-класс изменения.
  </what-built>
  <how-to-verify>
    Применить миграцию вручную на prod-БД через Supabase SQL-путь — НЕ через `prisma db push`, НИКОГДА с `--accept-data-loss`.
    1. Сделать свежий backup prod-БД (Supabase PITR должен быть включён; проверить или снять snapshot).
    2. Убедиться, что цель — prod ref `saecuecevicwjkpmaoot` И что операция выполняется ТОЛЬКО из MAAL-репозитория.
    3. Выполнить 5 операторов `ALTER TABLE` из `migration.sql` на prod Supabase через Supabase SQL Editor / Management API query endpoint.
    4. Вставить строку в таблицу `_prisma_migrations` с `migration_name = '20260518000000_add_onboarding_fields'`, `applied = 1`, `finished_at = now()` (паттерн ручного INSERT, как при R1-восстановлении Lesson.order — см. CLAUDE.md Last Session).
    5. Проверить колонки: `SELECT column_name FROM information_schema.columns WHERE table_name = 'UserProfile' AND column_name IN ('onboardingCompletedAt','marketplaces','experienceLevel','goals','goalText');` — должно вернуть 5 строк.
    Миграция применяется на prod ДО docker rebuild (recurring lesson Phase 28). Docker rebuild — отдельный шаг деплоя в конце фазы, НЕ в этой задаче.
  </how-to-verify>
  <resume-signal>Напишите "applied" после подтверждения 5 колонок на prod, или опишите проблему</resume-signal>
  <acceptance_criteria>
    - Каталог `packages/db/prisma/migrations/20260518000000_add_onboarding_fields/migration.sql` существует с 5 операторами `ALTER TABLE "UserProfile" ADD COLUMN`
    - `migration.sql` НЕ содержит `DROP`, `--accept-data-loss`, `prisma db push`
    - На prod `information_schema.columns` для таблицы `UserProfile` возвращает все 5 новых колонок
    - Строка `20260518000000_add_onboarding_fields` присутствует в `_prisma_migrations`
    - Существующие пользователи UserProfile не потеряны (count до = count после)
  </acceptance_criteria>
  <done>Миграция применена на prod, 5 колонок подтверждены через information_schema, _prisma_migrations синхронизирована, данные пользователей целы.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → prod Supabase | DDL-операция против live production БД (158 платящих пользователей) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-01 | Denial of Service | prod UserProfile таблица | mitigate | Миграция чисто additive — 5 nullable/DEFAULT колонок, без DROP/ALTER существующих. Запуск только из MAAL-репо (декларирует все 24+ таблицы), никогда `--accept-data-loss`. Прецедент инцидента 2026-05-12 зафиксирован в CLAUDE.md. |
| T-56-02 | Tampering | prod данные при миграции | mitigate | Свежий backup (Supabase PITR) перед DDL; verify count до/после; recovery procedure в CLAUDE.md при сбое. |
| T-56-03 | Repudiation | _prisma_migrations несинхронизирована | accept | Ручной INSERT строки миграции — established pattern проекта (R1 Lesson.order recovery). Низкий риск: расхождение видно при следующем `prisma migrate status`. |
</threat_model>

<verification>
- `pnpm typecheck` зелёный после `pnpm db:generate`
- На prod Supabase таблица `UserProfile` содержит 5 новых колонок (information_schema проверка)
- Count строк UserProfile до и после миграции совпадает
</verification>

<success_criteria>
- 5 полей объявлены в `schema.prisma` с корректными типами
- Каталог миграции создан с additive `migration.sql`
- Миграция применена на prod без потери данных
- Prisma Client регенерирован, downstream-планы могут типизироваться против новых полей
</success_criteria>

<output>
After completion, create `.planning/phases/56-entry-flow-redesign/56-01-SUMMARY.md`
</output>
