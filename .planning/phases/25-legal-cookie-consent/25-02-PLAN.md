---
phase: 25-legal-cookie-consent
plan: 02
type: execute
wave: 2
depends_on: ["25-01"]
files_modified:
  - apps/web/src/app/(auth)/register/page.tsx
  - apps/web/src/components/shared/CookieConsent.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/components/ui/checkbox.tsx
autonomous: true
requirements: [LEGAL-03, LEGAL-04]

must_haves:
  truths:
    - "Форма регистрации содержит 3 чекбокса: оферта (обязательный), ПДн (обязательный), рассылка (опциональный)"
    - "Регистрация невозможна без обязательных чекбоксов (кнопка disabled)"
    - "Cookie consent баннер появляется при первом визите на любой странице"
    - "Выбор cookies сохраняется в localStorage и баннер больше не показывается"
    - "Кнопка Настроить раскрывает категории: необходимые (disabled on), аналитика, маркетинг"
  artifacts:
    - path: "apps/web/src/app/(auth)/register/page.tsx"
      provides: "Registration form with 3 legal checkboxes"
      contains: "acceptOffer"
    - path: "apps/web/src/components/shared/CookieConsent.tsx"
      provides: "Cookie consent banner with category toggles"
      min_lines: 80
    - path: "apps/web/src/components/ui/checkbox.tsx"
      provides: "shadcn/ui Checkbox component"
      min_lines: 15
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root layout with CookieConsent component"
      contains: "CookieConsent"
  key_links:
    - from: "apps/web/src/app/(auth)/register/page.tsx"
      to: "/legal/offer, /legal/pdn, /legal/adv"
      via: "Link components in checkbox labels"
      pattern: "href=./legal/"
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/src/components/shared/CookieConsent.tsx"
      via: "Component import in root layout"
      pattern: "CookieConsent"
    - from: "apps/web/src/components/shared/CookieConsent.tsx"
      to: "localStorage cookie_consent"
      via: "Read/write consent preferences"
      pattern: "localStorage.*cookie_consent"
---

<objective>
Add 3 legal checkboxes to the registration form and a cookie consent banner to the root layout.

Purpose: Legal compliance -- users must explicitly accept the offer and PD consent before registration (per D-06, D-07). Cookie consent banner informs users about tracking cookies and lets them opt out (per D-09, D-10, D-11, D-12).
Output: Updated registration form with consent checkboxes, CookieConsent banner component in root layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/25-legal-cookie-consent/25-CONTEXT.md
@.planning/phases/25-legal-cookie-consent/25-01-SUMMARY.md
@apps/web/src/app/(auth)/register/page.tsx
@apps/web/src/app/layout.tsx
</context>

<interfaces>
<!-- From register/page.tsx: current form structure -->
<!-- Form uses `action={handleSubmit}` with FormData, has useState for error/loading -->
<!-- Submit button at line 129: <Button type="submit" className="w-full" disabled={loading}> -->
<!-- Need to add checkboxes BEFORE this button, and add disabled condition for required checkboxes -->

<!-- From layout.tsx: root layout structure -->
<!-- Body contains: LandingThemeProvider > TRPCProvider > children, then Toaster, then YandexMetrika -->
<!-- CookieConsent should be added after Toaster, before closing </body> -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Checkbox + registration form checkboxes</name>
  <files>
    apps/web/src/components/ui/checkbox.tsx
    apps/web/src/app/(auth)/register/page.tsx
  </files>
  <action>
    **1. Install shadcn/ui Checkbox component** (per D-06):
    - Run `npx shadcn@latest add checkbox` in `apps/web/` directory
    - If that fails, create manually: `apps/web/src/components/ui/checkbox.tsx` using Radix `@radix-ui/react-checkbox` (already a dependency via other shadcn components, check package.json)
    - If `@radix-ui/react-checkbox` is not installed, run `pnpm add @radix-ui/react-checkbox` in apps/web
    - Standard shadcn checkbox with Tailwind styling, mp-blue-600 checked state

    **2. Modify `register/page.tsx`** (per D-06, D-07, D-08):

    Add 3 state variables after existing useState declarations:
    ```typescript
    const [acceptOffer, setAcceptOffer] = useState(false);
    const [acceptPdn, setAcceptPdn] = useState(false);
    const [acceptAdv, setAcceptAdv] = useState(false);
    ```

    Compute `canSubmit`:
    ```typescript
    const canSubmit = acceptOffer && acceptPdn && !loading;
    ```

    Add checkbox group INSIDE the `<form>` element, AFTER the confirmPassword field (line ~127) and BEFORE the submit button (line ~129). Structure:

    ```tsx
    <div className="space-y-3 pt-2">
      <label className="flex items-start gap-2 cursor-pointer">
        <Checkbox
          checked={acceptOffer}
          onCheckedChange={(v) => setAcceptOffer(v === true)}
          disabled={loading}
          className="mt-0.5"
        />
        <span className="text-sm text-gray-600 leading-tight">
          Я принимаю условия{' '}
          <Link href="/legal/offer" target="_blank" className="text-blue-600 hover:underline">
            оферты
          </Link>
          <span className="text-red-500 ml-0.5">*</span>
        </span>
      </label>

      <label className="flex items-start gap-2 cursor-pointer">
        <Checkbox
          checked={acceptPdn}
          onCheckedChange={(v) => setAcceptPdn(v === true)}
          disabled={loading}
          className="mt-0.5"
        />
        <span className="text-sm text-gray-600 leading-tight">
          Я согласен на{' '}
          <Link href="/legal/pdn" target="_blank" className="text-blue-600 hover:underline">
            обработку персональных данных
          </Link>
          <span className="text-red-500 ml-0.5">*</span>
        </span>
      </label>

      <label className="flex items-start gap-2 cursor-pointer">
        <Checkbox
          checked={acceptAdv}
          onCheckedChange={(v) => setAcceptAdv(v === true)}
          disabled={loading}
          className="mt-0.5"
        />
        <span className="text-sm text-gray-600 leading-tight">
          Я согласен на получение{' '}
          <Link href="/legal/adv" target="_blank" className="text-blue-600 hover:underline">
            рекламных материалов
          </Link>
        </span>
      </label>

      <p className="text-xs text-gray-400">
        <span className="text-red-500">*</span> — обязательные поля
      </p>
    </div>
    ```

    Update submit button to use `canSubmit`:
    ```tsx
    <Button type="submit" className="w-full" disabled={!canSubmit}>
    ```

    **Per D-08:** Pass `acceptAdv` state in the signUp call for future CQ `pa_adv_consent` tracking. Add it to FormData before calling signUp:
    ```typescript
    // Inside handleSubmit, before signUp call:
    formData.set('adv_consent', acceptAdv ? 'true' : 'false');
    ```
    The server action `signUp` in `lib/auth/actions.ts` does not need modification now -- the FormData param is there but unused. This prepares for future CQ integration where `pa_adv_consent` prop will be set.

    Import Checkbox from `@/components/ui/checkbox`.
    Import Link (already imported).

    **Do NOT add checkboxes to Yandex OAuth flow** -- OAuth users accept terms by clicking the button (implicit consent). This matches the offer text: "регистрация Заказчика на сайте" is the acceptance action.
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Registration form shows 3 checkboxes with links to legal pages
    - Required checkboxes (offer, PD) have red asterisk
    - Submit button is disabled until both required checkboxes are checked
    - Optional advertising checkbox does not affect submit availability
    - Links open legal pages in new tab
    - adv_consent value is passed in FormData
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CookieConsent banner + add to root layout</name>
  <files>
    apps/web/src/components/shared/CookieConsent.tsx
    apps/web/src/app/layout.tsx
  </files>
  <action>
    **Per D-09, D-10, D-11, D-12:** Create cookie consent banner component.

    **1. Create `CookieConsent.tsx`** — client component ('use client'):

    State management:
    ```typescript
    const [visible, setVisible] = useState(false);
    const [showSettings, setShowSettings] = useState(false);
    const [analytics, setAnalytics] = useState(true);   // Яндекс Метрика — default on
    const [marketing, setMarketing] = useState(true);    // Carrot Quest — default on
    ```

    On mount (useEffect), check localStorage for `cookie_consent`:
    ```typescript
    useEffect(() => {
      const consent = localStorage.getItem('cookie_consent');
      if (!consent) {
        // Small delay to avoid layout shift on page load
        const timer = setTimeout(() => setVisible(true), 1000);
        return () => clearTimeout(timer);
      }
    }, []);
    ```

    Functions:
    - `acceptAll()`: save `{ necessary: true, analytics: true, marketing: true, timestamp: Date.now() }` to localStorage as `cookie_consent`, setVisible(false)
    - `savePreferences()`: save `{ necessary: true, analytics, marketing, timestamp: Date.now() }` to localStorage, setVisible(false)
    - `rejectOptional()`: save `{ necessary: true, analytics: false, marketing: false, timestamp: Date.now() }`, setVisible(false)

    Render (when `visible` is true):
    ```tsx
    <div className="fixed bottom-0 left-0 right-0 z-[9999] p-4 animate-slide-up">
      <div className="max-w-2xl mx-auto bg-white rounded-2xl shadow-2xl border border-gray-200 p-5 sm:p-6">
        <div className="flex items-start gap-3 mb-4">
          <div className="text-2xl">🍪</div>
          <div>
            <h3 className="font-semibold text-gray-900 text-sm">Мы используем cookies</h3>
            <p className="text-xs text-gray-500 mt-1 leading-relaxed">
              Для корректной работы сайта, аналитики и персонализации. Подробнее в{' '}
              <Link href="/legal/cookies" className="text-blue-600 hover:underline">
                политике cookies
              </Link>.
            </p>
          </div>
        </div>

        {/* Settings panel — shown when "Настроить" is clicked */}
        {showSettings && (
          <div className="border-t border-gray-100 pt-4 mb-4 space-y-3">
            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-900">Необходимые</p>
                <p className="text-xs text-gray-400">Авторизация, безопасность</p>
              </div>
              <div className="text-xs text-gray-400 bg-gray-100 px-2 py-1 rounded">Всегда вкл</div>
            </div>

            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-900">Аналитика</p>
                <p className="text-xs text-gray-400">Яндекс Метрика</p>
              </div>
              {/* Toggle switch for analytics */}
            </div>

            <div className="flex items-center justify-between">
              <div>
                <p className="text-sm font-medium text-gray-900">Маркетинг</p>
                <p className="text-xs text-gray-400">Carrot Quest</p>
              </div>
              {/* Toggle switch for marketing */}
            </div>
          </div>
        )}

        {/* Action buttons */}
        <div className="flex flex-col sm:flex-row gap-2">
          <Button onClick={acceptAll} className="flex-1 bg-mp-blue-600 hover:bg-mp-blue-700 text-white text-sm h-9 rounded-lg">
            Принять все
          </Button>
          {!showSettings ? (
            <Button variant="outline" onClick={() => setShowSettings(true)} className="flex-1 text-sm h-9 rounded-lg">
              Настроить
            </Button>
          ) : (
            <Button variant="outline" onClick={savePreferences} className="flex-1 text-sm h-9 rounded-lg">
              Сохранить выбор
            </Button>
          )}
        </div>
      </div>
    </div>
    ```

    For the toggle switches, use a simple `<button>` with conditional styling (rounded pill, bg-mp-blue-600 when on, bg-gray-300 when off, with a sliding circle). Do NOT use Checkbox here -- cookie toggles are traditionally toggle switches. Simple implementation:
    ```tsx
    function Toggle({ checked, onChange }: { checked: boolean; onChange: (v: boolean) => void }) {
      return (
        <button
          type="button"
          role="switch"
          aria-checked={checked}
          onClick={() => onChange(!checked)}
          className={cn(
            'relative inline-flex h-5 w-9 items-center rounded-full transition-colors',
            checked ? 'bg-mp-blue-600' : 'bg-gray-300'
          )}
        >
          <span className={cn(
            'inline-block h-3.5 w-3.5 rounded-full bg-white transition-transform',
            checked ? 'translate-x-4' : 'translate-x-0.5'
          )} />
        </button>
      );
    }
    ```

    Add CSS keyframe for slide-up animation if not already in globals.css (check first — it may exist from Sprint 2.5 animations). If `animate-slide-up` doesn't exist, add inline style or use `animate-fade-in` which exists.

    **NOTE on conditional loading (per D-12):** The cookie consent component saves preferences but does NOT need to conditionally load/block Яндекс Метрика or CQ scripts in this phase. The scripts are already loaded in layout.tsx. Conditional blocking based on consent is a future enhancement (deferred per CONTEXT.md). The consent banner is primarily for legal compliance (informing users) rather than technical blocking at this stage.

    **2. Add CookieConsent to root layout** (`apps/web/src/app/layout.tsx`):
    - Import: `import { CookieConsent } from '@/components/shared/CookieConsent'`
    - Add `<CookieConsent />` after `<Toaster />` and before the YandexMetrika conditional block
    - The component is a client component but will be tree-shaken appropriately by Next.js
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Cookie consent banner appears at bottom of page on first visit (no cookie_consent in localStorage)
    - "Принять все" saves full consent and hides banner permanently
    - "Настроить" expands category toggles (necessary always on, analytics and marketing toggleable)
    - "Сохранить выбор" saves selected preferences and hides banner
    - Banner does not appear on subsequent visits (localStorage persists)
    - Banner works on all pages (landing, auth, main layout)
    - Registration form + cookie consent both build cleanly
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds without errors
2. Registration page shows 3 checkboxes, submit disabled until required ones checked
3. Cookie consent banner appears on first visit, disappears after acceptance
4. localStorage `cookie_consent` contains JSON with consent preferences
5. Legal page links in checkboxes open in new tab
</verification>

<success_criteria>
- Registration form enforces acceptance of offer and PD consent before allowing signup
- Cookie consent banner provides informed consent UI with category controls
- All components build without TypeScript errors
- Consent state persists across page reloads via localStorage
</success_criteria>

<output>
After completion, create `.planning/phases/25-legal-cookie-consent/25-02-SUMMARY.md`
</output>
