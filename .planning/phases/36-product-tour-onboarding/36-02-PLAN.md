---
phase: 36-product-tour-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - apps/web/src/app/(main)/dashboard/page.tsx
  - apps/web/src/app/(main)/learn/page.tsx
  - apps/web/src/app/(main)/learn/[id]/page.tsx
autonomous: false
requirements:
  - TOUR-02

must_haves:
  truths:
    - "Dashboard page has 3 data-tour attributes on correct cards"
    - "Learn page has 5 data-tour attributes on search, filters, toggle, sections, add-to-track"
    - "Lesson page has 5 data-tour attributes on video, summary, chat, comments, nav"
    - "All 3 tours launch correctly on first visit with 1.5s delay"
    - "HelpCircle button replays tour on each page"
    - "Tour popover shows MPSTATS brand styling (white bg, mp-blue CTA)"
    - "Mobile dashboard tour targets mobile-nav instead of sidebar"
  artifacts:
    - path: "apps/web/src/app/(main)/dashboard/page.tsx"
      provides: "Dashboard tour target attributes"
      contains: "data-tour=\"dashboard-diagnostic-cta\""
    - path: "apps/web/src/app/(main)/learn/page.tsx"
      provides: "Learn tour target attributes"
      contains: "data-tour=\"learn-search\""
    - path: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      provides: "Lesson tour target attributes"
      contains: "data-tour=\"lesson-video\""
  key_links:
    - from: "dashboard/page.tsx"
      to: "definitions.ts dashboardSteps"
      via: "data-tour attribute selectors"
      pattern: "data-tour="
    - from: "learn/page.tsx"
      to: "definitions.ts learnSteps"
      via: "data-tour attribute selectors"
      pattern: "data-tour="
    - from: "learn/[id]/page.tsx"
      to: "definitions.ts lessonSteps"
      via: "data-tour attribute selectors"
      pattern: "data-tour="
---

<objective>
Add data-tour attributes to all 3 tour target pages (Dashboard, Learn, Lesson) so driver.js can highlight the correct elements. Verify full tour flow end-to-end.

Purpose: Complete the tour wiring — Plan 01 built the engine, this plan adds the targets.
Output: All 13 data-tour attributes placed on correct DOM elements across 3 pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-product-tour-onboarding/36-CONTEXT.md
@.planning/phases/36-product-tour-onboarding/36-UI-SPEC.md
@.planning/phases/36-product-tour-onboarding/36-01-SUMMARY.md

<interfaces>
<!-- Tour step definitions (from Plan 01) — these are the selectors that need matching DOM elements -->
From apps/web/src/lib/tours/definitions.ts:
```typescript
// Dashboard steps target:
// [data-tour="dashboard-diagnostic-cta"] — the "Пройти диагностику" Card
// [data-tour="dashboard-skill-radar"] — the "Профиль навыков" Card
// [data-tour="dashboard-learn-cta"] — the "Продолжить обучение" Card

// Learn steps target:
// [data-tour="learn-search"] — SearchBar wrapper
// [data-tour="learn-filters"] — FilterPanel wrapper
// [data-tour="learn-view-toggle"] — path/courses toggle buttons
// [data-tour="learn-sections"] — first section accordion
// [data-tour="learn-add-to-track"] — first visible "+" button

// Lesson steps target:
// [data-tour="lesson-video"] — video player container
// [data-tour="lesson-summary"] — CollapsibleSummary wrapper
// [data-tour="lesson-chat"] — chat sidebar Card
// [data-tour="lesson-comments"] — comments section (Phase 35)
// [data-tour="lesson-nav"] — prev/next navigation container
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-tour attributes to Dashboard and Learn pages</name>
  <files>
    apps/web/src/app/(main)/dashboard/page.tsx,
    apps/web/src/app/(main)/learn/page.tsx
  </files>
  <read_first>
    apps/web/src/app/(main)/dashboard/page.tsx,
    apps/web/src/app/(main)/learn/page.tsx,
    apps/web/src/lib/tours/definitions.ts
  </read_first>
  <action>
1. Edit `apps/web/src/app/(main)/dashboard/page.tsx`:
   - Find the Card/section that contains the "Пройти диагностику" CTA (look for text "диагностик" or Link to "/diagnostic") — add `data-tour="dashboard-diagnostic-cta"` to its wrapper element (the Card or enclosing div)
   - Find the Card/section showing "Профиль навыков" or RadarChart — add `data-tour="dashboard-skill-radar"` to its wrapper
   - Find the Card/section with "Продолжить обучение" CTA or Link to "/learn" — add `data-tour="dashboard-learn-cta"` to its wrapper
   - Do NOT change any existing classNames or structure — only ADD the data-tour attribute to existing elements

2. Edit `apps/web/src/app/(main)/learn/page.tsx`:
   - Find the search input/bar wrapper (look for `<input` with search or `SearchBar` component) — add `data-tour="learn-search"` to its wrapper div
   - Find the filter panel wrapper (look for `FilterPanel` or filter controls container) — add `data-tour="learn-filters"` to its wrapper div
   - Find the view mode toggle (buttons for "Мой трек" / "Все курсы" or path/courses toggle) — add `data-tour="learn-view-toggle"` to the toggle container div
   - Find the first section accordion or section list (where error/recommendation sections render) — add `data-tour="learn-sections"` to the sections container or first accordion wrapper
   - Find the first "+" add-to-track button or its container — add `data-tour="learn-add-to-track"` to a representative add button or its wrapper. If the button is rendered per-card, add the attribute to the FIRST LessonCard's add button wrapper or to the card grid container. Prefer adding it to a wrapper div around the first card that has a "+" button.
   - NOTE: The learn page is likely complex with conditional rendering. Place data-tour on stable wrapper elements that are always present, even if their children vary.
  </action>
  <verify>
    <automated>grep -c 'data-tour=' apps/web/src/app/\(main\)/dashboard/page.tsx && grep -c 'data-tour=' apps/web/src/app/\(main\)/learn/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/page.tsx` contains `data-tour="dashboard-diagnostic-cta"`
    - `dashboard/page.tsx` contains `data-tour="dashboard-skill-radar"`
    - `dashboard/page.tsx` contains `data-tour="dashboard-learn-cta"`
    - `learn/page.tsx` contains `data-tour="learn-search"`
    - `learn/page.tsx` contains `data-tour="learn-filters"`
    - `learn/page.tsx` contains `data-tour="learn-view-toggle"`
    - `learn/page.tsx` contains `data-tour="learn-sections"`
    - `learn/page.tsx` contains `data-tour="learn-add-to-track"`
    - grep count: dashboard has exactly 3 data-tour attributes, learn has exactly 5
  </acceptance_criteria>
  <done>Dashboard page has 3 data-tour attributes on diagnostic CTA, skill radar, and learn CTA cards. Learn page has 5 data-tour attributes on search, filters, toggle, sections, and add-to-track elements.</done>
</task>

<task type="auto">
  <name>Task 2: Add data-tour attributes to Lesson page</name>
  <files>
    apps/web/src/app/(main)/learn/[id]/page.tsx
  </files>
  <read_first>
    apps/web/src/app/(main)/learn/[id]/page.tsx,
    apps/web/src/lib/tours/definitions.ts
  </read_first>
  <action>
1. Edit `apps/web/src/app/(main)/learn/[id]/page.tsx`:
   - Find the video player container (look for KinescopePlayer or `#video-player` or iframe wrapper) — add `data-tour="lesson-video"` to its wrapper div
   - Find the AI summary section (look for CollapsibleSummary component or summary tab/panel) — add `data-tour="lesson-summary"` to its wrapper
   - Find the AI chat section (look for chat Card or chat input area) — add `data-tour="lesson-chat"` to its wrapper Card/div
   - Find the comments section (Phase 35 component — look for Comments or LessonComments component) — add `data-tour="lesson-comments"` to its wrapper. If Phase 35 is not yet integrated, add the attribute to a placeholder div: `<div data-tour="lesson-comments"></div>` (driver.js will skip missing targets gracefully)
   - Find the prev/next navigation buttons (look for Link to previous/next lesson or navigation container) — add `data-tour="lesson-nav"` to the navigation container div
   - Do NOT change any existing structure — only ADD data-tour attributes
  </action>
  <verify>
    <automated>grep -c 'data-tour=' apps/web/src/app/\(main\)/learn/\[id\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `learn/[id]/page.tsx` contains `data-tour="lesson-video"`
    - `learn/[id]/page.tsx` contains `data-tour="lesson-summary"`
    - `learn/[id]/page.tsx` contains `data-tour="lesson-chat"`
    - `learn/[id]/page.tsx` contains `data-tour="lesson-comments"`
    - `learn/[id]/page.tsx` contains `data-tour="lesson-nav"`
    - grep count: lesson page has exactly 5 data-tour attributes
  </acceptance_criteria>
  <done>Lesson page has 5 data-tour attributes on video, summary, chat, comments, and navigation elements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all 3 tours end-to-end</name>
  <what-built>
    3 tooltip tours (Dashboard, Learn, Lesson) with driver.js. Auto-start on first visit (1.5s delay), skip/complete saves to localStorage, HelpCircle button in header to replay.
  </what-built>
  <how-to-verify>
    1. Clear localStorage: open DevTools > Application > Local Storage > clear all tour_* keys
    2. Navigate to http://localhost:3000/dashboard
       - Tour should auto-start after ~1.5 seconds
       - Should see 4 steps: Навигация, Начните с диагностики, Профиль навыков, Персональный трек
       - Popover should be white with mp-blue "Далее" button, step counter "1 из 4"
       - Click through all steps, last step should say "Готово"
       - After completing, check localStorage has `tour_dashboard_completed = "true"`
    3. Navigate to http://localhost:3000/learn
       - Tour should auto-start (5 steps about search, filters, toggle, sections, track)
       - Verify popover styling matches brand
    4. Navigate to any lesson (e.g., /learn/some-lesson-id)
       - Tour should auto-start (5 steps: video, summary, chat, comments, nav)
    5. Refresh dashboard — tour should NOT auto-start (localStorage flag set)
    6. Click the ? (HelpCircle) button in header — tour should replay
    7. On mobile viewport (375px): dashboard tour step 1 should target bottom nav bar, not sidebar
    8. Navigate to /profile — HelpCircle button should NOT be visible
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `grep -r 'data-tour=' apps/web/src/app/(main)/dashboard/page.tsx` — 3 attributes
2. `grep -r 'data-tour=' apps/web/src/app/(main)/learn/page.tsx` — 5 attributes
3. `grep -r 'data-tour=' apps/web/src/app/(main)/learn/\[id\]/page.tsx` — 5 attributes
4. `grep -r 'data-tour=' apps/web/src/components/shared/sidebar.tsx` — sidebar-nav (from Plan 01)
5. `grep -r 'data-tour=' apps/web/src/components/shared/mobile-nav.tsx` — mobile-nav (from Plan 01)
6. Total: 15 data-tour attributes across 5 files (2 from Plan 01 + 13 from Plan 02)
7. `pnpm build` passes
</verification>

<success_criteria>
- All 13 page-level data-tour attributes placed on correct DOM elements
- All 3 tours complete without errors (no missing target crashes)
- Tour popover matches MPSTATS brand visually
- Tours auto-start only on first visit, HelpCircle replays on demand
- Mobile adaptation works (dashboard step 1 targets mobile-nav)
- Human verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/36-product-tour-onboarding/36-02-SUMMARY.md`
</output>
