---
phase: 36-product-tour-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/src/styles/tour.css
  - apps/web/src/lib/tours/definitions.ts
  - apps/web/src/components/shared/TourProvider.tsx
  - apps/web/src/components/shared/HelpCircleButton.tsx
  - apps/web/src/app/(main)/layout.tsx
  - apps/web/src/components/shared/sidebar.tsx
  - apps/web/src/components/shared/mobile-nav.tsx
autonomous: true
requirements:
  - TOUR-01
  - TOUR-03
  - TOUR-04

must_haves:
  truths:
    - "driver.js package installed and importable"
    - "TourProvider wraps all (main) pages via layout.tsx"
    - "HelpCircle button visible in header on /dashboard, /learn, /learn/[id]"
    - "Tour auto-starts with 1.5s delay on first visit to a page with a tour"
    - "Skip and Gotovo buttons set localStorage flag preventing re-trigger"
    - "HelpCircle click re-launches tour for current page"
    - "Tour popover styled with MPSTATS brand (mp-blue CTA, white bg, Inter font)"
  artifacts:
    - path: "apps/web/src/styles/tour.css"
      provides: "MPSTATS brand CSS overrides for driver.js"
      contains: "driver-popover"
    - path: "apps/web/src/lib/tours/definitions.ts"
      provides: "Declarative tour step arrays for 3 pages"
      exports: ["dashboardSteps", "learnSteps", "lessonSteps", "getTourForPage"]
    - path: "apps/web/src/components/shared/TourProvider.tsx"
      provides: "React context + driver.js orchestration"
      exports: ["TourProvider", "useTour"]
    - path: "apps/web/src/components/shared/HelpCircleButton.tsx"
      provides: "Header help button to replay tours"
      exports: ["HelpCircleButton"]
  key_links:
    - from: "apps/web/src/app/(main)/layout.tsx"
      to: "TourProvider"
      via: "wraps children"
      pattern: "<TourProvider>"
    - from: "HelpCircleButton"
      to: "TourProvider"
      via: "useTour context"
      pattern: "useTour"
    - from: "TourProvider"
      to: "definitions.ts"
      via: "import tour steps"
      pattern: "getTourForPage"
---

<objective>
Install driver.js, create tour infrastructure (provider, CSS, step definitions, help button), integrate into main layout, and add data-tour attributes to sidebar/mobile-nav.

Purpose: Establish all tour plumbing so Plan 02 only needs to add data-tour attributes to page components.
Output: Working TourProvider in layout, HelpCircle button in header, MPSTATS-branded popover CSS, 3 tour definitions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-product-tour-onboarding/36-CONTEXT.md
@.planning/phases/36-product-tour-onboarding/36-UI-SPEC.md

<interfaces>
<!-- Main layout (server component) — TourProvider must wrap children as client boundary -->
From apps/web/src/app/(main)/layout.tsx:
```tsx
// Server component. Header has: mobile logo | spacer | UserNav
// TourProvider + HelpCircleButton must be added here.
// HelpCircleButton goes between spacer div and <UserNav>
// TourProvider wraps {children} in the <main> area (or the entire inner div)
export default async function MainLayout({ children }: { children: React.ReactNode })
```

<!-- Sidebar — needs data-tour="sidebar-nav" on <nav> element -->
From apps/web/src/components/shared/sidebar.tsx:
```tsx
// Fixed sidebar, hidden on mobile (hidden md:flex)
// Has <nav> element with Link items
```

<!-- MobileNav — needs data-tour="mobile-nav" on root nav -->
From apps/web/src/components/shared/mobile-nav.tsx:
```tsx
// Bottom nav bar, visible on mobile only
// Root element needs data-tour="mobile-nav"
```

<!-- Existing localStorage patterns -->
From apps/web/src/components/shared/CookieConsent.tsx:
```tsx
// Pattern: localStorage.getItem('cookie-consent') → parse JSON → show/hide
// Pattern: localStorage.setItem('cookie-consent', JSON.stringify(state))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install driver.js, create tour CSS + definitions + TourProvider + HelpCircleButton</name>
  <files>
    apps/web/package.json,
    apps/web/src/styles/tour.css,
    apps/web/src/lib/tours/definitions.ts,
    apps/web/src/components/shared/TourProvider.tsx,
    apps/web/src/components/shared/HelpCircleButton.tsx
  </files>
  <read_first>
    apps/web/package.json,
    apps/web/src/styles/globals.css,
    apps/web/src/components/shared/CookieConsent.tsx,
    .planning/phases/36-product-tour-onboarding/36-UI-SPEC.md
  </read_first>
  <action>
1. Install driver.js:
   ```bash
   cd apps/web && pnpm add driver.js
   ```

2. Create `apps/web/src/styles/tour.css` — CSS overrides for driver.js to match MPSTATS brand. Copy EXACTLY from UI-SPEC "driver.js Configuration (CSS overrides)" section:
   - `.driver-popover` — white bg, 16px border-radius, 24px padding, max-width 340px, shadow
   - `.driver-popover-title` — 1.125rem, 600 weight, #111827
   - `.driver-popover-description` — 0.875rem, 400, #6B7280
   - `.driver-popover-progress-text` — 0.75rem, #9CA3AF
   - `.driver-popover-next-btn` — bg #2C4FF8, white text, 8px border-radius
   - `.driver-popover-prev-btn` — transparent, #2C4FF8 text
   - `.driver-popover-close-btn` — #9CA3AF text, hover #4B5563
   - `.driver-overlay` — rgba(9, 16, 50, 0.55)
   - Add mobile override: `@media (max-width: 767px) { .driver-popover { max-width: calc(100vw - 32px); } }`

3. Create `apps/web/src/lib/tours/definitions.ts`:
   - Export type `TourPage = 'dashboard' | 'learn' | 'lesson'`
   - Export `TOUR_PAGES` map: `{ '/dashboard': 'dashboard', '/learn': 'learn' }` (lesson matched by startsWith '/learn/')
   - Export `getTourForPage(pathname: string): TourPage | null` — returns TourPage or null
   - Export `getLocalStorageKey(page: TourPage): string` — returns `tour_${page}_completed`
   - Export `dashboardSteps`, `learnSteps`, `lessonSteps` — arrays of driver.js `DriveStep` objects
   - Each step has: `element: '[data-tour="..."]'`, `popover: { title, description }`
   - Use EXACT titles and descriptions from UI-SPEC Copywriting Contract tables
   - Dashboard tour (4 steps): sidebar-nav/mobile-nav (step 1 handled at runtime), dashboard-diagnostic-cta, dashboard-skill-radar, dashboard-learn-cta
   - Learn tour (5 steps): learn-search, learn-filters, learn-view-toggle, learn-sections, learn-add-to-track
   - Lesson tour (5 steps): lesson-video, lesson-summary, lesson-chat, lesson-comments, lesson-nav
   - Export `getSteps(page: TourPage, isMobile: boolean): DriveStep[]` — for dashboard, if isMobile, replace step 1 element from `[data-tour="sidebar-nav"]` to `[data-tour="mobile-nav"]`
   - Export shared config: `popoverClass: 'mpstats-tour-popover'`, `nextBtnText: 'Далее'`, `prevBtnText: 'Назад'`, `doneBtnText: 'Готово'`, `progressText: '{{current}} из {{total}}'`

4. Create `apps/web/src/components/shared/TourProvider.tsx` (`'use client'`):
   - Import `driver` from `driver.js`, import `driver.js/dist/driver.css`, import `@/styles/tour.css`
   - Create React context `TourContext` with `{ startTour: (page: TourPage) => void }`
   - Export `useTour()` hook that reads TourContext
   - `TourProvider` component:
     - Props: `{ children: ReactNode }`
     - `usePathname()` from `next/navigation` to detect current page
     - `useEffect` on mount (pathname changes):
       - Call `getTourForPage(pathname)` — if null, return
       - Check `localStorage.getItem(getLocalStorageKey(page)) === 'true'` — if true, return
       - Detect mobile: `window.matchMedia('(min-width: 768px)').matches` → `!matches` = isMobile
       - `setTimeout(() => { ... }, 1500)` to delay start
       - Inside timeout: create driver instance with `{ showProgress: true, steps: getSteps(page, isMobile), progressText, nextBtnText, prevBtnText, doneBtnText, allowClose: true, overlayClickBehavior: 'none', onDestroyStarted: () => { localStorage.setItem(key, 'true'); driverObj.destroy(); } }`
       - Call `driverObj.drive()`
       - Return cleanup: `clearTimeout(timer)`
     - `startTour` function: creates fresh driver instance with same config (no localStorage check, no delay), calls `drive()`
     - Render: `<TourContext.Provider value={{ startTour }}>{children}</TourContext.Provider>`

5. Create `apps/web/src/components/shared/HelpCircleButton.tsx` (`'use client'`):
   - Import `HelpCircle` from `lucide-react`, `useTour`, `usePathname`, `getTourForPage`
   - Check pathname — if `getTourForPage(pathname)` is null, return null (hidden)
   - Render: `<button onClick={() => startTour(page)} className="w-10 h-10 rounded-lg flex items-center justify-center text-mp-gray-400 hover:text-mp-blue-500 hover:bg-mp-gray-100 transition-colors duration-200" title="Повторить тур"><HelpCircle className="w-5 h-5" /></button>`
  </action>
  <verify>
    <automated>cd apps/web && node -e "require('driver.js')" && echo "driver.js OK" && test -f src/styles/tour.css && echo "tour.css OK" && test -f src/lib/tours/definitions.ts && echo "definitions OK" && test -f src/components/shared/TourProvider.tsx && echo "TourProvider OK" && test -f src/components/shared/HelpCircleButton.tsx && echo "HelpCircle OK"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/package.json` contains `"driver.js"` in dependencies
    - `apps/web/src/styles/tour.css` contains `.driver-popover` with `border-radius: 16px` and `background: #FFFFFF`
    - `apps/web/src/styles/tour.css` contains `.driver-popover-next-btn` with `background: #2C4FF8`
    - `apps/web/src/styles/tour.css` contains `@media (max-width: 767px)` with `calc(100vw - 32px)`
    - `apps/web/src/lib/tours/definitions.ts` exports `getTourForPage`, `getSteps`, `dashboardSteps`, `learnSteps`, `lessonSteps`
    - `definitions.ts` contains all 14 step titles from UI-SPEC: "Навигация", "Начните с диагностики", "Профиль навыков", "Персональный трек", "Поиск по урокам", "Фильтры", "Мой трек / Все курсы", "Секции трека", "Добавить в трек", "Видеоурок", "AI-конспект", "AI-чат", "Комментарии", "Навигация" (lesson)
    - `TourProvider.tsx` contains `'use client'`, imports `driver.js/dist/driver.css`, exports `TourProvider` and `useTour`
    - `TourProvider.tsx` contains `setTimeout` with `1500` delay
    - `TourProvider.tsx` contains `localStorage.setItem` with `'true'`
    - `TourProvider.tsx` contains `overlayClickBehavior: 'none'` (prevent accidental dismissal per UI-SPEC)
    - `HelpCircleButton.tsx` contains `HelpCircle` import from `lucide-react`
    - `HelpCircleButton.tsx` contains `title="Повторить тур"`
    - `HelpCircleButton.tsx` returns `null` when `getTourForPage` returns null
  </acceptance_criteria>
  <done>driver.js installed, tour CSS matches UI-SPEC brand colors, 3 tour definitions with 14 total steps, TourProvider with auto-start + localStorage persistence, HelpCircleButton with conditional visibility</done>
</task>

<task type="auto">
  <name>Task 2: Integrate TourProvider + HelpCircle into layout, add data-tour to sidebar and mobile-nav</name>
  <files>
    apps/web/src/app/(main)/layout.tsx,
    apps/web/src/components/shared/sidebar.tsx,
    apps/web/src/components/shared/mobile-nav.tsx
  </files>
  <read_first>
    apps/web/src/app/(main)/layout.tsx,
    apps/web/src/components/shared/sidebar.tsx,
    apps/web/src/components/shared/mobile-nav.tsx,
    apps/web/src/components/shared/TourProvider.tsx,
    apps/web/src/components/shared/HelpCircleButton.tsx
  </read_first>
  <action>
1. Edit `apps/web/src/app/(main)/layout.tsx`:
   - Add imports: `import { TourProvider } from '@/components/shared/TourProvider';` and `import { HelpCircleButton } from '@/components/shared/HelpCircleButton';`
   - In header, between the spacer `<div className="hidden md:block" />` and `<UserNav>`, add:
     ```tsx
     <div className="flex items-center gap-2">
       <HelpCircleButton />
       <UserNav user={{...}} />
     </div>
     ```
     Remove the standalone `<UserNav>` that was there before.
   - Wrap the `<main>` content area with TourProvider. Since layout is a server component and TourProvider is `'use client'`, wrap children:
     ```tsx
     <main className="flex-1 p-4 md:p-6 pb-20 md:pb-6 overflow-x-hidden">
       <TourProvider>
         {children}
       </TourProvider>
     </main>
     ```

2. Edit `apps/web/src/components/shared/sidebar.tsx`:
   - Find the `<nav>` element that wraps the sidebar navigation links
   - Add attribute `data-tour="sidebar-nav"` to it

3. Edit `apps/web/src/components/shared/mobile-nav.tsx`:
   - Find the root `<nav>` or container element
   - Add attribute `data-tour="mobile-nav"` to it
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `layout.tsx` contains `import { TourProvider }` from `@/components/shared/TourProvider`
    - `layout.tsx` contains `import { HelpCircleButton }` from `@/components/shared/HelpCircleButton`
    - `layout.tsx` contains `<TourProvider>` wrapping children
    - `layout.tsx` contains `<HelpCircleButton />`
    - `sidebar.tsx` contains `data-tour="sidebar-nav"`
    - `mobile-nav.tsx` contains `data-tour="mobile-nav"`
    - TypeScript compilation passes with no errors related to tour files
  </acceptance_criteria>
  <done>TourProvider wraps page content, HelpCircle button in header next to UserNav, sidebar and mobile-nav have data-tour attributes for dashboard tour step 1</done>
</task>

</tasks>

<verification>
1. `pnpm build` completes without errors
2. `grep -r 'driver.js' apps/web/package.json` shows dependency
3. `grep -r 'data-tour=' apps/web/src/components/shared/sidebar.tsx` shows `sidebar-nav`
4. `grep -r 'data-tour=' apps/web/src/components/shared/mobile-nav.tsx` shows `mobile-nav`
5. `grep -r 'TourProvider' apps/web/src/app/\(main\)/layout.tsx` shows import and usage
6. `grep -r 'HelpCircleButton' apps/web/src/app/\(main\)/layout.tsx` shows import and usage
</verification>

<success_criteria>
- driver.js installed as dependency
- TourProvider client component wraps main layout children
- HelpCircle button appears in header (visible only on /dashboard, /learn, /learn/[id])
- 3 tour definitions with 14 total steps matching UI-SPEC copy exactly
- CSS overrides match MPSTATS brand (mp-blue-500 CTA, white popover, 16px radius)
- Sidebar and MobileNav have data-tour attributes
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/36-product-tour-onboarding/36-01-SUMMARY.md`
</output>
