---
phase: 44-promo-codes
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - apps/web/src/app/pricing/page.tsx
  - apps/web/src/components/pricing/PromoCodeInput.tsx
  - apps/web/src/app/(main)/profile/page.tsx
autonomous: true
requirements:
  - D-03
  - D-04
  - D-10

must_haves:
  truths:
    - "User sees 'Есть промо-код?' section under pricing cards"
    - "Unauthenticated user is redirected to login with promo code preserved in URL"
    - "After login, promo code from URL is auto-filled"
    - "Successful activation shows toast and redirects to /dashboard"
    - "Promo subscription in profile shows 'Промо' badge without cancel button"
  artifacts:
    - path: "apps/web/src/components/pricing/PromoCodeInput.tsx"
      provides: "Collapsible promo code input component"
      exports: ["PromoCodeInput"]
    - path: "apps/web/src/app/pricing/page.tsx"
      provides: "Pricing page with auth header and promo code section"
      contains: "PromoCodeInput"
    - path: "apps/web/src/app/(main)/profile/page.tsx"
      provides: "Profile page with promo badge rendering"
      contains: "Промо"
  key_links:
    - from: "apps/web/src/components/pricing/PromoCodeInput.tsx"
      to: "packages/api/src/routers/promo.ts"
      via: "trpc.promo.activate.useMutation"
      pattern: "trpc\\.promo\\.activate"
    - from: "apps/web/src/app/pricing/page.tsx"
      to: "apps/web/src/components/pricing/PromoCodeInput.tsx"
      via: "import and render"
      pattern: "PromoCodeInput"
---

<objective>
Добавить UI промо-кодов на /pricing и отображение промо-подписки в профиле.

Purpose: Пользователь может ввести и активировать промо-код, а также видеть промо-подписку в профиле.
Output: PromoCodeInput компонент, обновлённая /pricing страница с auth-хедером, обновлённый профиль.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/44-promo-codes/44-CONTEXT.md
@.planning/phases/44-promo-codes/44-01-SUMMARY.md
@docs/plans/2026-04-06-promo-codes-design.md

<interfaces>
<!-- From Plan 01 promo router -->
```typescript
// promo.validate input/output:
// Input: { code: string }
// Output: { valid: boolean, error?: string, planType?: 'PLATFORM'|'COURSE', courseTitle?: string, durationDays?: number }

// promo.activate input/output:
// Input: { code: string }
// Output: { success: boolean, subscriptionId: string, planType: string, courseTitle?: string, durationDays: number, accessUntil: Date }
```

From apps/web/src/app/(main)/layout.tsx:
```typescript
// Auth header pattern:
// Server component uses createClient() -> supabase.auth.getUser()
// Renders UserNav with { email, name, avatarUrl }
```

From apps/web/src/app/pricing/page.tsx:
```typescript
// Current pricing page is 'use client' with billing queries
// Header: back button + Logo + spacer
// Content: plans grid, course select, payment buttons
```

From apps/web/src/app/(main)/profile/page.tsx:
```typescript
// Subscription card shows: plan name, price, course (if COURSE), period end
// Badge: subscriptionStatusMap[status] -> { label, variant }
// Cancel button shown when status === 'ACTIVE'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PromoCodeInput component and integrate into pricing page with auth header</name>
  <files>apps/web/src/components/pricing/PromoCodeInput.tsx, apps/web/src/app/pricing/page.tsx</files>
  <read_first>
    - apps/web/src/app/pricing/page.tsx
    - apps/web/src/app/(main)/layout.tsx
    - apps/web/src/components/shared/Logo.tsx
    - apps/web/src/components/ui/button.tsx
    - apps/web/src/components/ui/input.tsx
    - docs/plans/2026-04-06-promo-codes-design.md
    - .planning/phases/44-promo-codes/44-CONTEXT.md
  </read_first>
  <action>
**A) Create `apps/web/src/components/pricing/PromoCodeInput.tsx`:**

'use client' component that:

1. Renders a collapsible section: "Есть промо-код?" text link that toggles open/closed state.
2. When expanded, shows:
   - Input field (text, placeholder "Введите промо-код", uppercase transform via CSS `uppercase`)
   - "Активировать" button
   - Error/success message area below
3. Props: `{ isAuthenticated: boolean; initialCode?: string }`
4. If `initialCode` is provided, auto-expand and pre-fill the input.
5. On "Активировать" click:
   - If NOT authenticated: redirect to `/login?redirect=/pricing&promo=CODE` (per D-04)
   - If authenticated: call `trpc.promo.activate.useMutation()`
   - On success: `toast.success('Промо-код активирован!')` + `router.push('/dashboard')` after 1.5s delay
   - On error: show error message from TRPCError (the Russian messages from D-07)
6. Use existing UI components: Button, Input from shadcn/ui.
7. Styling: centered section under pricing cards, max-w-md mx-auto, subtle border/bg.

```tsx
'use client';

import { useState, useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { Button } from '@/components/ui/button';
import { Input } from '@/components/ui/input';
import { trpc } from '@/lib/trpc/client';
import { toast } from 'sonner';

interface PromoCodeInputProps {
  isAuthenticated: boolean;
  initialCode?: string;
}

export function PromoCodeInput({ isAuthenticated, initialCode }: PromoCodeInputProps) {
  const router = useRouter();
  const [isOpen, setIsOpen] = useState(!!initialCode);
  const [code, setCode] = useState(initialCode || '');
  const [error, setError] = useState('');

  const activate = trpc.promo.activate.useMutation({
    onSuccess: (data) => {
      toast.success('Промо-код активирован!', {
        description: `Доступ до ${new Date(data.accessUntil).toLocaleDateString('ru-RU')}`,
      });
      setTimeout(() => router.push('/dashboard'), 1500);
    },
    onError: (err) => {
      setError(err.message);
    },
  });

  const handleActivate = () => {
    if (!code.trim()) { setError('Введите промо-код'); return; }
    setError('');

    if (!isAuthenticated) {
      const encoded = encodeURIComponent(code.trim().toUpperCase());
      router.push(`/login?redirect=/pricing&promo=${encoded}`);
      return;
    }

    activate.mutate({ code: code.trim() });
  };

  // ... render collapsible UI
}
```

**B) Modify `apps/web/src/app/pricing/page.tsx`:**

1. **Auth header (per D-04):** The pricing page currently has a simple header with back button + Logo + spacer. Replace the right-side spacer with auth state display:
   - The page is 'use client', so use a tRPC query or Supabase client-side check.
   - Add a query to check auth: use `trpc.profile.get.useQuery(undefined, { retry: false })` — if it returns data, user is authenticated.
   - Render: if authenticated -> show user name/avatar + Link to /profile. If not -> show "Войти" button linking to `/login?redirect=/pricing`.
   - Keep the back button on the left, Logo in center.

2. **Promo code section:** After the pricing cards grid (after the closing `</div>` of `grid md:grid-cols-2`), add:
   ```tsx
   <div className="mt-10">
     <PromoCodeInput
       isAuthenticated={!!profile}
       initialCode={searchParams.get('promo') || undefined}
     />
   </div>
   ```

3. **Read URL params:** Add `useSearchParams()` from `next/navigation` to read `?promo=` param. Import and use `PromoCodeInput`.

4. **Auth state:** Use a simple approach — add a query `trpc.profile.get.useQuery(undefined, { retry: false, refetchOnWindowFocus: false })`. If it succeeds, user is authenticated. Pass `isAuthenticated={!!profileData}` to PromoCodeInput.

Per D-03: Promo code input only on /pricing, under tariff cards.
Per D-04: Header with auth state, redirect to login preserving promo code in URL.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File apps/web/src/components/pricing/PromoCodeInput.tsx exists
    - grep "PromoCodeInput" apps/web/src/components/pricing/PromoCodeInput.tsx returns match for export
    - grep "trpc.promo.activate" apps/web/src/components/pricing/PromoCodeInput.tsx returns a match
    - grep "/login?redirect=/pricing" apps/web/src/components/pricing/PromoCodeInput.tsx returns a match
    - grep "PromoCodeInput" apps/web/src/app/pricing/page.tsx returns a match (component imported and used)
    - grep "useSearchParams" apps/web/src/app/pricing/page.tsx returns a match
    - grep "promo" apps/web/src/app/pricing/page.tsx returns matches for URL param reading
    - grep "Войти" apps/web/src/app/pricing/page.tsx returns a match (login button in header)
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>PromoCodeInput component renders collapsible promo field with activation. Pricing page has auth header and promo section. URL param ?promo= auto-fills code. Unauthenticated redirect preserves code.</done>
</task>

<task type="auto">
  <name>Task 2: Add promo subscription badge and hide cancel button in profile page</name>
  <files>apps/web/src/app/(main)/profile/page.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/profile/page.tsx
    - docs/plans/2026-04-06-promo-codes-design.md
    - .planning/phases/44-promo-codes/44-CONTEXT.md
  </read_first>
  <action>
Modify `apps/web/src/app/(main)/profile/page.tsx` to handle promo subscriptions (per D-10):

1. **Update subscription query** to include `promoCodeId` field. The existing query is:
```typescript
const { data: subscription, ... } = trpc.billing.getSubscription.useQuery(...)
```
The `billing.getSubscription` procedure already does `include: { plan: true, course: true }` but does NOT include `promoCodeId`. Since this is a direct field on Subscription (not a relation), it should already be returned by default. Verify — if not, the billing router needs a tweak. Actually, Prisma `findFirst` returns all scalar fields by default, so `promoCodeId` WILL be in the response already. No changes needed to the query.

2. **Detect promo subscription:** Add a computed variable:
```typescript
const isPromoSubscription = subscription?.promoCodeId != null;
```

3. **Badge:** In the subscription card header, where the Badge is rendered:
```tsx
<Badge variant={subscriptionStatusMap[subscription.status]?.variant || 'default'}>
  {subscriptionStatusMap[subscription.status]?.label || subscription.status}
</Badge>
```
Change to:
```tsx
<Badge variant={isPromoSubscription ? 'secondary' : (subscriptionStatusMap[subscription.status]?.variant || 'default')}>
  {isPromoSubscription ? 'Промо' : (subscriptionStatusMap[subscription.status]?.label || subscription.status)}
</Badge>
```
Use variant `'secondary'` or a custom color. Check available Badge variants in the component — if `secondary` exists, use it. If not, use `'default'` with a purple/blue color override, or just keep the existing success variant but change label to "Промо".

4. **Subscription details text:** When `isPromoSubscription`, change the "Стоимость" row to show "Бесплатно (промо)" instead of the price:
```tsx
{isPromoSubscription ? (
  <div className="flex justify-between gap-4 text-body-sm">
    <span className="text-mp-gray-500 shrink-0">Тип</span>
    <span className="font-medium text-mp-gray-900">Промо-доступ</span>
  </div>
) : (
  <div className="flex justify-between gap-4 text-body-sm">
    <span className="text-mp-gray-500 shrink-0">Стоимость</span>
    <span className="font-medium text-mp-gray-900">{formatPrice(subscription.plan.price)} / мес</span>
  </div>
)}
```

5. **Hide cancel button for promo subscriptions (per D-10):**
Change the cancel button condition from:
```tsx
{subscription.status === 'ACTIVE' && (
```
to:
```tsx
{subscription.status === 'ACTIVE' && !isPromoSubscription && (
```

6. **Access period label for promo:** When promo, always show "Доступ до" (not "Следующее списание"):
```tsx
<span className="text-mp-gray-500 shrink-0">
  {subscription.status === 'CANCELLED' || isPromoSubscription ? 'Доступ до' : 'Следующее списание'}
</span>
```
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && pnpm typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "promoCodeId" apps/web/src/app/(main)/profile/page.tsx returns a match
    - grep "isPromoSubscription" apps/web/src/app/(main)/profile/page.tsx returns a match
    - grep "Промо" apps/web/src/app/(main)/profile/page.tsx returns a match (badge label)
    - grep "Промо-доступ" apps/web/src/app/(main)/profile/page.tsx returns a match
    - grep "!isPromoSubscription" apps/web/src/app/(main)/profile/page.tsx returns a match (cancel button guard)
    - pnpm typecheck passes
  </acceptance_criteria>
  <done>Profile shows "Промо" badge for promo subscriptions, "Промо-доступ" instead of price, "Доступ до" instead of "Следующее списание", cancel button hidden for promo subscriptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> promo.activate | User input (promo code) from pricing page |
| URL params | ?promo= param could contain arbitrary strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-07 | Tampering | URL ?promo= param | mitigate | Input sanitized via .trim().toUpperCase(), zod validation on server |
| T-44-08 | Information Disclosure | Profile subscription | accept | promoCodeId is a CUID, no sensitive data exposed |
</threat_model>

<verification>
1. pnpm typecheck passes
2. PromoCodeInput renders and handles auth/unauth flows
3. Profile correctly detects and displays promo subscriptions
</verification>

<success_criteria>
- PromoCodeInput component on /pricing under tariff cards (D-03)
- Auth header on /pricing with login/avatar (D-04)
- ?promo= URL param auto-fills code after login redirect (D-04)
- Promo badge in profile, no cancel button, "Промо-доступ" label (D-10)
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/44-promo-codes/44-02-SUMMARY.md`
</output>
