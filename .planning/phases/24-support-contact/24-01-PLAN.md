---
phase: 24-support-contact
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/support/page.tsx
  - apps/web/src/lib/carrotquest/types.ts
  - apps/web/src/components/shared/sidebar.tsx
  - apps/web/src/components/shared/mobile-nav.tsx
  - apps/web/src/app/page.tsx
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
  - SUPP-03
  - SUPP-04

must_haves:
  truths:
    - "Пользователь видит страницу /support с контактами, FAQ и формой обратной связи"
    - "Форма отправляет CQ event 'Support Request' с темой, сообщением и email"
    - "Ссылка 'Поддержка' видна в sidebar footer и mobile-nav"
    - "Ссылка 'Поддержка' видна в landing footer для неавторизованных"
    - "Страница /support публичная — доступна без авторизации"
  artifacts:
    - path: "apps/web/src/app/support/page.tsx"
      provides: "Support page with contacts, FAQ accordion, feedback form"
      min_lines: 100
    - path: "apps/web/src/lib/carrotquest/types.ts"
      provides: "Support Request event type"
      contains: "Support Request"
  key_links:
    - from: "apps/web/src/app/support/page.tsx"
      to: "/api/trpc"
      via: "CQ trackEvent server action or client fetch"
      pattern: "Support Request"
    - from: "apps/web/src/components/shared/sidebar.tsx"
      to: "/support"
      via: "Link href"
      pattern: "/support"
---

<objective>
Create the /support page with contact info, FAQ accordion, and feedback form that sends CQ events. Add navigation links in sidebar, mobile-nav, and landing footer.

Purpose: Users need a way to contact support from any point in the app.
Output: Public /support page, updated navigation components.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-support-contact/24-CONTEXT.md

<interfaces>
<!-- CQ client for server-side event tracking -->
From apps/web/src/lib/carrotquest/client.ts:
```typescript
export class CarrotQuestClient {
  async trackEvent(userId: string, event: CQEventName, params?: CQEventData): Promise<void>;
  async setUserProps(userId: string, props: Record<string, unknown>): Promise<void>;
}
export const cq: CarrotQuestClient;
```

From apps/web/src/lib/carrotquest/types.ts:
```typescript
export type CQEventName =
  | 'Payment Success'
  | 'Payment Failed'
  | 'Subscription Cancelled'
  | 'Subscription Expiring'
  | 'User Registered'
  | 'Inactive 7d'
  | 'Inactive 14d'
  | 'Inactive 30d'
  | 'Email Confirmation'
  | 'Password Reset'
  | 'Email Change';

export type CQEventData = Record<string, string | number | boolean | null>;
```

From apps/web/src/app/pricing/page.tsx (pattern for public page):
- 'use client' component outside (main) layout
- Own header with Logo/LogoMark + back button
- trpc queries with retry:false for unauthenticated graceful handling
- toast from sonner for notifications

From apps/web/src/components/shared/sidebar.tsx:
- Footer section: `<div className="p-4 border-t border-mp-gray-200 space-y-1">`
- Admin link conditionally rendered with isAdmin check
- NavItem interface: { title, href, icon }

From apps/web/src/components/shared/mobile-nav.tsx:
- Admin link pushed via `items.push(adminNavItem)` after role check
- Bottom fixed nav bar with icon + title per item
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /support page with contacts, FAQ, and feedback form</name>
  <files>apps/web/src/app/support/page.tsx, apps/web/src/lib/carrotquest/types.ts</files>
  <read_first>
    - apps/web/src/app/pricing/page.tsx (pattern for public page with own header, LogoMark on mobile)
    - apps/web/src/lib/carrotquest/client.ts (CQ trackEvent API)
    - apps/web/src/lib/carrotquest/types.ts (existing event names to extend)
    - apps/web/src/app/layout.tsx (CQ widget script — for window.carrotquest.open() reference)
  </read_first>
  <action>
1. Add `'Support Request'` to CQEventName union in `apps/web/src/lib/carrotquest/types.ts`.

2. Create `apps/web/src/app/support/page.tsx` as a 'use client' component (public, outside (main) layout — same pattern as /pricing):

**Header:** Same pattern as /pricing — Logo/LogoMark (LogoMark on mobile via `hidden sm:block` / `block sm:hidden`), "Назад" button that does `router.back()`.

**Contact block:**
- Card with heading "Свяжитесь с нами"
- Email: `clients@mpstats.academy` (mailto link)
- Button "Написать в чат" — calls `window.carrotquest?.open()` to open CQ widget
- Use `Card` + `CardContent` from shadcn/ui

**FAQ block:**
- Heading "Частые вопросы"
- Use shadcn `Accordion` / `AccordionItem` / `AccordionTrigger` / `AccordionContent` (from `@/components/ui/accordion`)
- 5 items:
  1. "Как оформить подписку?" — "Перейдите на страницу тарифов, выберите план и оплатите картой. Подписка активируется мгновенно."
  2. "Как отменить подписку?" — "Откройте раздел Профиль, найдите блок 'Подписка' и нажмите 'Отменить'. Доступ сохранится до конца оплаченного периода."
  3. "Не приходит письмо подтверждения" — "Проверьте папку Спам. Если письма нет — напишите нам в чат или на clients@mpstats.academy, и мы подтвердим email вручную."
  4. "Не воспроизводится видео" — "Убедитесь, что браузер обновлён. Попробуйте отключить VPN или блокировщик рекламы. Если проблема сохраняется — напишите в поддержку."
  5. "Как пройти диагностику повторно?" — "Откройте раздел Диагностика и нажмите 'Начать диагностику'. Результаты предыдущей диагностики сохранятся для сравнения."

**Feedback form:**
- Card with heading "Напишите нам"
- Fields:
  - Select dropdown "Тема обращения" with options: "Оплата и подписка" / "Проблема с доступом" / "Техническая проблема" / "Предложение" / "Другое"
  - Textarea "Сообщение" (required, min 10 chars)
  - Input "Email" — visible only when user is NOT authenticated (detect via trpc `profile.get` with retry:false). For authenticated users, auto-fill from profile query result.
- Submit button "Отправить"
- On submit: POST to a Next.js server action or API route `/api/support` that calls `cq.trackEvent(userId || 'anonymous', 'Support Request', { theme, message, email })`.
  - For simplicity, create an inline server action using `'use server'` that imports `cq` from carrotquest/client.
  - Alternatively, use a simple fetch to a small API route `apps/web/src/app/api/support/route.ts` that accepts `{theme, message, email}` and calls cq.trackEvent.
  - Choose the API route approach since the page is 'use client'.
- After success: `toast.success('Обращение отправлено! Мы ответим в ближайшее время.')` and reset form.
- On error: `toast.error('Не удалось отправить. Попробуйте написать в чат.')`

3. Create `apps/web/src/app/api/support/route.ts`:
   - POST handler accepting JSON `{ theme: string, message: string, email: string, userId?: string }`
   - Calls `cq.trackEvent(userId || 'anonymous-' + email, 'Support Request', { theme, message, email })`
   - Returns `{ success: true }` on 200
   - No auth required (public endpoint)

**Styling:** Use mp-colors (mp-blue for accents, mp-gray for text), `animate-fade-in` on the page wrapper. Max width `max-w-3xl mx-auto`. Responsive padding `px-4 sm:px-6`.

**Metadata:** Export `metadata` with `title: 'Поддержка | MPSTATS Academy'` and `description: 'Служба поддержки MPSTATS Academy — FAQ, контакты, форма обратной связи'`.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/support/page.tsx exists and contains `'use client'`
    - apps/web/src/app/support/page.tsx contains `window.carrotquest` (chat open button)
    - apps/web/src/app/support/page.tsx contains `clients@mpstats.academy` (email contact)
    - apps/web/src/app/support/page.tsx contains `AccordionItem` (FAQ section)
    - apps/web/src/app/support/page.tsx contains `Тема обращения` (form dropdown)
    - apps/web/src/app/api/support/route.ts exists and contains `cq.trackEvent`
    - apps/web/src/app/api/support/route.ts contains `Support Request`
    - apps/web/src/lib/carrotquest/types.ts contains `Support Request`
    - TypeScript compilation passes without errors
  </acceptance_criteria>
  <done>
    /support page renders with 3 sections (contacts, FAQ accordion, form), form submits to API route that fires CQ event, page is public and follows /pricing layout pattern.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Поддержка" navigation links to sidebar, mobile-nav, and landing footer</name>
  <files>apps/web/src/components/shared/sidebar.tsx, apps/web/src/components/shared/mobile-nav.tsx, apps/web/src/app/page.tsx</files>
  <read_first>
    - apps/web/src/components/shared/sidebar.tsx (full file — see footer section with admin link pattern)
    - apps/web/src/components/shared/mobile-nav.tsx (full file — see admin link push pattern)
    - apps/web/src/app/page.tsx (footer section at bottom — lines 387-393)
  </read_first>
  <action>
1. **Sidebar** (`apps/web/src/components/shared/sidebar.tsx`):
   - Add a `supportNavItem` constant (same structure as `adminNavItem`):
     ```
     const supportNavItem: NavItem = {
       title: 'Поддержка',
       href: '/support',
       icon: <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M8.228 9c.549-1.165 2.03-2 3.772-2 2.21 0 4 1.343 4 3 0 1.4-1.278 2.575-3.006 2.907-.542.104-.994.54-.994 1.093m0 3h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
     };
     ```
   - In the footer `<div>`, render the support link BEFORE the admin link (always visible, not conditional):
     ```tsx
     <Link href={supportNavItem.href} className={cn(
       'flex items-center gap-3 px-3 py-2.5 rounded-lg text-body-sm font-medium transition-all duration-200',
       pathname === '/support'
         ? 'bg-mp-blue-50 text-mp-blue-600 shadow-mp-sm'
         : 'text-mp-gray-600 hover:bg-mp-gray-100 hover:text-mp-gray-900'
     )}>
       <span className={cn('transition-colors', pathname === '/support' ? 'text-mp-blue-500' : 'text-mp-gray-400')}>
         {supportNavItem.icon}
       </span>
       {supportNavItem.title}
     </Link>
     ```

2. **Mobile nav** (`apps/web/src/components/shared/mobile-nav.tsx`):
   - Add same `supportNavItem` constant (with `w-6 h-6` icon size to match mobile pattern).
   - Push `supportNavItem` into `items` array BEFORE the admin link push: `items.push(supportNavItem);`
   - This ensures "Поддержка" appears for ALL users (not just admins).

3. **Landing footer** (`apps/web/src/app/page.tsx`):
   - In the footer section (around line 388-392), add a link between the Logo and copyright text:
     ```tsx
     <div className="flex items-center gap-4">
       <Link href="/support" className="text-xs text-[var(--landing-text-faint)] hover:text-[var(--landing-text)] transition-colors">
         Поддержка
       </Link>
     </div>
     ```
   - Add `import Link from 'next/link';` if not already imported (it is already imported in this file).
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/components/shared/sidebar.tsx contains `href: '/support'`
    - apps/web/src/components/shared/sidebar.tsx contains `Поддержка`
    - apps/web/src/components/shared/mobile-nav.tsx contains `href: '/support'`
    - apps/web/src/components/shared/mobile-nav.tsx contains `Поддержка`
    - apps/web/src/app/page.tsx contains `/support` (landing footer link)
    - TypeScript compilation passes without errors
  </acceptance_criteria>
  <done>
    "Поддержка" link visible in sidebar footer (always, for all users), mobile bottom nav (always), and landing page footer. All links navigate to /support.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `/support` page accessible without login (public route — not in protectedRoutes array in middleware.ts)
3. FAQ accordion expands/collapses
4. Form submits and shows toast
5. "Поддержка" link visible in sidebar, mobile nav, and landing footer
</verification>

<success_criteria>
- /support page renders with contacts, FAQ, and feedback form
- Feedback form sends "Support Request" CQ event via /api/support route
- Navigation to /support available from sidebar, mobile nav, and landing
- Page is public (no auth required)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/24-support-contact/24-01-SUMMARY.md`
</output>
