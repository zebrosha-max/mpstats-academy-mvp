---
phase: 27-seo-custom-error-pages-sitemap-robots-txt-og-404-500
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/sitemap.ts
  - apps/web/src/app/robots.ts
  - apps/web/public/og-default.png
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03

must_haves:
  truths:
    - "Ссылка на / в Telegram/VK показывает OG-карточку с логотипом и описанием"
    - "robots.txt блокирует краулеры от /dashboard, /learn, /diagnostic, /profile, /admin, /api"
    - "sitemap.xml содержит публичные страницы (/, /pricing, /login, /register)"
    - "Root metadata задаёт title template и default OG-теги для всего сайта"
  artifacts:
    - path: "apps/web/src/app/layout.tsx"
      provides: "Root metadata with title template, OG defaults, og:locale ru_RU"
      contains: "openGraph"
    - path: "apps/web/src/app/sitemap.ts"
      provides: "Static sitemap with public URLs"
      exports: ["default"]
    - path: "apps/web/src/app/robots.ts"
      provides: "robots.txt with Disallow rules"
      exports: ["default"]
    - path: "apps/web/public/og-default.png"
      provides: "Default OG image 1200x630"
  key_links:
    - from: "apps/web/src/app/layout.tsx"
      to: "apps/web/public/og-default.png"
      via: "metadata.openGraph.images"
      pattern: "og-default.png"
    - from: "apps/web/src/app/robots.ts"
      to: "apps/web/src/app/sitemap.ts"
      via: "sitemap URL reference"
      pattern: "sitemap"
---

<objective>
SEO Foundation: root metadata with OG tags, sitemap.xml, robots.txt, OG image

Purpose: Search engines and social media (Telegram, VK) correctly index public pages and display rich previews when links are shared.
Output: Root layout with full metadata + title template, sitemap.ts, robots.ts, default OG PNG image.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-seo-custom-error-pages-sitemap-robots-txt-og-404-500/27-CONTEXT.md

<interfaces>
<!-- Root layout currently has minimal metadata -->
From apps/web/src/app/layout.tsx:
```typescript
export const metadata: Metadata = {
  title: 'MPSTATS Academy',
  description: 'Образовательная платформа для селлеров маркетплейсов',
};
```

From apps/web/src/components/shared/Logo.tsx:
```typescript
// LogoIcon SVG viewBox="0 0 100 143", primary color '#2C4FF8'
export function Logo({ size, showText, variant, className, href }: LogoProps)
export function LogoMark({ size, variant, className, href }: Omit<LogoProps, 'showText'>)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root metadata + sitemap + robots</name>
  <files>
    apps/web/src/app/layout.tsx,
    apps/web/src/app/sitemap.ts,
    apps/web/src/app/robots.ts
  </files>
  <read_first>
    apps/web/src/app/layout.tsx
  </read_first>
  <action>
**1. Update `apps/web/src/app/layout.tsx` metadata:**

Replace the current `metadata` export with:

```typescript
export const metadata: Metadata = {
  title: {
    default: 'MPSTATS Academy — образовательная платформа для селлеров',
    template: '%s | MPSTATS Academy',
  },
  description: 'Образовательная платформа для селлеров маркетплейсов. AI-диагностика навыков, персонализированный трек обучения, 400+ видеоуроков.',
  metadataBase: new URL('https://platform.mpstats.academy'),
  openGraph: {
    type: 'website',
    locale: 'ru_RU',
    siteName: 'MPSTATS Academy',
    title: 'MPSTATS Academy — образовательная платформа для селлеров',
    description: 'AI-диагностика навыков, персонализированный трек обучения, 400+ видеоуроков для селлеров маркетплейсов.',
    url: 'https://platform.mpstats.academy',
    images: [
      {
        url: '/og-default.png',
        width: 1200,
        height: 630,
        alt: 'MPSTATS Academy',
      },
    ],
  },
};
```

Keep `viewport` export and everything else unchanged.

**2. Create `apps/web/src/app/sitemap.ts`:**

```typescript
import type { MetadataRoute } from 'next';

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://platform.mpstats.academy';

  return [
    {
      url: baseUrl,
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1.0,
    },
    {
      url: `${baseUrl}/pricing`,
      lastModified: new Date(),
      changeFrequency: 'monthly',
      priority: 0.8,
    },
    {
      url: `${baseUrl}/login`,
      lastModified: new Date(),
      changeFrequency: 'yearly',
      priority: 0.3,
    },
    {
      url: `${baseUrl}/register`,
      lastModified: new Date(),
      changeFrequency: 'yearly',
      priority: 0.3,
    },
  ];
}
```

**3. Create `apps/web/src/app/robots.ts`:**

```typescript
import type { MetadataRoute } from 'next';

export default function robots(): MetadataRoute.Robots {
  return {
    rules: [
      {
        userAgent: '*',
        allow: ['/', '/pricing', '/login', '/register'],
        disallow: ['/dashboard', '/learn', '/diagnostic', '/profile', '/admin', '/api'],
      },
    ],
    sitemap: 'https://platform.mpstats.academy/sitemap.xml',
  };
}
```

Do NOT add Twitter Card tags. Do NOT add JSON-LD.
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/layout.tsx contains `openGraph:` with `locale: 'ru_RU'`
    - apps/web/src/app/layout.tsx contains `title:` with `template: '%s | MPSTATS Academy'`
    - apps/web/src/app/layout.tsx contains `metadataBase: new URL('https://platform.mpstats.academy')`
    - apps/web/src/app/layout.tsx contains `og-default.png`
    - apps/web/src/app/layout.tsx does NOT contain `twitter`
    - apps/web/src/app/sitemap.ts exists and contains `platform.mpstats.academy`
    - apps/web/src/app/sitemap.ts contains `/pricing` and `/login` and `/register`
    - apps/web/src/app/robots.ts exists and contains `disallow: ['/dashboard'`
    - apps/web/src/app/robots.ts contains `sitemap: 'https://platform.mpstats.academy/sitemap.xml'`
  </acceptance_criteria>
  <done>Root layout exports full Metadata with title template + OG defaults, sitemap.ts serves 4 public URLs, robots.ts blocks protected routes and references sitemap.</done>
</task>

<task type="auto">
  <name>Task 2: Generate default OG image</name>
  <files>
    apps/web/public/og-default.png
  </files>
  <read_first>
    apps/web/src/components/shared/Logo.tsx
  </read_first>
  <action>
Create a static OG image at `apps/web/public/og-default.png` (1200x630 pixels).

**Design spec:**
- Background: mp-blue gradient (linear gradient from `#2C4FF8` at top-left to `#1a3ad4` at bottom-right)
- Center: MPSTATS Academy logo icon (the play-button mark from Logo.tsx) in white, approximately 120px tall
- Below logo: text "MPSTATS Academy" in white, bold, ~48px equivalent
- Below title: slogan "Образовательная платформа для селлеров маркетплейсов" in white/semi-transparent, ~24px equivalent
- Clean, minimal composition with generous padding

**Implementation approach:** Create the image using an SVG template rendered to PNG. Use a Node.js script with `sharp` or `@resvg/resvg-js` to convert SVG to PNG. If neither is available in the project, use a pure SVG approach:

1. Create a temporary SVG file with the design
2. Convert to PNG using available tooling (sharp, resvg, or canvas)
3. Save as `apps/web/public/og-default.png`

If no image library is installed, install `sharp` as a devDependency (`pnpm add -D sharp --filter web`) and use it to create the PNG from an SVG string.

The SVG should embed the LogoIcon path data from Logo.tsx (viewBox `0 0 100 143`, the two `<path>` elements) but rendered in white (`fill="#FFFFFF"`).

**Final file must be exactly 1200x630 PNG.**
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f='apps/web/public/og-default.png'; const s=fs.statSync(f); console.log('exists:', fs.existsSync(f), 'size:', s.size, 'ok:', s.size > 5000)"</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/public/og-default.png exists
    - File size is > 5KB (valid PNG, not empty placeholder)
    - File is a PNG (first 8 bytes are PNG magic: 89 50 4E 47)
  </acceptance_criteria>
  <done>OG image exists at public/og-default.png, is a valid 1200x630 PNG with mp-blue gradient background and white MPSTATS Academy branding.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds without errors
- Visiting `/sitemap.xml` in dev server returns XML with 4 URLs
- Visiting `/robots.txt` in dev server returns rules with Disallow entries
- `og-default.png` is a valid PNG file > 5KB
</verification>

<success_criteria>
1. Root layout metadata has title template, OG tags with ru_RU locale, metadataBase pointing to production URL
2. sitemap.xml serves 4 public page URLs (/, /pricing, /login, /register)
3. robots.txt blocks /dashboard, /learn, /diagnostic, /profile, /admin, /api and references sitemap
4. OG image exists as 1200x630 PNG in public/
</success_criteria>

<output>
After completion, create `.planning/phases/27-seo-custom-error-pages-sitemap-robots-txt-og-404-500/27-01-SUMMARY.md`
</output>
