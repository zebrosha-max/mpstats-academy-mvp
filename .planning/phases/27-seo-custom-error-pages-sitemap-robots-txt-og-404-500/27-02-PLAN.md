---
phase: 27-seo-custom-error-pages-sitemap-robots-txt-og-404-500
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/app/not-found.tsx
  - apps/web/src/app/error.tsx
  - apps/web/src/app/global-error.tsx
  - apps/web/src/app/(main)/error.tsx
  - apps/web/src/app/(auth)/layout.tsx
  - apps/web/src/app/(main)/layout.tsx
  - apps/web/src/app/pricing/layout.tsx
autonomous: false
requirements:
  - SEO-04
  - SEO-05
  - SEO-06

must_haves:
  truths:
    - "404 page shows MPSTATS Academy Logo component and links to / (not /dashboard)"
    - "Error pages (error.tsx, global-error.tsx, main/error.tsx) show Logo and have Retry + Home buttons"
    - "Each public/protected page route has unique title and description via metadata"
    - "Yandex Webmaster verification meta tag is present in root layout"
  artifacts:
    - path: "apps/web/src/app/not-found.tsx"
      provides: "Branded 404 page with Logo, link to /"
      contains: "Logo"
    - path: "apps/web/src/app/error.tsx"
      provides: "Branded error page with Logo"
      contains: "Logo"
    - path: "apps/web/src/app/global-error.tsx"
      provides: "Root-level error with inline branding and link to /"
      contains: "MPSTATS"
    - path: "apps/web/src/app/(auth)/layout.tsx"
      provides: "Auth pages metadata"
      contains: "metadata"
    - path: "apps/web/src/app/(main)/layout.tsx"
      provides: "Main pages metadata"
      contains: "metadata"
    - path: "apps/web/src/app/pricing/layout.tsx"
      provides: "Pricing page metadata"
      contains: "metadata"
  key_links:
    - from: "apps/web/src/app/not-found.tsx"
      to: "apps/web/src/components/shared/Logo.tsx"
      via: "import Logo"
      pattern: "import.*Logo.*from.*components/shared/Logo"
---

<objective>
Per-page metadata, branded error pages, and Yandex Webmaster verification

Purpose: Every page has a unique title/description for SEO, error pages are branded with MPSTATS Academy identity, and Yandex can verify site ownership.
Output: Updated error pages with Logo component, per-page metadata via layout files, Yandex Webmaster checkpoint.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-seo-custom-error-pages-sitemap-robots-txt-og-404-500/27-CONTEXT.md

<interfaces>
From apps/web/src/components/shared/Logo.tsx:
```typescript
export function Logo({ size, showText, variant, className, href }: LogoProps)
// sizes: 'sm' | 'md' | 'lg' | 'xl'
// variants: 'default' | 'white' | 'mono'
```

From apps/web/src/components/ui/button.tsx:
```typescript
// variants: default | outline | secondary | ghost | link | success | featured
```

From apps/web/src/components/ui/card.tsx:
```typescript
export { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter }
```

Current not-found.tsx links to /dashboard (should be /).
Current error.tsx has Logo-like text but not the Logo component.
Current global-error.tsx uses inline styles (no Tailwind - correct for root error).
Current (main)/error.tsx links to /dashboard.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Polish error pages with Logo + correct links</name>
  <files>
    apps/web/src/app/not-found.tsx,
    apps/web/src/app/error.tsx,
    apps/web/src/app/global-error.tsx,
    apps/web/src/app/(main)/error.tsx
  </files>
  <read_first>
    apps/web/src/app/not-found.tsx,
    apps/web/src/app/error.tsx,
    apps/web/src/app/global-error.tsx,
    apps/web/src/app/(main)/error.tsx,
    apps/web/src/components/shared/Logo.tsx
  </read_first>
  <action>
**1. Update `apps/web/src/app/not-found.tsx`:**

Replace the inline text logo with the real `Logo` component. Change the link from `/dashboard` to `/`. Keep the same overall layout (centered Card, mp-gray-50 background, shadow-mp-card).

```tsx
import Link from 'next/link';
import { Logo } from '@/components/shared/Logo';
import { Button } from '@/components/ui/button';
import { Card, CardContent } from '@/components/ui/card';

export default function NotFound() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-mp-gray-50 p-4">
      <Card className="max-w-md w-full shadow-mp-card">
        <CardContent className="py-12 text-center">
          <div className="mb-6 flex justify-center">
            <Logo size="md" href={undefined} />
          </div>
          <div className="text-8xl font-bold text-mp-blue-100 mb-4">404</div>
          <h1 className="text-xl font-bold text-mp-gray-900 mb-2">
            Страница не найдена
          </h1>
          <p className="text-sm text-mp-gray-500 mb-6">
            Запрашиваемая страница не существует или была перемещена.
          </p>
          <Link href="/">
            <Button>На главную</Button>
          </Link>
        </CardContent>
      </Card>
    </div>
  );
}
```

Key changes: `import { Logo }`, `<Logo size="md" href={undefined} />` (no link on logo itself), `Link href="/"` (was `/dashboard`).

**2. Update `apps/web/src/app/error.tsx`:**

Add Logo component import and render. Keep `'use client'`, keep reset() button, keep link to `/`.

Replace the header area (currently has icon-only triangle) with:
```tsx
<div className="mb-4 flex justify-center">
  <Logo size="md" href={undefined} />
</div>
```
Add it BEFORE the error icon div. Keep everything else the same. The Link href already points to `/` — no change needed there.

**3. Update `apps/web/src/app/global-error.tsx`:**

This uses inline styles (correct — root error can't use Tailwind). Add the MPSTATS Academy SVG logo inline (copy the LogoIcon path data). Add `href="/"` on the "На главную" link (already correct).

Add before the "Критическая ошибка" heading:
```tsx
<div style={{ marginBottom: '1rem', display: 'flex', justifyContent: 'center' }}>
  <svg width="40" height="57" viewBox="0 0 100 143" fill="none" xmlns="http://www.w3.org/2000/svg">
    <path d="M91.5463 37.7559L27.0743 0.534714C24.4536 -0.982506 21.163 0.909093 21.163 3.94353V78.3858C21.163 81.4203 24.4536 83.3119 27.0743 81.7947L91.5463 44.5735C94.167 43.0563 94.167 39.2731 91.5463 37.7559Z" fill="#2C4FF8"/>
    <path d="M98.3041 82.2873L74.994 68.8294C73.7724 68.12 72.2749 68.12 71.0532 68.8294L16.3939 100.395C13.7732 101.913 10.4826 100.021 10.4826 96.9866V38.4653C10.4826 35.5688 8.13783 33.224 5.24131 33.224C2.3448 33.224 0 35.5688 0 38.4653V139.055C0 142.089 3.27089 143.981 5.91125 142.464L98.3238 89.105C100.944 87.5878 100.944 83.8046 98.3238 82.2873H98.3041Z" fill="#2C4FF8"/>
  </svg>
</div>
```

Replace the `!` character in the error icon div with this SVG logo. Remove the old red circle icon div entirely.

**4. Update `apps/web/src/app/(main)/error.tsx`:**

Add Logo import. Change `/dashboard` link to `/dashboard` (keep it — user is authenticated in main layout, dashboard is appropriate). Add Logo before error icon:

```tsx
<div className="mb-2 flex justify-center">
  <Logo size="sm" href={undefined} showText={false} />
</div>
```

Add this line right before the existing red icon div. Import `Logo` from `@/components/shared/Logo`. Keep the `'use client'` directive. Keep all other code unchanged.

Note: `(main)/error.tsx` keeps link to `/dashboard` because this error occurs inside the authenticated layout. The root `not-found.tsx` and `error.tsx` link to `/` because unauthenticated users see those.
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/not-found.tsx contains `import { Logo } from '@/components/shared/Logo'`
    - apps/web/src/app/not-found.tsx contains `href="/"` (NOT `/dashboard`)
    - apps/web/src/app/not-found.tsx does NOT contain `href="/dashboard"`
    - apps/web/src/app/error.tsx contains `import { Logo }`
    - apps/web/src/app/global-error.tsx contains `viewBox="0 0 100 143"` (MPSTATS logo SVG)
    - apps/web/src/app/global-error.tsx contains `fill="#2C4FF8"` (mp-blue)
    - apps/web/src/app/(main)/error.tsx contains `import { Logo }`
    - Build succeeds
  </acceptance_criteria>
  <done>All 4 error pages show MPSTATS Academy branding (Logo component or inline SVG). not-found.tsx and root error.tsx link to /. main/error.tsx links to /dashboard. global-error.tsx has inline SVG logo.</done>
</task>

<task type="auto">
  <name>Task 2: Per-page metadata via layout files</name>
  <files>
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/app/(main)/layout.tsx,
    apps/web/src/app/pricing/layout.tsx
  </files>
  <read_first>
    apps/web/src/app/(auth)/layout.tsx,
    apps/web/src/app/(main)/layout.tsx,
    apps/web/src/app/layout.tsx
  </read_first>
  <action>
All page.tsx files are `'use client'` so metadata must go in layout files. The root layout.tsx (Plan 01) provides the `title.template: '%s | MPSTATS Academy'` — child metadata only needs `title: 'Page Name'` and the template auto-appends.

**1. Update `apps/web/src/app/(auth)/layout.tsx`:**

Add at the top of the file (after imports, before the component):

```typescript
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Авторизация',
  description: 'Войдите или зарегистрируйтесь на платформе MPSTATS Academy для доступа к курсам по маркетплейсам.',
};
```

This produces `<title>Авторизация | MPSTATS Academy</title>` for all auth pages (/login, /register, /forgot-password, /reset-password, /verify).

Keep the existing layout JSX completely unchanged.

**2. Update `apps/web/src/app/(main)/layout.tsx`:**

Add metadata export after imports, before the component function:

```typescript
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Личный кабинет',
  description: 'Ваш персональный кабинет MPSTATS Academy: диагностика навыков, обучение, прогресс.',
  robots: {
    index: false,
    follow: false,
  },
};
```

The `robots: noindex, nofollow` ensures protected pages are not indexed even if a crawler somehow reaches them. This produces `<title>Личный кабинет | MPSTATS Academy</title>`.

Keep all existing layout code unchanged.

**3. Create `apps/web/src/app/pricing/layout.tsx`:**

```typescript
import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Тарифы и цены',
  description: 'Выберите тарифный план MPSTATS Academy: подписка на отдельный курс или полный доступ к 400+ видеоурокам.',
  openGraph: {
    title: 'Тарифы и цены | MPSTATS Academy',
    description: 'Подписка на курсы по маркетплейсам: аналитика, маркетинг, контент, финансы.',
  },
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
```

Pricing is a public page so it gets its own OG overrides. The layout is a pass-through (pricing page already has its own full-page structure).
  </action>
  <verify>
    <automated>cd apps/web && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/(auth)/layout.tsx contains `export const metadata: Metadata`
    - apps/web/src/app/(auth)/layout.tsx contains `title: 'Авторизация'`
    - apps/web/src/app/(main)/layout.tsx contains `export const metadata: Metadata`
    - apps/web/src/app/(main)/layout.tsx contains `robots:` with `index: false`
    - apps/web/src/app/pricing/layout.tsx exists and contains `title: 'Тарифы и цены'`
    - apps/web/src/app/pricing/layout.tsx contains `openGraph:`
    - Build succeeds
  </acceptance_criteria>
  <done>Auth routes show "Авторизация | MPSTATS Academy", main routes show "Личный кабинет | MPSTATS Academy" with noindex, pricing shows "Тарифы и цены | MPSTATS Academy" with custom OG.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of SEO and error pages</name>
  <files>none</files>
  <action>
    Human verifies the complete SEO implementation and branded error pages visually in the browser.
  </action>
  <verify>
    <automated>echo "Checkpoint: requires human verification"</automated>
  </verify>
  <what-built>
    Per-page metadata on all routes, branded 404/500 error pages with Logo, Yandex Webmaster readiness.
  </what-built>
  <how-to-verify>
    1. Run `pnpm dev` and visit http://localhost:3000 — check page title is "MPSTATS Academy — образовательная платформа для селлеров"
    2. Visit http://localhost:3000/pricing — check page title contains "Тарифы и цены | MPSTATS Academy"
    3. Visit http://localhost:3000/login — check page title contains "Авторизация | MPSTATS Academy"
    4. Visit http://localhost:3000/nonexistent-page — should show branded 404 with MPSTATS Academy logo and "На главную" button linking to /
    5. Visit http://localhost:3000/sitemap.xml — should show XML with 4 URLs
    6. Visit http://localhost:3000/robots.txt — should show Disallow rules for protected routes
    7. Right-click page source on / — verify og:image, og:locale, og:type meta tags present
    8. **Yandex Webmaster:** If you have a verification code, provide it and I'll add the meta tag. Otherwise, approve and we'll add it when available.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues. Optionally provide Yandex Webmaster verification code.</resume-signal>
  <done>User has visually verified metadata, OG tags, error pages, sitemap and robots.txt in the browser.</done>
</task>

</tasks>

<verification>
- `pnpm build` succeeds
- Page titles render correctly via Next.js metadata system
- 404 page shows Logo and links to /
- Error pages show MPSTATS Academy branding
- All metadata visible in page source
</verification>

<success_criteria>
1. All error pages (not-found, error, global-error, main/error) show MPSTATS Academy Logo
2. not-found.tsx "На главную" links to / (not /dashboard)
3. Auth pages titled "Авторизация | MPSTATS Academy"
4. Main pages titled "Личный кабинет | MPSTATS Academy" with noindex
5. Pricing page titled "Тарифы и цены | MPSTATS Academy" with OG override
6. Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/27-seo-custom-error-pages-sitemap-robots-txt-og-404-500/27-02-SUMMARY.md`
</output>
