---
phase: 17-yandex-id-auth
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/register/page.tsx
  - apps/web/src/app/page.tsx
  - apps/web/tests/auth/no-google.test.ts
autonomous: false
requirements: [AUTH-02, AUTH-03]
user_setup:
  - service: supabase
    why: "Disable Google OAuth provider in Supabase dashboard"
    dashboard_config:
      - task: "Disable Google provider: Authentication -> Providers -> Google -> toggle OFF"
        location: "Supabase Dashboard (saecuecevicwjkpmaoot)"
      - task: "Create admin account for Egor Vasilev via email/password signup OR Yandex OAuth, then set isAdmin=true in UserProfile via Prisma Studio or SQL"
        location: "Supabase Dashboard -> SQL Editor: UPDATE \"UserProfile\" SET \"isAdmin\" = true WHERE email = 'egor@...';"

must_haves:
  truths:
    - "Login page shows 'Войти с Яндекс ID' button with red Я logo instead of Google button"
    - "Register page shows 'Продолжить с Яндекс ID' button with red Я logo instead of Google button"
    - "No Google OAuth references remain in codebase (no signInWithGoogle imports, no Google SVG)"
    - "Email/password login continues to work as fallback"
    - "Landing page has no Google OAuth mentions"
  artifacts:
    - path: "apps/web/src/app/(auth)/login/page.tsx"
      provides: "Login page with Yandex button"
      contains: "signInWithYandex"
    - path: "apps/web/src/app/(auth)/register/page.tsx"
      provides: "Register page with Yandex button"
      contains: "signInWithYandex"
  key_links:
    - from: "apps/web/src/app/(auth)/login/page.tsx"
      to: "apps/web/src/lib/auth/actions.ts"
      via: "import signInWithYandex"
      pattern: "signInWithYandex"
    - from: "apps/web/src/app/(auth)/register/page.tsx"
      to: "apps/web/src/lib/auth/actions.ts"
      via: "import signInWithYandex"
      pattern: "signInWithYandex"
---

<objective>
Replace Google OAuth UI with Yandex ID on login and register pages, remove all Google OAuth references from codebase, verify clean removal.

Purpose: Complete the user-facing auth transition from Google to Yandex ID. After this plan, users see only Yandex and email/password options.
Output: Updated login/register pages with Yandex branding, zero Google OAuth remnants.
</objective>

<execution_context>
@C:/Users/Zebrosha/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Zebrosha/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-yandex-id-auth/17-CONTEXT.md
@.planning/phases/17-yandex-id-auth/17-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->
From apps/web/src/lib/auth/actions.ts (after Plan 01):
```typescript
export async function signInWithYandex(): Promise<AuthResult>
// signInWithGoogle is REMOVED
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace Google with Yandex on login + register pages, clean landing</name>
  <files>
    apps/web/src/app/(auth)/login/page.tsx,
    apps/web/src/app/(auth)/register/page.tsx,
    apps/web/src/app/page.tsx,
    apps/web/tests/auth/no-google.test.ts
  </files>
  <behavior>
    - Test 1: grep for "signInWithGoogle" in src/ returns zero matches
    - Test 2: grep for "Google" in login/page.tsx and register/page.tsx returns zero matches
    - Test 3: login/page.tsx imports signInWithYandex from actions
    - Test 4: register/page.tsx imports signInWithYandex from actions
  </behavior>
  <action>
    1. Modify `apps/web/src/app/(auth)/login/page.tsx`:
       - Replace `import { signIn, signInWithGoogle }` with `import { signIn, signInWithYandex }`
       - Rename `handleGoogleSignIn` to `handleYandexSignIn`, call `signInWithYandex()` instead of `signInWithGoogle()`
       - Replace the Google OAuth button with Yandex ID button:
         - Yandex brand style: SVG of red "Я" letter (Cyrillic Ya) as logo on the left
         - Text: "Войти с Яндекс ID"
         - Button variant: outline, full width
         - onClick: handleYandexSignIn
       - Keep the "или" divider between email/password form and OAuth button
       - Keep all email/password form logic unchanged

    2. Modify `apps/web/src/app/(auth)/register/page.tsx`:
       - Same pattern as login: replace Google import/handler/button with Yandex
       - Button text: "Продолжить с Яндекс ID"
       - Same red "Я" SVG logo
       - Keep email/password/name registration form unchanged

    3. Check `apps/web/src/app/page.tsx` (landing page) — scan for any Google OAuth mentions, remove if found. Per user decision: Claude's discretion.

    4. Yandex "Я" SVG for both buttons (inline, not an external file):
       ```
       <svg className="w-5 h-5 mr-2" viewBox="0 0 24 24" fill="none">
         <circle cx="12" cy="12" r="12" fill="#FC3F1D"/>
         <path d="M13.63 7.18h-.92c-1.53 0-2.33.84-2.33 2.08 0 1.39.6 2.16 1.82 3.05l1 .73-2.92 4.96H8.73l2.6-4.38c-1.52-1.15-2.35-2.28-2.35-3.95 0-2.16 1.47-3.49 3.87-3.49h2.28V18h-1.5V7.18z" fill="white"/>
       </svg>
       ```

    5. Create `apps/web/tests/auth/no-google.test.ts`:
       - Use `fs.readFileSync` to read login/page.tsx and register/page.tsx
       - Assert no "signInWithGoogle" string present
       - Assert no "Google" string present in those files
       - Read actions.ts and assert no "signInWithGoogle" export
  </action>
  <verify>
    <automated>cd apps/web && npx vitest run tests/auth/no-google.test.ts && pnpm build</automated>
  </verify>
  <done>Login page shows "Войти с Яндекс ID" with red Я logo. Register page shows "Продолжить с Яндекс ID". Zero Google references in auth files. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Yandex ID auth pages and setup</name>
  <files>apps/web/src/app/(auth)/login/page.tsx, apps/web/src/app/(auth)/register/page.tsx</files>
  <action>
    Human verifies the complete Yandex ID OAuth replacement visually and functionally:
    - Login page: "Войти с Яндекс ID" button with red Я logo (replaces Google)
    - Register page: "Продолжить с Яндекс ID" button with red Я logo (replaces Google)
    - Backend: Full server-side OAuth flow (from Plan 01)
    - Email/password forms unchanged

    User must also complete external setup:
    - Register Yandex OAuth app at https://oauth.yandex.ru/client/new
    - Add YANDEX_CLIENT_ID, YANDEX_CLIENT_SECRET, SUPABASE_SERVICE_ROLE_KEY to .env
    - Disable Google provider in Supabase Dashboard
    - Create admin account for Egor Vasilev (isAdmin=true)
  </action>
  <verify>
    Steps for human verification:
    1. Run `pnpm dev` and open http://localhost:3000/login
    2. Verify: email/password form on top, "или" divider, "Войти с Яндекс ID" button with red Я logo on bottom
    3. Open http://localhost:3000/register
    4. Verify: registration form on top, "или" divider, "Продолжить с Яндекс ID" button with red Я logo on bottom
    5. Verify NO Google button or Google mentions on either page
    6. (Optional) If Yandex OAuth app is registered: click "Войти с Яндекс ID" and verify redirect to oauth.yandex.ru
    7. Verify email/password login still works with existing test account
  </verify>
  <done>User approves visual appearance. Google fully removed. Yandex OAuth functional (or ready for credentials). Admin account for Egor created.</done>
</task>

</tasks>

<verification>
1. `cd apps/web && npx vitest run tests/auth/` — all auth tests pass (oauth-provider, yandex-oauth, no-google)
2. `pnpm build` — clean build, no TypeScript errors
3. Visual: login and register pages show Yandex button, no Google traces
4. grep -r "signInWithGoogle" apps/web/src/ returns nothing
</verification>

<success_criteria>
- Login page: "Войти с Яндекс ID" button with correct Yandex branding
- Register page: "Продолжить с Яндекс ID" button with correct Yandex branding
- Zero Google OAuth references in entire src/ directory
- Email/password auth unchanged and working
- Build passes cleanly
- User approves visual appearance of auth pages
</success_criteria>

<output>
After completion, create `.planning/phases/17-yandex-id-auth/17-02-SUMMARY.md`
</output>
