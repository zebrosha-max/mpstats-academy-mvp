---
phase: 39-ai-content-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ai/src/generation.ts
  - packages/ai/src/__tests__/generation.test.ts
  - apps/web/src/app/(main)/learn/[id]/page.tsx
  - apps/web/src/components/diagnostic/DiagnosticHint.tsx
autonomous: true
requirements: [R42, R17, R18]

must_haves:
  truths:
    - "AI summary/chat output содержит Wildberries, а не Валберес/Вайлдберриз"
    - "Клик на таймкод в DiagnosticHint перематывает видео через playerRef.seekTo и скроллит к плееру"
    - "Клик на footnote [N] в SourceTooltip перематывает видео к нужному таймкоду"
    - "Таймкод-кнопка в DiagnosticHint подсвечивается amber на 800ms при клике"
  artifacts:
    - path: "packages/ai/src/generation.ts"
      provides: "fixBrandNames post-processing + system prompt brand instruction"
      contains: "fixBrandNames"
    - path: "packages/ai/src/__tests__/generation.test.ts"
      provides: "Unit tests for fixBrandNames regex"
      contains: "Валберес"
    - path: "apps/web/src/components/diagnostic/DiagnosticHint.tsx"
      provides: "Visual highlight state on timecode click"
      contains: "activeTimecode"
  key_links:
    - from: "packages/ai/src/generation.ts"
      to: "LLM response content"
      via: "fixBrandNames applied after response.choices[0]"
      pattern: "fixBrandNames"
    - from: "apps/web/src/app/(main)/learn/[id]/page.tsx"
      to: "DiagnosticHint"
      via: "onSeek prop uses playerRef.current?.seekTo"
      pattern: "playerRef\\.current\\??\\.seekTo"
---

<objective>
Исправить три бага из аудита: AI транслитерирует бренды (R42), таймкоды в подсказках не перематывают видео (R17), footnotes не скроллят к нужному моменту (R18).

Purpose: Пользователи видят корректные названия брендов в AI-текстах и могут кликать по таймкодам/footnotes для перемотки видео.
Output: Исправленные generation.ts, lesson page, DiagnosticHint + unit тесты на brand regex.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/ai/src/generation.ts
@apps/web/src/app/(main)/learn/[id]/page.tsx
@apps/web/src/components/diagnostic/DiagnosticHint.tsx
@apps/web/src/components/learning/SourceTooltip.tsx
</context>

<interfaces>
<!-- Existing interfaces executor needs -->

From apps/web/src/components/video/KinescopePlayer.tsx:
```typescript
export interface PlayerHandle {
  seekTo: (seconds: number) => Promise<void>;
}
// Used via: const playerRef = useRef<PlayerHandle>(null);
```

From apps/web/src/components/diagnostic/DiagnosticHint.tsx:
```typescript
interface DiagnosticHintProps {
  lessonId: string;
  hints: Array<{ question: string; userAnswer: string; correctAnswer: string; timecodes: Array<{ start: number; end: number; formatted: string }> }>;
  onSeek: (seconds: number) => void;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brand name fix — system prompt + regex post-processing + unit tests</name>
  <files>packages/ai/src/generation.ts, packages/ai/src/__tests__/generation.test.ts</files>
  <read_first>
    - packages/ai/src/generation.ts (full file — system prompts on lines 62-85 and 152-164, LLM response on lines 100 and 181-183)
    - packages/ai/src/openrouter.ts (MODELS and MODEL_CONFIG exports)
  </read_first>
  <behavior>
    - fixBrandNames('Валберес') === 'Wildberries'
    - fixBrandNames('Валбериса') === 'Wildberries'
    - fixBrandNames('Валберёсу') === 'Wildberries'
    - fixBrandNames('Вайлдберриз') === 'Wildberries'
    - fixBrandNames('Вайлдберис') === 'Wildberries'
    - fixBrandNames('работа на Валберес и Озон') === 'работа на Wildberries и Озон' (Озон не трогаем)
    - fixBrandNames('текст без брендов') === 'текст без брендов'
  </behavior>
  <action>
    Per D-01: Add brand preservation instruction to BOTH system prompts in generation.ts:
    - In `generateLessonSummary` system prompt (line 84, before closing backtick): add rule "- Названия маркетплейсов пиши ТОЛЬКО по-английски: Wildberries (не «Валберес»), Ozon (не «Озон»), MPSTATS"
    - In `generateChatResponse` system prompt (line 163, before closing backtick): add same rule

    Per D-02: Create and apply `fixBrandNames` function:
    - Export function `fixBrandNames(text: string): string` that does:
      `.replace(/Валбер[иеё]с(?:а|у|ом|е)?/gi, 'Wildberries')`
      `.replace(/Вайлдберриз/gi, 'Wildberries')`
      `.replace(/Вайлдберис/gi, 'Wildberries')`
    - Do NOT replace Озон — it's valid Russian spelling (per research Open Question 2)
    - Apply fixBrandNames to LLM response content on line 100: `const content = fixBrandNames(response.choices[0]?.message?.content || 'Ошибка генерации.');`
    - Apply fixBrandNames to LLM response content on line 181-183: wrap same pattern

    Create unit test file `packages/ai/src/__tests__/generation.test.ts`:
    - Import fixBrandNames from '../generation'
    - Test all behavior cases listed above
    - Use vitest (describe/it/expect)
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx vitest run packages/ai/src/__tests__/generation.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fixBrandNames" packages/ai/src/generation.ts
    - grep -q "Wildberries" packages/ai/src/generation.ts (system prompt instruction)
    - grep -q "Валбер" packages/ai/src/__tests__/generation.test.ts (test cases)
    - grep -c "fixBrandNames" packages/ai/src/generation.ts returns 4+ (function def + 2 call sites + export)
  </acceptance_criteria>
  <done>fixBrandNames exported, applied to both generateLessonSummary and generateChatResponse responses, both system prompts contain brand instruction, all unit tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Timecode seek fix — DiagnosticHint uses playerRef + visual highlight + scroll</name>
  <files>apps/web/src/app/(main)/learn/[id]/page.tsx, apps/web/src/components/diagnostic/DiagnosticHint.tsx</files>
  <read_first>
    - apps/web/src/app/(main)/learn/[id]/page.tsx (lines 270-290 for handleTimecodeClick, lines 600-630 for DiagnosticHint integration)
    - apps/web/src/components/diagnostic/DiagnosticHint.tsx (full file — timecode buttons rendering)
    - apps/web/src/components/learning/SourceTooltip.tsx (lines 50-65 for handleClick — verify scroll works)
  </read_first>
  <action>
    Per D-03: Fix DiagnosticHint.onSeek in lesson page (apps/web/src/app/(main)/learn/[id]/page.tsx):
    - Find the DiagnosticHint onSeek callback (around lines 613-625) that uses direct postMessage to iframe
    - Replace with: `onSeek={(seconds) => { playerRef.current?.seekTo(seconds); document.getElementById('video-player')?.scrollIntoView({ behavior: 'smooth', block: 'start' }); }}`
    - Use `block: 'start'` (not 'nearest') per research Pitfall 4 — DiagnosticHint is below the fold, user explicitly wants to see video

    Per D-04: Add visual highlight to DiagnosticHint timecode buttons (apps/web/src/components/diagnostic/DiagnosticHint.tsx):
    - Add state: `const [activeTimecode, setActiveTimecode] = useState<number | null>(null);`
    - Add ref for timer cleanup: `const highlightTimer = useRef<ReturnType<typeof setTimeout> | null>(null);`
    - Create `handleSeek(seconds: number, index: number)` that:
      1. Clears previous timer: `if (highlightTimer.current) clearTimeout(highlightTimer.current);`
      2. Sets active: `setActiveTimecode(index);`
      3. Calls `onSeek(seconds);`
      4. Sets timeout 800ms to clear: `highlightTimer.current = setTimeout(() => setActiveTimecode(null), 800);`
    - Update timecode button onClick to use `handleSeek(tc.start, i)` instead of direct `onSeek(tc.start)`
    - Update button className: when `activeTimecode === i` use `bg-amber-300 text-amber-800`, otherwise `bg-amber-100 text-amber-700 hover:bg-amber-200`
    - Add `transition-colors duration-300` to button className for smooth transition
    - Import useState, useRef from react

    Per D-05/D-06: Verify SourceTooltip scroll — research confirms SourceTooltip already uses handleTimecodeClick via onSourceSeek prop which does `playerRef.current?.seekTo(seconds)` + `scrollIntoView`. If SourceTooltip.handleClick already calls `onSeek` and scrollIntoView, no changes needed. If not — add scrollIntoView with `block: 'nearest'`.
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "playerRef.current" apps/web/src/app/\(main\)/learn/\[id\]/page.tsx (seekTo via playerRef, not postMessage)
    - grep -q "activeTimecode" apps/web/src/components/diagnostic/DiagnosticHint.tsx
    - grep -q "bg-amber-300" apps/web/src/components/diagnostic/DiagnosticHint.tsx
    - grep -q "scrollIntoView" apps/web/src/app/\(main\)/learn/\[id\]/page.tsx
  </acceptance_criteria>
  <done>DiagnosticHint timecode click perematches video via playerRef.seekTo, scrolls to player, and highlights clicked button amber for 800ms. SourceTooltip scroll verified working. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
1. `npx vitest run packages/ai/src/__tests__/generation.test.ts` — all brand regex tests pass
2. `npx tsc --noEmit --project apps/web/tsconfig.json` — no type errors
3. Manual: open lesson page, click timecode in DiagnosticHint — video seeks + scrolls + button highlights amber
4. Manual: click footnote [N] in summary — video seeks to correct timecode
</verification>

<success_criteria>
- fixBrandNames unit tests pass (7 cases)
- Both system prompts contain Wildberries instruction
- DiagnosticHint uses playerRef.seekTo (not postMessage)
- DiagnosticHint buttons highlight amber on click
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/39-ai-content-quality/39-01-SUMMARY.md`
</output>
