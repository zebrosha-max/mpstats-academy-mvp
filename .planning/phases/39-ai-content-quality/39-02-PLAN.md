---
phase: 39-ai-content-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/dedup-lessons.ts
autonomous: true
requirements: [R35]

must_haves:
  truths:
    - "Скрипт находит дубликаты уроков по videoId и выводит группы в dry-run режиме"
    - "С флагом --execute скрипт удаляет дубликаты, сохраняя урок с наименьшим order"
    - "LessonProgress переносится с удаляемого на оставляемый урок (с обработкой @@unique конфликта)"
  artifacts:
    - path: "scripts/dedup-lessons.ts"
      provides: "One-time dedup script with dry-run and execute modes"
      contains: "videoId"
  key_links:
    - from: "scripts/dedup-lessons.ts"
      to: "Prisma Lesson + LessonProgress"
      via: "findMany + groupBy videoId + delete duplicates"
      pattern: "prisma\\.lesson"
---

<objective>
Удалить дубликаты уроков из БД (баг R35 — уроки 4=6, 7=10 с одинаковым videoId). One-time скрипт с dry-run и execute режимами.

Purpose: Пользователи не видят дубликаты уроков в курсах.
Output: scripts/dedup-lessons.ts — запускается вручную.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/db/prisma/schema.prisma
</context>

<interfaces>
<!-- Key schema the executor needs -->

From packages/db/prisma/schema.prisma:
```prisma
model Lesson {
  id          String   @id @default(cuid())
  courseId    String
  title      String
  order      Int
  videoId    String?
  duration   Int      @default(0)
  course     Course   @relation(fields: [courseId], references: [id])
  progress   LessonProgress[]
  comments   LessonComment[]
  // ...
}

model LessonProgress {
  id        String   @id @default(cuid())
  pathId    String
  lessonId  String
  completed Boolean  @default(false)
  // ...
  path      LearningPath @relation(fields: [pathId], references: [id])
  lesson    Lesson       @relation(fields: [lessonId], references: [id], onDelete: Cascade)

  @@unique([pathId, lessonId])
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create dedup-lessons.ts script with dry-run and execute modes</name>
  <files>scripts/dedup-lessons.ts</files>
  <read_first>
    - packages/db/prisma/schema.prisma (Lesson model — fields id, courseId, title, order, videoId; LessonProgress — @@unique([pathId, lessonId]), onDelete: Cascade)
    - scripts/seed/seed-from-manifest.ts (first 30 lines — see how Prisma is imported in scripts)
  </read_first>
  <action>
    Per D-07 and D-08: Create `scripts/dedup-lessons.ts`:

    1. Import PrismaClient from `@prisma/client` (not from @mpstats/db — scripts run standalone)
    2. Parse CLI args: `--execute` flag means real deletion, default is dry-run
    3. Find all Lessons with non-null videoId, ordered by courseId asc, order asc
    4. Group by videoId using Map — groups with size > 1 are duplicates
    5. For each duplicate group:
       a. Keep lesson with smallest `order` (first in sorted list)
       b. Log: `KEEP: {id} course={courseId} order={order} title={title}`
       c. For each duplicate to remove: `DELETE: {id} course={courseId} order={order} title={title}`
    6. If `--execute`:
       a. For each duplicate lesson to remove:
          - Find all LessonProgress records for the duplicate
          - For each progress record, check if a conflicting record exists on the keep lesson (same pathId):
            - If conflict exists: delete the duplicate's progress (keep lesson already has progress)
            - If no conflict: update lessonId to keep lesson's id
          - Also handle LessonComment: find comments for duplicate, update lessonId to keep's id (no unique constraint, safe updateMany)
          - Delete the duplicate lesson
       b. Log summary: `Deleted {N} duplicate lessons, transferred {M} progress records`
    7. If dry-run: log `DRY RUN — use --execute to apply changes`
    8. Disconnect Prisma

    Handle the @@unique([pathId, lessonId]) constraint per research Pitfall 2:
    ```typescript
    const progRecords = await prisma.lessonProgress.findMany({ where: { lessonId: dup.id } });
    for (const prog of progRecords) {
      const existing = await prisma.lessonProgress.findUnique({
        where: { pathId_lessonId: { pathId: prog.pathId, lessonId: keep.id } }
      });
      if (existing) {
        await prisma.lessonProgress.delete({ where: { id: prog.id } });
      } else {
        await prisma.lessonProgress.update({ where: { id: prog.id }, data: { lessonId: keep.id } });
      }
    }
    ```

    Run format: `npx tsx scripts/dedup-lessons.ts` (dry-run) or `npx tsx scripts/dedup-lessons.ts --execute`
  </action>
  <verify>
    <automated>cd "D:/GpT_docs/MPSTATS ACADEMY ADAPTIVE LEARNING/MAAL" && npx tsx scripts/dedup-lessons.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "videoId" scripts/dedup-lessons.ts
    - grep -q "dry" scripts/dedup-lessons.ts (dry-run mode reference)
    - grep -q "execute" scripts/dedup-lessons.ts (execute flag)
    - grep -q "pathId_lessonId" scripts/dedup-lessons.ts (unique constraint handling)
    - grep -q "LessonProgress" scripts/dedup-lessons.ts (progress transfer)
  </acceptance_criteria>
  <done>Script runs in dry-run mode by default showing duplicate groups. With --execute flag, deletes duplicates keeping lowest-order lesson, transfers progress with unique constraint handling, transfers comments.</done>
</task>

</tasks>

<verification>
1. `npx tsx scripts/dedup-lessons.ts` — runs dry-run, outputs duplicate groups (if any)
2. Script handles LessonProgress @@unique conflict correctly (findUnique check before update)
3. Script handles LessonComment transfer (updateMany)
</verification>

<success_criteria>
- Script file exists at scripts/dedup-lessons.ts
- Dry-run mode outputs duplicate groups without modifying DB
- Execute mode deletes duplicates, transfers progress/comments
- @@unique constraint handled (no crash on conflicting progress records)
</success_criteria>

<output>
After completion, create `.planning/phases/39-ai-content-quality/39-02-SUMMARY.md`
</output>
